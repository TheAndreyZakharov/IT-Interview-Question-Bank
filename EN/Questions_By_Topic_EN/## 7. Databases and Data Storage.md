## 7. Databases and data storage

### 7.1 DBMS fundamentals and data modeling
- What are data, a database, and a DBMS?
- Why do we need a DBMS at all if data can be stored in files?
- What are the main functions of a DBMS?
- How does a database differ from a table?
- What is a database schema?
- What is a database instance?
- What are a record, row, field, and column?
- What are an attribute, entity, and relationship?
- Which data models do you know?
- What is the relational data model?
- How does a relational database differ from a non-relational one?
- Which types of DBMS do you know?
- What is the difference between OLTP and OLAP?
- How do operational databases differ from analytical ones?
- What are a data store, database, data warehouse, and data lake?
- In which cases are relational databases used?
- In which cases is the relational model inconvenient?
- What is NoSQL and which types of NoSQL stores do you know?
- How do key-value, document, wide-column, and graph databases differ from each other?
- What is ACID?
- What is BASE?
- Why is data integrity important in relational DBMSs?
- What is data consistency in the broad and narrow sense?
- What is data independence?
- What is logical and physical data independence?
- What is database normalization?
- Why is normalization needed?
- What problems does normalization solve?
- When can normalization be harmful?
- What is denormalization?
- When is denormalization justified?
- What trade-offs between normalization and denormalization do you know?
- What is an entity in a domain model?
- What are a business key and a surrogate key?
- When is it better to use a natural key, and when a surrogate key?
- What is a primary key?
- What properties should a primary key have?
- Can a primary key be composite?
- When is a composite primary key a good solution, and when is it a bad one?
- What is a foreign key?
- Why are foreign keys needed?
- Why do some teams deliberately avoid using foreign keys?
- What are the pros and cons of not using foreign keys at the database level?
- What is a unique key?
- How does UNIQUE differ from PRIMARY KEY?
- What is a candidate key?
- What is an alternate key?
- What is a super key?
- What is a surrogate key?
- What is NULL in a database?
- How does NULL differ from an empty string, zero, and false?
- Why does NULL often cause errors in modeling and querying?
- When is NULL acceptable, and when is it better to avoid it?
- What types of integrity constraints do you know?
- What is a domain constraint?
- What is entity integrity?
- What is referential integrity?
- What is a CHECK constraint?
- What is a NOT NULL constraint?
- What is a DEFAULT constraint?
- Where is it better to define constraints: in the application or in the database?
- Why is application-level validation insufficient without database constraints?
- What are lookup tables and reference tables?
- When should values be moved into a separate table, and when should they be stored as enums/strings?
- What are the pros and cons of enum types in a database?
- What is an ER model?
- What is an ER diagram?
- Which notations in ER diagrams do you know?
- What is relationship cardinality?
- What types of relationships exist: one-to-one, one-to-many, many-to-many?
- How is a one-to-one relationship implemented in a relational database?
- How is a one-to-many relationship implemented?
- How is a many-to-many relationship implemented?
- What is a junction table?
- When should a many-to-many relationship be turned into a separate entity?
- What is a mandatory vs optional relationship?
- What is a weak entity?
- What is an identifying vs non-identifying relationship?
- How would you model a hierarchy or tree in a relational database?
- What approaches to storing trees in SQL do you know?
- How would you model a category catalog with nesting?
- How would you model tags for an entity?
- How would you model the change history of an object?
- How would you model soft delete?
- What are the pros and cons of soft delete?
- How does soft delete affect unique constraints and indexes?
- How would you model an audit trail of changes?
- What is temporal data and what are temporal tables?
- How do you model data that changes over time?
- What are slowly changing dimensions?
- What is 1NF?
- What is 2NF?
- What is 3NF?
- What is BCNF?
- What are 4NF and 5NF?
- Do you need to know normal forms above 3NF in application development?
- How can you tell in practice that a table is poorly normalized?
- What is a functional dependency?
- What is a partial dependency?
- What is a transitive dependency?
- What is a multivalued dependency?
- What is data redundancy?
- What design anomalies do you know?
- What is an insertion anomaly?
- What is an update anomaly?
- What is a deletion anomaly?
- Give an example of a table with anomalies and explain how you would fix it.
- How can you tell that one table should be split into several?
- How can you tell that several tables should be merged?
- What is an aggregate in domain terms and how does it affect the database model?
- What is a bounded context and how does it affect the data model?
- Why is it not always a good idea to build one “ideal” universal data schema for the entire company?
- What is a physical data model?
- What is a logical data model?
- What is a conceptual data model?
- How do conceptual, logical, and physical models differ?
- How do you go from business requirements to a table schema?
- What is your process for designing a database from scratch?
- Where would you start data modeling for a new service?
- What questions should you ask the business before designing the schema?
- What would you clarify with an analyst or product manager before creating a data model?
- How do you choose data types for columns?
- Why is it important to choose the right data types?
- When should you use integer, bigint, numeric/decimal, float/double?
- Why is float usually not used for money?
- When should you use date, time, timestamp, timestamptz?
- How should you store date and time: locally or in UTC?
- How should you store time zones?
- How should you store monetary values and currency?
- How should you store percentages, ratings, and fractional values?
- How should you store phone numbers, emails, and addresses?
- Should calculated values be stored in the database?
- When should aggregates be stored, and when should they be calculated on the fly?
- What is derived data?
- When is storing derived data justified?
- What is a materialized view and how is it related to modeling?
- What is data partitioning at the model level?
- When should you start thinking about future sharding during design?
- Should you design a schema “for billions of records” from day one?
- How do you balance simplicity of the current model with future scaling?
- What is a multi-tenant data model?
- What multi-tenancy options exist at the database level?
- How would you model tenant_id?
- What are the risks of a shared-schema multi-tenant model?
- When should you use a separate database per customer, and when should you use a shared one?
- What are polymorphic relationships and why are they controversial?
- How do you implement a polymorphic association in a relational database?
- What problems do polymorphic associations create?
- How do you model nested JSON structures in a relational database?
- When is it acceptable to store JSON inside a relational table?
- Where is the line between “flexibility” and a “bad data model”?
- What is worse: a model that is too normalized or one that is too denormalized?
- How would you model a users and roles system?
- How would you model permissions and RBAC?
- How would you model orders, order items, and payments?
- How would you model a shopping cart?
- How would you model likes, follows, favorites?
- How would you model an event feed?
- How would you model nested comments?
- How would you model notifications?
- How would you model the history of an entity’s statuses?
- How would you model a state machine at the database level?
- How would you model a reference catalog of countries, currencies, and locales?
- How would you model text localization in a database?
- How would you model a document versioning system?
- How would you model attachments and files?
- How should binary data be stored: in the database or in external storage?
- What criteria influence the decision to store a file in the database or outside it?
- How would you model high-cardinality data?
- How would you model events and an event log?
- How do you design append-only tables?
- When is an append-only model better than regular update/delete?
- How do you model immutable data?
- What is event sourcing from the perspective of the storage model?
- When is event sourcing justified, and when is it overkill?
- How would you model idempotency keys?
- How would you model deduplication of incoming events or requests?
- How would you model a queue system using tables?
- When does a database start being used for the wrong purpose?
- What is a hotspot in a data model?
- How can a poor primary key choice affect performance?
- What are UUID, ULID, Snowflake-like IDs, and when should each be used?
- What are the pros and cons of UUID as a primary key?
- What should you choose for a primary key: int, bigint, or uuid?
- How does the key choice affect indexes and table sizes?
- What is natural ordering of records and how is it related to keys?
- Should every table have an auto-increment id?
- In what cases is a separate id column unnecessary?
- How would you model tables for analytics and reporting if the main database is OLTP?
- Why is the same model often a poor fit for both transactions and analytics?
- What is a star schema?
- What is a snowflake schema?
- How is a fact different from a dimension?
- How can you tell that part of the data should be moved to a separate storage system?
- What modeling anti-patterns do you know?
- What is a “catch-all table” and why is it bad?
- Why is the EAV model often considered an anti-pattern?
- When can EAV still be justified?
- Why does “one table for all entity types” often lead to problems?
- What are the downsides of having too many tables?
- What are the downsides of having too few tables?
- What compromise between query convenience and model purity do you consider optimal?
- What data should not be deleted carelessly even if the business asks for it?
- How is the data model related to audit, security, and compliance requirements?
- How do privacy requirements affect schema design?
- How would you model PII data?
- Should sensitive data be separated into dedicated tables?
- How would you model deletion/anonymization of personal data?
- How does data retention period affect the model?
- What is a data retention policy and how is it reflected in the schema?
- How do you design a model so that old data can be archived conveniently?
- How do you design a model so that reporting queries are convenient?
- How do you design a model to avoid N+1 issues at the application level?
- How do ORM characteristics affect the design of tables and relationships?
- Should you tailor the data model to the ORM?
- When is it acceptable to violate an “academically correct” model for performance reasons?
- What would you choose: strict model consistency or ease of development? What does it depend on?
- How would you explain to a non-technical person why a good data model is so important?
- How does a bad data model show up in production?
- What signals indicate that a data model should be reconsidered?
- Tell me about a case where you had to redesign a poor database schema.
- What trade-offs would you build into a model for an early-stage startup?
- What trade-offs would you build into a model for a large enterprise system?
- How do you document a data model?
- How do you keep ER diagrams and schemas up to date?
- What data modeling tools have you used?
- Should you design the schema on paper/in a diagram first, or can you go straight to code/DDL?
- How do you review data model changes within a team?
- What questions would you ask a junior/middle/senior candidate about data modeling?
- What modeling mistakes do junior developers make most often?
- What modeling mistakes do teams on a fast-growing project make most often?
- What database-level architectural decisions do you consider premature optimization?
- How would you design a data model for an e-commerce system?
- How would you design a data model for a chat system?
- How would you design a data model for a booking system?
- How would you design a data model for subscriptions and billing?
- How would you design a data model for a SaaS product with multiple organizations and users?
- How would you design a data model for a CMS or blog?
- How would you design a data model for event tracking and analytics?
- How would you design a data model for a system of roles, permissions, and audit?
- How would you migrate from a bad old model to a new one without long downtime?
- How do you assess the risks of changing the schema of a live database?
- How would you convince a team or management that the problem should be solved at the data model level rather than with a “hack” in code?

### 7.2 PostgreSQL

#### 7.2.1 Indexes
- What is an index in PostgreSQL and why is it needed?
- What problems does an index solve, and what does it not solve?
- In what cases does an index speed up a query, and in what cases might it not help at all?
- What are the main types of indexes in PostgreSQL?
- How does a B-Tree index differ from Hash, GIN, GiST, BRIN, and SP-GiST?
- Which index type does PostgreSQL use by default?
- Why is B-Tree suitable for most typical queries?
- How is an index physically related to a table?
- What is stored inside index pages?
- What data structures underlie a B-Tree index?
- How is an index different from sorting a table?
- Why does having an index not mean a query will always be fast?
- Why does PostgreSQL sometimes not use an index even when it exists?
- What is index selectivity?
- How does column cardinality affect the usefulness of an index?
- What is query cost in the context of index selection?
- Why is an index on gender, a boolean flag, or status often useless?
- Why is an index on a unique email usually useful?
- What is the difference between a regular index and a unique index?
- What is a primary key and what index does it create?
- What is a UNIQUE constraint and how is it related to an index?
- Do PRIMARY KEY and UNIQUE always create an index?
- What is a composite index?
- When should you use a composite index instead of several single-column ones?
- How does column order work in a composite index?
- Why is column order in a composite index critically important?
- What is the leftmost prefix rule in composite indexes?
- Will an index on `(a, b)` be useful for a query only on `b`?
- Will an index on `(a, b, c)` be useful for conditions on `a` and `c` if `b` is not used?
- How do you choose the order of fields in a composite index?
- Should the most selective field come first in a composite index?
- When does an index on `(created_at, id)` make sense instead of `(id, created_at)`?
- How does an index work for `WHERE col = ...`?
- How does an index work for `WHERE col > ...` and range queries?
- How does an index work for `BETWEEN`, `<`, `<=`, `>=`?
- How does an index work for `ORDER BY`?
- Can PostgreSQL use an index both for filtering and for sorting?
- When does an index help avoid an explicit sort?
- How does an index affect `ORDER BY ... LIMIT`?
- Why is an index especially useful for top-N queries?
- How does an index work for `LIKE 'abc%'`?
- Why does an index usually not help for `LIKE '%abc%'`?
- How can substring search be done efficiently in PostgreSQL?
- When is a trigram index needed and how is it related to `pg_trgm`?
- What is an expression index?
- When do you need an index on an expression, for example `LOWER(email)`?
- Why won’t an index on `email` help for `WHERE LOWER(email) = ...` if there is no expression index?
- What is a partial index?
- When is a partial index more useful than a full one?
- How can you use a partial index for soft delete?
- How can you use a partial index for tables with a flag like `is_active = true`?
- What are the pitfalls of partial indexes?
- What is a covering index in PostgreSQL?
- How are INCLUDE columns different from regular indexed columns?
- When is `INCLUDE` useful in an index?
- What is an index-only scan?
- Under what conditions can PostgreSQL perform an index-only scan?
- Why does the presence of an index still not guarantee an index-only scan?
- What is the visibility map and how does it affect index-only scans?
- Why is autovacuum important for index-only scans?
- What is a bitmap index scan?
- How does a bitmap index scan differ from an index scan and a seq scan?
- In what cases does PostgreSQL choose a bitmap heap scan?
- Can PostgreSQL use several indexes in one query?
- How does PostgreSQL combine several indexes through bitmap operations?
- What is recheck condition in a bitmap scan?
- What is a GIN index and for which data types is it useful?
- Where is GIN usually used: full-text search, arrays, jsonb?
- What is a GiST index and where is it used?
- What is the difference between GIN and GiST?
- When should you choose GIN, and when GiST, for full-text search?
- What is a BRIN index and when is it effective?
- Why is BRIN suitable for very large tables?
- For what kind of data is BRIN especially good?
- How does BRIN differ from B-Tree in terms of size and speed?
- What is SP-GiST and in what rare cases is it useful?
- When does a Hash index make sense in PostgreSQL?
- Why were Hash indexes rarely used for a long time?
- What is a bloom index and where can it be useful?
- How are arrays indexed in PostgreSQL?
- How do you index `jsonb`?
- What is the difference between `jsonb_ops` and `jsonb_path_ops` for GIN?
- What index should you choose for searching fields inside `jsonb`?
- How do you index full-text search in PostgreSQL?
- What is `tsvector` and how is it indexed?
- What index is needed for geodata/PostGIS?
- What is an operator class and why is it important for indexes?
- How does collation affect index usage?
- Can a different collation break expected index usage?
- How does an index affect INSERT?
- How does an index affect UPDATE?
- How does an index affect DELETE?
- Why do a large number of indexes slow down writes?
- How do you estimate how many indexes on a table is already too many?
- Why is it dangerous to index everything blindly?
- What is write amplification caused by indexes?
- How do indexes affect database size?
- How do you estimate index size?
- Why can an index be larger than expected?
- What is an index bloat?
- How does index bloat appear?
- How is table bloat different from index bloat?
- How do you determine that an index has become bloated?
- How do you deal with index bloat?
- How does `REINDEX` differ from `VACUUM`?
- When is `REINDEX CONCURRENTLY` needed?
- When is regular `REINDEX` dangerous in production?
- What does `CREATE INDEX CONCURRENTLY` do?
- How does `CREATE INDEX` differ from `CREATE INDEX CONCURRENTLY`?
- What limitations and downsides does `CREATE INDEX CONCURRENTLY` have?
- Can you roll back concurrent index creation?
- Why is `CREATE INDEX CONCURRENTLY` slower than regular `CREATE INDEX`?
- When is `CONCURRENTLY` mandatory in production?
- How do you drop an index without a long lock?
- What does `DROP INDEX CONCURRENTLY` do?
- How does an index affect locks during migrations?
- What locks are taken when creating an index?
- What locks are taken when dropping an index?
- How do you check what indexes exist on a table?
- How can you tell which indexes are actually used?
- How do you find unused indexes in PostgreSQL?
- Can an unused index be safely removed?
- Why may an index appear unused even though it is needed?
- What PostgreSQL system views help analyze indexes?
- What does `pg_stat_user_indexes` show?
- What does `pg_statio_user_indexes` show?
- What are `idx_scan`, `idx_tup_read`, `idx_tup_fetch`?
- How can you tell from statistics that an index is not very useful?
- How can you tell that an index is used only to support a constraint?
- How do you check whether a specific query uses an index?
- How do you measure the effect of adding an index?
- How would you perform an A/B check of an index’s usefulness on staging?
- How do you determine which index is needed for a specific query?
- How do you design indexes based on real query patterns?
- Why shouldn’t indexes be designed “just in case”?
- Should all foreign keys be indexed?
- Why is an index on a foreign key often needed in practice?
- What happens when deleting/updating a parent row without an index on the foreign key?
- How do indexes help JOINs?
- What indexes are needed for typical JOINs?
- What index is needed for a many-to-many table?
- What indexes would you put on a junction table `user_roles(user_id, role_id)`?
- Do you need two indexes `(user_id, role_id)` and `(role_id, user_id)`?
- What indexes are needed for pagination?
- Why does offset pagination scale poorly even if an index exists?
- What indexes are needed for keyset pagination?
- How would you design an index for a query like `WHERE tenant_id = ? AND created_at < ? ORDER BY created_at DESC LIMIT 50`?
- What indexes are needed for multi-tenant applications?
- When is a partial index useful in a multi-tenant scenario?
- How do you index soft-deleted records?
- How do you index rarely used “archived” records?
- What indexes are typically needed for audit/log/event tables?
- What indexes are usually harmful for append-only tables?
- When is it better to use BRIN instead of B-Tree for logs and events?
- How does a high share of UPDATEs affect index choice?
- Why is HOT update important in PostgreSQL?
- How do indexes interfere with HOT update?
- What is HOT update and when is it possible?
- Can adding an unnecessary index worsen updates by preventing HOT?
- How is fillfactor related to indexes and HOT updates?
- How does an index affect upsert (`INSERT ... ON CONFLICT`)?
- Why is a unique index critical for correct upsert behavior?
- How do indexes affect data deduplication?
- Can a unique index be partial?
- When is a partial unique index useful?
- How can you enforce uniqueness only for “active” records?
- When is a constraint better than creating a unique index directly, and vice versa?
- How does PostgreSQL decide between a seq scan and an index scan?
- What influences the planner: statistics, random_page_cost, effective_cache_size?
- How do outdated statistics affect index choice?
- How does `ANALYZE` affect index-based plan selection?
- Why can an index “suddenly stop being used” after a bulk data load?
- What do you do if the planner consistently chooses a bad index?
- Is it worth disabling seqscan to “force” PostgreSQL to use an index?
- Why is it dangerous to force the planner for the sake of indexes?
- When is denormalization more effective than trying to “save everything with indexes”?
- How do you tell that the problem is not a missing index but a bad query?
- How do you tell that the problem is not the query but a bad data model?
- What typical indexing anti-patterns have you seen in real projects?
- What indexes would you create for a users table?
- What indexes would you create for an e-commerce orders table?
- What indexes would you create for an events table with billions of rows?
- What indexes would you add for case-insensitive email search?
- What indexes would you add for full-text search on articles?
- What indexes would you add for a JSONB field with arbitrary attributes?
- When is an index in PostgreSQL the right solution, and when does it mean the data architecture should be revisited?
- How would you audit indexes in a large production database?
- How would you explain to a junior developer that “index = always faster” is a myth?
- How would you choose an indexing strategy for a high-write system?
- How would you choose an indexing strategy for an analytical read-heavy system?
- How would you document the decision to add a new index?
- What trade-offs would you explain to the team before adding several new indexes to a hot table?

#### 7.2.2 EXPLAIN and ANALYZE
- What is `EXPLAIN` in PostgreSQL?
- Why is `EXPLAIN` needed?
- What does `EXPLAIN` show, and what does it not show?
- How does `EXPLAIN` differ from `EXPLAIN ANALYZE`?
- When is it safe to use `EXPLAIN`, and when should you be careful with `EXPLAIN ANALYZE`?
- Why can `EXPLAIN ANALYZE` be dangerous on `UPDATE`, `DELETE`, and `INSERT`?
- Does `EXPLAIN ANALYZE` actually execute the query?
- What does `EXPLAIN (ANALYZE, BUFFERS)` do?
- What does `BUFFERS` add to the execution plan?
- What does `EXPLAIN (ANALYZE, VERBOSE)` show?
- What does `EXPLAIN (ANALYZE, FORMAT JSON)` show?
- When is the JSON plan format useful?
- How do you read an execution plan from top to bottom?
- How do you read an execution plan from bottom to top?
- What is a plan node?
- What main types of plan nodes do you know?
- What is a Seq Scan?
- What is an Index Scan?
- What is an Index Only Scan?
- What is a Bitmap Index Scan?
- What is a Bitmap Heap Scan?
- What is a Tid Scan?
- What is a Nested Loop?
- What is a Hash Join?
- What is a Merge Join?
- What is Sort?
- What is Incremental Sort?
- What is Aggregate?
- What is GroupAggregate?
- What is HashAggregate?
- What is Materialize?
- What is Limit?
- What is Append / Merge Append?
- What is Gather / Gather Merge?
- What is Parallel Seq Scan?
- What is Parallel Hash Join?
- What is CTE Scan?
- What is Subquery Scan?
- What do `cost=...` values mean in an execution plan?
- What do startup cost and total cost mean?
- Are these milliseconds or abstract units?
- How does PostgreSQL calculate cost?
- What affects the cost model in PostgreSQL?
- What do `rows=...` values mean in a plan?
- What does `width=...` mean?
- Why is `rows` an estimate rather than a fact?
- What do `actual time=...` and `actual rows=...` show?
- What does `loops=...` mean?
- How do you interpret a plan node that ran many times?
- Why is it important to look not only at the node type but also at loops?
- How do you identify the bottleneck in a plan?
- How do you determine the most expensive node in a plan?
- Which matters more: high cost or high actual time?
- Why can estimated rows and actual rows differ significantly?
- What does a large mismatch between estimate and actual mean?
- What problems are caused by cardinality estimation errors?
- How do statistics errors affect plan choice?
- What should you do if the planner is wrong about the number of rows?
- How does `ANALYZE` help the planner build a better plan?
- When do statistics become stale?
- What are autovacuum/analyze and how do they affect plans?
- How does the size of the statistics sample affect plan accuracy?
- What are extended statistics and why are they needed?
- When are regular statistics not enough?
- What is correlation between columns and how can it break planner estimates?
- Why may the planner choose Seq Scan instead of Index Scan?
- How do you tell when Seq Scan is normal rather than a problem?
- Why can Seq Scan be the best choice for a small table?
- Why can an index be worse for a query that returns a large fraction of the table?
- How do you tell from the plan that an index is being used inefficiently?
- How do you tell that sorting has become a bottleneck?
- How can you see from the plan that the query is spilling data to disk?
- What does `Sort Method: external merge Disk` mean?
- What is `work_mem` and how is it related to Sort/HashAggregate/HashJoin?
- How do you see in the plan that a hash table does not fit in memory?
- What does batching in Hash Join mean?
- How do you recognize insufficient `work_mem` from the plan?
- How do you tell from the plan that there is not enough memory for sorting?
- How do you interpret `Heap Fetches` in an Index Only Scan?
- Why can an Index Only Scan suddenly perform worse than expected?
- How do `VACUUM` and Index Only Scan/execution plans affect each other?
- What is recheck condition in a Bitmap Heap Scan?
- How do you understand why the planner chose Bitmap Heap Scan instead of Index Scan?
- How does EXPLAIN help identify an N+1 problem?
- How does EXPLAIN help analyze JOINs?
- How do you tell that Nested Loop has become a problem?
- When is Nested Loop a good choice?
- When is Hash Join the best choice?
- When is Merge Join the best choice?
- How do indexes affect the choice of JOIN type?
- How do you tell that a JOIN condition is written inefficiently?
- How do you determine from the plan which table is the “outer” and which is the “inner” in a join?
- How do you tell that a poor join order is hurting the plan?
- Can you influence join order in PostgreSQL?
- Do you need to rewrite the query if the planner chose a bad join strategy?
- How does EXPLAIN help analyze `ORDER BY ... LIMIT`?
- How do you tell from the plan that the query needs a proper index for sorting?
- How does EXPLAIN help analyze pagination with OFFSET?
- Why does `OFFSET 100000` often look bad in the plan?
- How do you compare offset pagination and keyset pagination through EXPLAIN?
- How does EXPLAIN help analyze GROUP BY?
- What is the difference between HashAggregate and GroupAggregate?
- When is HashAggregate worse than GroupAggregate?
- How does EXPLAIN help analyze DISTINCT?
- How do you tell that a CTE or subquery is being materialized?
- How did CTE behavior change in newer PostgreSQL versions?
- When does a CTE hurt query performance?
- How does EXPLAIN help analyze window functions?
- How does EXPLAIN help analyze subqueries and correlated subqueries?
- How do you recognize from the plan that a subquery is being executed repeatedly for each row?
- How does EXPLAIN help detect poor filter selectivity?
- How do you recognize unnecessary heap reads from the plan?
- What does `Rows Removed by Filter` show?
- What does `Rows Removed by Join Filter` show?
- Why are these fields useful during optimization?
- How do you tell from the plan that filtering is happening too late?
- How do you tell from the plan that a condition is not being pushed down?
- What is predicate pushdown in the context of query execution?
- How does EXPLAIN help analyze partition pruning?
- How do you tell that pruning did not work?
- How does EXPLAIN help analyze parallel query execution?
- How do you tell that a query is running in parallel?
- When does PostgreSQL decide to use parallelism?
- Why might a query not become parallel even though it seems suitable?
- What parameters affect the parallel query planner?
- How do you tell from the plan that parallelism is useless or even harmful?
- What is JIT in PostgreSQL?
- How can you see JIT in `EXPLAIN ANALYZE`?
- When does JIT speed up a query, and when does it only add overhead?
- How do you compare two execution plans?
- What do you look at first when comparing two plans?
- When comparing, what matters more: cost, actual time, rows, or buffers?
- How do you measure the effect of adding an index using EXPLAIN ANALYZE?
- How do you measure the effect of rewriting a query?
- How do you measure the effect of changing statistics?
- How do you measure the effect of changing planner parameters?
- How does EXPLAIN help tell that the problem is CPU, not I/O?
- How does `BUFFERS` help distinguish reading from shared buffers vs reading from disk?
- What do shared hit, shared read, shared dirtied, shared written mean?
- How do you tell from `BUFFERS` that a query is disk-bound?
- How do you tell from `BUFFERS` that a query is caching well?
- What do `Planning Time` and `Execution Time` mean?
- When does planning time itself become a problem?
- How do you analyze very complex plans with dozens of nodes?
- How do you visualize a plan tree more conveniently than reading raw text?
- What tools help analyze PostgreSQL plans?
- How do you use explain.dalibo / depesz / pgMustard and similar tools?
- How is EXPLAIN related to `pg_stat_statements`?
- How do you find slow queries before diving into EXPLAIN?
- Why shouldn’t you optimize a query only by the plan without understanding the real workload?
- Why can the same query have different plans on different data?
- Why can the same query have different plans on staging and production?
- What are parameter sniffing / generic plan / custom plan in PostgreSQL?
- How do prepared statements affect plans?
- Why can a prepared statement choose a suboptimal generic plan?
- How does EXPLAIN help in the case of a prepared statement?
- How can `LIMIT` change an execution plan?
- How can `ORDER BY` radically change a plan?
- How do conditions in `WHERE` affect pushdown and plan choice?
- How do OR conditions affect index usage and the plan?
- How does IN affect the plan?
- How does EXISTS differ from IN in terms of execution plan?
- How does JOIN differ from a subquery in terms of the plan?
- When does rewriting `OR` into `UNION ALL` help?
- How does EXPLAIN help justify such a query rewrite?
- How would you analyze a slow SELECT?
- How would you analyze a slow UPDATE?
- How would you analyze a slow DELETE?
- How would you analyze a slow UPSERT?
- How would you analyze a query that suddenly became slow after a release?
- How would you analyze a query that is fast locally but slow in production?
- How would you look for the cause of a bad plan: statistics, index, data structure, query text, or settings?
- How would you explain to a junior developer how to read EXPLAIN ANALYZE?
- What typical mistakes do developers make when reading EXPLAIN?
- Why can’t you conclude “there is a Seq Scan, so it’s bad”?
- Why can’t you look only at the top line of the plan?
- Why can’t you look only at total execution time?
- Which fields in EXPLAIN ANALYZE are the most important for you first?
- What signs in a plan almost always indicate a problem?
- What signs in a plan suggest the query is already optimal and the next step is redesign?
- When is a problem solved by an index, and when only by rewriting the query?
- When is a problem solved by refreshing statistics?
- When is a problem solved by partitioning?
- When is a problem solved by changing the data schema?
- When should you stop micro-optimizing a single query and think about an architectural solution?
- How would you build a systematic process for plan analysis in a team?
- What questions would you ask before proposing an optimization based on EXPLAIN ANALYZE?

#### 7.2.3 MVCC, Isolation Levels, Locks
- What is MVCC in PostgreSQL?
- Why does PostgreSQL use MVCC?
- How does MVCC help read data without constant locking?
- How is MVCC different from an approach where reads and writes block each other more strongly?
- What is a snapshot in PostgreSQL?
- When is a snapshot created for a transaction?
- Why can two transactions see different versions of the same rows?
- What are xmin and xmax of a row in PostgreSQL?
- How does PostgreSQL determine which row version is visible to the current transaction?
- What happens to a row during UPDATE in PostgreSQL from the perspective of MVCC?
- Why does UPDATE in PostgreSQL effectively create a new row version?
- What happens to the old row version after UPDATE or DELETE?
- What is a tuple in PostgreSQL and how is it related to MVCC?
- Why does MVCC increase disk space usage?
- How are MVCC and vacuum related?
- What is a transaction ID (XID) in PostgreSQL?
- Why is the transaction ID important for MVCC?
- What is transaction ID wraparound and why is it dangerous?
- What are hint bits and what are they for?
- What is the visibility map and what is it for?
- What is clog/pg_xact and how is it related to transaction visibility?
- How does a statement-level snapshot differ from a transaction-level snapshot?
- How does snapshot visibility work in READ COMMITTED?
- How does snapshot visibility work in REPEATABLE READ?
- What is the difference between READ COMMITTED and REPEATABLE READ in PostgreSQL?
- What isolation levels does PostgreSQL support?
- What isolation levels are defined by the SQL standard?
- Does PostgreSQL support READ UNCOMMITTED as distinct behavior?
- Why does READ UNCOMMITTED in PostgreSQL effectively behave like READ COMMITTED?
- What is a dirty read?
- What is a non-repeatable read?
- What is a phantom read?
- What is a lost update?
- What is write skew?
- What anomalies are allowed at READ COMMITTED?
- What anomalies are prevented by REPEATABLE READ in PostgreSQL?
- How is SERIALIZABLE in PostgreSQL different from “just very strict locking”?
- How does PostgreSQL implement SERIALIZABLE?
- What is Serializable Snapshot Isolation (SSI)?
- What are the advantages of SSI compared to coarse-grained locks?
- What is a serialization failure?
- Why may transactions need to be retried at SERIALIZABLE level?
- How should an application be designed if PostgreSQL uses retriable serialization failures?
- What is the difference between optimistic and pessimistic approaches to concurrent access?
- When is READ COMMITTED sufficient, and when is REPEATABLE READ needed?
- When is SERIALIZABLE truly required?
- What isolation level is used by default in PostgreSQL?
- Why was this isolation level chosen as the default?
- What happens if you run the same SELECT twice within one transaction at READ COMMITTED?
- What happens if you run the same SELECT twice within one transaction at REPEATABLE READ?
- How do SELECT, UPDATE, and DELETE behave with concurrent changes to the same rows?
- What is a row-level lock in PostgreSQL?
- What types of row-level locks exist in PostgreSQL?
- How do FOR UPDATE, FOR NO KEY UPDATE, FOR SHARE, and FOR KEY SHARE differ?
- When should you use SELECT ... FOR UPDATE?
- When should you use SELECT ... FOR SHARE?
- What happens if two transactions execute SELECT ... FOR UPDATE on the same row?
- How does SELECT ... FOR UPDATE differ from a regular SELECT?
- Why does a regular SELECT usually not block writes in PostgreSQL?
- What is a table-level lock in PostgreSQL?
- What table-level lock modes exist?
- What is ACCESS SHARE lock?
- What is ROW SHARE lock?
- What is ROW EXCLUSIVE lock?
- What is SHARE UPDATE EXCLUSIVE lock?
- What is ACCESS EXCLUSIVE lock?
- Which SQL operations take an ACCESS EXCLUSIVE lock?
- Why can ALTER TABLE unexpectedly block an application?
- What locks does CREATE INDEX take?
- How does CREATE INDEX CONCURRENTLY differ in locking from regular CREATE INDEX?
- What locks does VACUUM take?
- What locks does ANALYZE take?
- What locks does UPDATE take?
- What locks does DELETE take?
- What locks does INSERT take?
- How do you determine which locks are compatible with each other?
- Where can you see current locks in PostgreSQL?
- What does the pg_locks system view show?
- How do you combine pg_locks with pg_stat_activity to understand who is blocking whom?
- How do you find the blocking session in PostgreSQL?
- How do you determine which transaction is holding a lock the longest?
- What is a lock queue?
- What is lock timeout?
- How does lock_timeout differ from statement_timeout?
- What is a deadlock?
- How does PostgreSQL detect a deadlock?
- What happens when PostgreSQL finds a deadlock?
- What error message does the application get on a deadlock?
- How do you design code to reduce the chance of deadlocks?
- Why does updating tables and rows in a consistent order help fight deadlocks?
- Give an example of a deadlock between two transactions.
- How is a deadlock different from long blocking?
- How do you distinguish a true deadlock from simply long waiting?
- What are advisory locks in PostgreSQL?
- How does an advisory lock differ from a row lock?
- When is it appropriate to use advisory locks?
- What risks come with using advisory locks at the application level?
- How does a session-level advisory lock differ from a transaction-level advisory lock?
- Can PostgreSQL be used as a distributed locking mechanism?
- What limitations do advisory locks have as a distributed lock?
- What is predicate locking in SERIALIZABLE and why is it needed?
- Why are predicate locks important for preventing phantom/write skew anomalies?
- What is SKIP LOCKED and why is it needed?
- When is SELECT ... FOR UPDATE SKIP LOCKED useful for job queues?
- What risks and trade-offs are there with SKIP LOCKED?
- What is NOWAIT in the context of locking?
- When should NOWAIT be used instead of regular waiting?
- Which strategy should you choose: wait for the lock, fail immediately, or retry?
- What does idle in transaction mean and why is it harmful?
- Why are long-running transactions dangerous for MVCC?
- How do long-running transactions affect vacuum and table growth?
- Why does an open transaction “hold onto” old row versions?
- How can ORM behavior accidentally create long transactions?
- Why is autocommit often safer than a long manual transaction for simple operations?
- How do you choose transaction boundaries in an application?
- What problems arise if you keep a transaction open during network calls?
- What is a lost update in business logic and how does PostgreSQL help/not help protect against it?
- How do you deal with lost update: SELECT FOR UPDATE, version column, SERIALIZABLE?
- How does optimistic locking at the application level differ from PostgreSQL locks?
- How do you implement optimistic locking using a version/update timestamp?
- What anomalies are still possible at READ COMMITTED even if “everything seems to work”?
- Why is write skew especially important for financial and scheduling scenarios?
- Give a real business-case example of write skew.
- How does isolation affect performance?
- Why is the strictest isolation level not always the best choice?
- What would you choose for a typical CRUD API: READ COMMITTED, REPEATABLE READ, or SERIALIZABLE?
- Which isolation level would you choose for a banking money transfer operation and why?
- Which isolation level would you choose for analytical reports and why?
- When is it better to solve a concurrency problem with constraints rather than locks?
- How do UNIQUE constraints help avoid some race conditions?
- Can database-level locking fully replace proper business logic?
- How do you test race conditions and deadlocks in PostgreSQL?
- What queries and metrics would you use to monitor locks in production?
- What signs in a system indicate problems with MVCC and locking?
- What would you do if the number of blocked queries suddenly increased in production?
- How do you safely investigate locks in production without making the problem worse?
- What anti-patterns most often lead to MVCC and lock contention problems?
- What is hardest to explain to a junior developer about MVCC in PostgreSQL?
- If you had to explain MVCC briefly to a manager without a technical background, how would you do it?

#### 7.2.4 Replication, Vacuum, Bloat
- What is replication in PostgreSQL?
- Why is replication needed?
- What tasks are usually solved with replication?
- How is replication different from backups?
- Why does having a replica not replace backup strategy?
- What kinds of replication are available in PostgreSQL?
- What is streaming replication?
- What is physical replication?
- How does physical replication differ from logical replication?
- When is it better to use physical replication?
- When is it better to use logical replication?
- What are primary and replica in PostgreSQL?
- What is a standby server?
- What is WAL and what is it for?
- How is WAL related to replication?
- What exactly is replicated in physical replication?
- What exactly is replicated in logical replication?
- What are WAL sender and WAL receiver?
- What is a replication slot?
- Why are replication slots needed?
- How does a physical replication slot differ from a logical replication slot?
- What risks come with replication slots?
- What happens if a replica with a replication slot stops consuming WAL?
- Why can a “stuck” replication slot cause the primary to run out of disk space?
- What is replication lag?
- How do you measure replication lag?
- Why can lag be measured in bytes, time, and LSN?
- What is an LSN in PostgreSQL?
- How do you tell that a replica is critically lagging behind?
- What are the most common causes of replication lag?
- How do network problems affect lag?
- How do heavy queries on a replica affect replication delay?
- How can vacuum on primary conflict with long-running queries on replica?
- What is hot standby?
- Can you run SELECTs on a replica?
- Can you write to a standard physical replica?
- What is hot_standby_feedback?
- Why is hot_standby_feedback needed?
- What are the pros and cons of hot_standby_feedback?
- Why can hot_standby_feedback increase bloat on the primary?
- What is synchronous replication?
- How does synchronous replication differ from asynchronous replication?
- What are the pros and cons of synchronous replication?
- When is synchronous replication truly needed?
- How does synchronous replication affect write latency?
- What is synchronous_standby_names?
- Can quorum-based synchronous replication be configured?
- How is synchronous replication useful for durability?
- Why is even synchronous replication not equal to a full DR plan?
- What is failover?
- How does failover differ from switchover?
- What is replica promotion?
- How do you promote a standby to primary?
- What needs to be considered during failover to avoid data loss?
- What is a timeline in PostgreSQL and what is it for?
- Why do new timelines appear after failover?
- How does cascading replication work?
- When is cascading replication useful?
- What are the risks of cascading replication?
- What is logical decoding?
- What are publication and subscription in logical replication?
- What objects and changes can be replicated logically?
- What limitations does logical replication have?
- Why is logical replication convenient for migrations and selective replication?
- When is logical replication more convenient for zero-downtime migration?
- Can only selected tables be replicated?
- Can replication work between different major PostgreSQL versions?
- How do physical and logical replication differ for upgrades?
- How is a major PostgreSQL upgrade usually done with minimal downtime?
- What is a base backup?
- How and why is initial sync done for a new replica?
- What is pg_basebackup?
- What pitfalls exist when creating a new replica from a large primary?
- How do you monitor replication state in PostgreSQL?
- What does pg_stat_replication show?
- What does pg_stat_wal_receiver show?
- What should you do if replication lag suddenly increases?
- What actions should be taken if a replica stops applying WAL?
- How do you tell whether the problem is network, disk, CPU, or the queries themselves?
- Why can a replica conflict with vacuum cleanup records?
- What is a conflict on recovery?
- Why can a query on a replica be canceled because of a recovery conflict?
- What settings affect replica behavior during recovery conflicts?
- What is max_standby_streaming_delay?
- How do you choose between “not cancel queries on replica” and “not delay cleanup on primary”?
- When is reading from replicas dangerous for business logic?
- What consistency problems arise when reading from lagging replicas?
- How do you communicate to the application that data on a replica may be stale?
- What is read-after-write consistency and why is it non-trivial with replicas?
- What approaches can be used if you need to guarantee reading fresh data right after a write?
- When should you read only from primary, and when can reads be distributed to replicas?
- What is split-brain and how relevant is it for PostgreSQL failover scenarios?
- What external tools are commonly used for failover orchestration?
- How do Patroni/repmgr/pg_auto_failover conceptually help?
- What risks come with automatic failover?
- What would you verify after failover?
- How do you make sure the old primary does not start accepting writes after failover?
- What is fencing in the context of failover?
- What is vacuum in PostgreSQL?
- Why is vacuum needed?
- What happens if you do not run vacuum?
- How does VACUUM differ from VACUUM FULL?
- Why is VACUUM FULL considered a heavy operation?
- What locks does VACUUM FULL take?
- Why shouldn’t VACUUM FULL be run casually in production?
- How is regular VACUUM useful for MVCC?
- What does vacuum remove, and what does it not remove?
- Why does regular VACUUM not always reduce the on-disk file size of a table?
- What is autovacuum?
- Why is autovacuum needed?
- How does autovacuum solve MVCC problems?
- What main autovacuum parameters do you know?
- What is autovacuum_vacuum_threshold?
- What is autovacuum_vacuum_scale_factor?
- What is autovacuum_analyze_threshold?
- What is autovacuum_analyze_scale_factor?
- What is autovacuum_freeze_max_age?
- What is freeze in PostgreSQL?
- Why is freezing old XIDs necessary?
- What is anti-wraparound vacuum?
- Why is anti-wraparound vacuum critically important?
- What will happen if PostgreSQL approaches wraparound and vacuum does not keep up?
- What is relfrozenxid?
- How do you identify which tables are close to wraparound trouble?
- What is analyze and why is it needed?
- How does ANALYZE differ from VACUUM?
- Why does autovacuum usually include both vacuum and analyze?
- How do outdated statistics lead to poor query plans?
- What is bloat in PostgreSQL?
- Why does table bloat occur?
- Why does index bloat occur?
- Why does an UPDATE-heavy workload cause bloat?
- How do long-running transactions affect bloat?
- How can hot_standby_feedback increase bloat?
- How is fillfactor related to bloat and HOT updates?
- What is a HOT update?
- Under what conditions can PostgreSQL perform a HOT update?
- Why are HOT updates useful for reducing index bloat?
- What prevents a HOT update from happening?
- How does the presence of indexes affect the cost of UPDATE?
- How do frequent UPDATEs on indexed columns affect bloat?
- How do you diagnose table bloat?
- How do you diagnose index bloat?
- What system views and extensions are used to estimate bloat?
- How do you know when a table or index needs maintenance?
- When is regular VACUUM enough, and when do you need REINDEX?
- When is REINDEX CONCURRENTLY appropriate?
- When should pg_repack be used?
- How does pg_repack differ from VACUUM FULL?
- What trade-offs does pg_repack have?
- Why does bloat degrade performance even when there is enough disk space?
- How does bloat affect cache hit ratio and I/O?
- Why can bloat lead to degraded EXPLAIN plans and scan cost?
- How do you set up regular maintenance for tables with high churn?
- Which tables suffer from bloat most often?
- How does soft delete affect bloat?
- How can database-backed job queues cause bloat?
- How does partitioning help reduce vacuum and bloat issues?
- How does autovacuum behave on very large tables?
- Why are “the same global autovacuum settings for all tables” often a bad idea?
- When should autovacuum be tuned per table?
- What autovacuum parameters would you make more aggressive for a high-write table?
- What vacuum and bloat metrics would you monitor in production?
- How do you tell that autovacuum is not keeping up?
- What symptoms indicate autovacuum problems?
- How does high I/O load affect vacuum efficiency?
- Can vacuum itself create noticeable load on the system?
- How do you balance vacuum aggressiveness with production load?
- What are vacuum cost delay and vacuum cost limit?
- How do vacuum cost settings affect performance and maintenance?
- Why can autovacuum seem to “interfere,” but turning it off is almost always a bad idea?
- In what rare cases is autovacuum temporarily disabled or reconfigured?
- What do you do if a table size in production grows several times over a short period?
- How do you distinguish normal data growth from bloat?
- What should you do if pg_wal grows quickly because of a replication slot and/or lag?
- How do you investigate a “disk is running out on primary” situation in PostgreSQL?
- How are long-running transactions, replication lag, and vacuum related?
- How would you explain to a junior engineer the chain MVCC → dead tuples → vacuum → bloat?
- What plan of action would you propose if production simultaneously has high replica lag, table bloat, and slow queries?
- What application-level architectural decisions reduce vacuum and bloat issues?
- When does PostgreSQL require especially careful operations for high-write/high-delete workloads?
- What questions about replication, vacuum, and bloat would you ask a candidate for a senior backend / DBA role?

### 7.3 MySQL

#### 7.3.1 InnoDB, Transactions, and Locks
- What is InnoDB and how does it differ from other storage engines in MySQL?
- Why is InnoDB the default storage engine in MySQL?
- What key capabilities does InnoDB provide compared to MyISAM?
- In what scenarios is InnoDB the best fit?
- In what scenarios might InnoDB be overkill?
- What is ACID and how does InnoDB implement each of its properties?
- What is a transaction in MySQL?
- What commands are used to manage transactions?
- What is autocommit and how does it affect query behavior?
- When does it make sense to disable autocommit?
- What happens if you open a transaction and forget to COMMIT or ROLLBACK?
- What is SAVEPOINT and how is it used?
- Can you roll back only part of a transaction? How is that done in MySQL?
- What is the redo log in InnoDB?
- What is the undo log in InnoDB?
- What is the difference between the redo log and the undo log?
- How does InnoDB recover after a server crash?
- What is crash recovery?
- What is the doublewrite buffer and what problem does it solve?
- What is the buffer pool?
- Why is the size of the buffer pool critical for InnoDB performance?
- What is a dirty page?
- When are dirty pages flushed to disk?
- What is a checkpoint in InnoDB?
- What is purge in InnoDB?
- Why can long-running transactions interfere with purge?
- What is history list length and why is it important?
- How do long-running transactions affect InnoDB performance?
- How does InnoDB physically store rows?
- What is a clustered index in InnoDB?
- Why is the primary key especially important in InnoDB?
- What happens if a table in InnoDB has no primary key?
- Why can a random UUID as a primary key be a bad idea for InnoDB?
- Why is an auto-increment key often better for InnoDB in terms of inserts?
- What is row format in InnoDB?
- What row formats does InnoDB support?
- What is the difference between COMPACT, DYNAMIC, and COMPRESSED?
- How are large TEXT/BLOB fields stored in InnoDB?
- What is MVCC in InnoDB?
- How is MVCC related to the undo log?
- What is a read view?
- How does InnoDB determine which row version to show to a transaction?
- What is a snapshot read?
- What is a current read?
- How does a regular SELECT differ from SELECT ... FOR UPDATE?
- How does SELECT ... FOR UPDATE differ from SELECT ... FOR SHARE / LOCK IN SHARE MODE?
- When do you need a locking SELECT and when is a regular SELECT enough?
- What isolation levels does MySQL InnoDB support?
- What isolation level is used by default in InnoDB?
- What is READ UNCOMMITTED?
- What is READ COMMITTED?
- What is REPEATABLE READ?
- What is SERIALIZABLE?
- How do READ COMMITTED and REPEATABLE READ differ in InnoDB?
- What read anomalies exist?
- What is a dirty read?
- What is a non-repeatable read?
- What is a phantom read?
- How does InnoDB prevent phantom reads?
- Why does REPEATABLE READ behavior in InnoDB differ from some other databases?
- What is a gap lock?
- What is a record lock?
- What is a next-key lock?
- What is an insert intention lock?
- What is the difference between a record lock and a gap lock?
- Why is next-key locking needed?
- When does InnoDB set gap locks?
- Why can a range query block insertion of new rows?
- Why does the lack of a proper index increase the lock scope?
- What are intention locks?
- What is a shared lock?
- What is an exclusive lock?
- What is a metadata lock in MySQL?
- Why can DDL hang even when no one is explicitly modifying data?
- What does ALTER TABLE block?
- What is online DDL in MySQL and what are its limitations?
- What is a deadlock?
- How does a deadlock differ from a lock wait timeout?
- How does InnoDB detect a deadlock?
- What happens when InnoDB finds a deadlock?
- Why is one of the transactions chosen as the victim?
- Where in MySQL can you view information about a deadlock?
- How do you read the output of SHOW ENGINE INNODB STATUS?
- What are the typical causes of deadlocks in applications?
- How do you reduce the likelihood of deadlocks?
- Why is it important to update data in the same order?
- Why are large transactions dangerous for concurrency?
- Why is UPDATE without an index especially dangerous?
- How can SELECT ... FOR UPDATE affect system throughput?
- What is lock wait timeout?
- How does innodb_lock_wait_timeout affect application behavior?
- Can you catch a lock timeout and safely retry the transaction?
- What is optimistic locking and how do you implement it in MySQL?
- What is pessimistic locking and when is it appropriate?
- How do you implement row versioning for optimistic locking?
- In what scenarios is optimistic locking better than pessimistic locking?
- In what scenarios is pessimistic locking better than optimistic locking?
- Why can identical SQL queries behave differently under different isolation levels?
- How does REPEATABLE READ affect consistency of reads in a long transaction?
- Can a regular SELECT in one transaction see old data while SELECT ... FOR UPDATE sees new data?
- Why is that possible?
- What is lost update and how can you avoid it in MySQL?
- What is write skew and does InnoDB prevent it?
- How does SERIALIZABLE affect performance?
- Why is SERIALIZABLE rarely used in high-load systems?
- What is an auto-increment lock?
- How does auto-increment work in InnoDB with concurrent inserts?
- What is innodb_autoinc_lock_mode?
- How do different auto-inc lock modes affect parallelism?
- What problems can long-running transactions cause in replication?
- Why can an open transaction interfere with removal of old row versions?
- How do transactions affect the binlog?
- How does InnoDB interact with the binary log to ensure consistency?
- What are XA transactions in MySQL?
- When are XA transactions needed, and when is it better to avoid them?
- What pitfalls does XA have in MySQL?
- What is the redo log flush policy?
- How does innodb_flush_log_at_trx_commit affect reliability and performance?
- What do the values 0, 1, and 2 mean for innodb_flush_log_at_trx_commit?
- How does sync_binlog affect durability?
- Which combinations of innodb_flush_log_at_trx_commit and sync_binlog are safe, and which are risky?
- How would you explain the difference between durability on paper and actual durability in production?
- What will be faster: one large transaction or many small ones? What does it depend on?
- How can you find which transactions are currently holding locks?
- How can you view active transactions in MySQL?
- How can you see who is blocking whom?
- Which Performance Schema / INFORMATION_SCHEMA tables are useful for lock analysis?
- How would you diagnose a production incident when all queries suddenly “hang”?
- How would you investigate an increase in lock wait timeout in production?
- How would you investigate a sudden increase in deadlocks?
- How would you design tables and queries to reduce the likelihood of lock conflicts?
- What trade-offs between consistency, isolation, and throughput would you explain to the team?
- When does it make sense to lower the isolation level?
- When is that dangerous?
- What questions would you ask the team before changing isolation from REPEATABLE READ to READ COMMITTED?
- What common mistakes do developers make when working with transactions in MySQL?
- What anti-patterns when working with InnoDB have you seen in practice?
- What would you check first if InnoDB suddenly started slowing down on writes?
- What would you check first if the system is bottlenecked by locks rather than CPU?
- How would you explain to a junior developer why “just wrap everything in a transaction” is not always a good idea?

#### 7.3.2 Indexes and the Optimizer
- What is an index in MySQL?
- Why are indexes needed?
- What types of indexes does MySQL support?
- What is a B-Tree index?
- What is a Hash index and where is it used in MySQL?
- Which indexes are actually used most often in InnoDB?
- What is a clustered index?
- What is a secondary index in InnoDB?
- How does a secondary index in InnoDB differ from a secondary index in other DBMSs?
- Why does a secondary index in InnoDB store the primary key value?
- How does that affect index size?
- Why does a long primary key make all secondary indexes heavier?
- What is a composite index?
- How does a composite index work?
- What is the leftmost prefix rule?
- Why is the order of columns in a composite index so important?
- How do you choose the order of columns in a composite index?
- Which is more efficient: an index `(a, b)` or two separate indexes `(a)` and `(b)`?
- In which cases does an index `(a, b)` cover a query on `a`, but not a query on `b`?
- What is a covering index?
- Why can a covering index be significantly faster than a regular index?
- What is index condition pushdown?
- How does ICP affect performance?
- What is index cardinality?
- Why can low selectivity make an index useless?
- What is index selectivity?
- Why does an index on a boolean/status field often not help?
- In which cases is an index on a low-cardinality column still useful?
- What is a prefix index?
- When does a prefix index make sense?
- What pitfalls does a prefix index have?
- Can you index TEXT and BLOB in MySQL?
- What is a functional index in MySQL?
- When can a functional index replace computations in WHERE?
- What are generated columns and how do they help with indexing?
- What is an invisible index?
- Why are invisible indexes needed?
- How do you use an invisible index to safely test whether an index is necessary?
- What is a descending index and how important is it in MySQL?
- How does MySQL use an index for ORDER BY?
- When does an index help for ORDER BY, and when does it not?
- How does MySQL use an index for GROUP BY?
- What does “Using filesort” mean in EXPLAIN?
- What is filesort and why is the name misleading?
- When is filesort normal, and when is it a problem?
- What does “Using temporary” mean in EXPLAIN?
- Why can a temporary table hurt performance?
- What is a full table scan?
- Why is a full table scan sometimes better than a bad index?
- Why might the optimizer not use an index even if it exists?
- Why is an index not used on a small table?
- Why might an index not be used because of poor statistics?
- Why can functions applied to a column break index usage?
- Why is `WHERE DATE(created_at) = ...` often bad?
- Why can type conversion in a condition prevent index usage?
- Why does `LIKE '%abc'` use a B-Tree index poorly?
- When does `LIKE 'abc%'` use an index, and when does it not?
- How does collation affect index usage?
- Can OR break index usage?
- When can MySQL combine multiple indexes via index merge?
- What is index merge?
- Why is index merge often worse than one good composite index?
- What is skip scan and does MySQL have it?
- How does MySQL choose an index for a query?
- What is a cost-based optimizer?
- What are MySQL query cost estimates based on?
- What statistics does the MySQL optimizer use?
- What are persistent optimizer statistics?
- What is a histogram in MySQL?
- When do histograms help the optimizer?
- How do you collect or update statistics in MySQL?
- What does ANALYZE TABLE do?
- When can ANALYZE TABLE help a bad plan?
- What is optimizer trace?
- When is it useful to use optimizer trace instead of simple EXPLAIN?
- What does EXPLAIN show in MySQL?
- What is the difference between EXPLAIN and EXPLAIN ANALYZE in MySQL?
- How do you interpret type in EXPLAIN?
- What do ALL, index, range, ref, eq_ref, const, and system mean in EXPLAIN?
- Which join type is considered good, and which is bad?
- What does key mean in EXPLAIN?
- What does possible_keys mean?
- What does rows mean?
- What does filtered mean?
- How can you tell from EXPLAIN that a query reads too many rows?
- How can you tell from EXPLAIN that the chosen index is poor?
- What is a sargable predicate?
- How do you rewrite a condition so that it becomes sargable?
- Why do expressions on columns often make a query non-sargable?
- How does LIMIT affect plan selection?
- How does OFFSET affect performance?
- Why can `LIMIT 100000, 20` be very expensive?
- What index is needed for keyset pagination?
- Why is keyset pagination often better than OFFSET pagination?
- How do you optimize a query with a large OFFSET in MySQL?
- How does MySQL execute JOINs?
- Which JOIN algorithms does MySQL use?
- What is a nested loop join?
- When does a nested loop join become expensive?
- What is a block nested loop join?
- Is there a hash join in MySQL, and in which versions/scenarios is it applicable?
- How does join order affect performance?
- Can the optimizer reorder tables in a JOIN on its own?
- When should optimizer hints be used?
- Which optimizer hints does MySQL support?
- When is FORCE INDEX justified, and when is it a bad idea?
- How does USE INDEX differ from FORCE INDEX and IGNORE INDEX?
- Why are hints often a temporary workaround?
- How do you design indexes for frequent queries rather than for an abstract schema?
- Why should you not blindly add indexes to all columns?
- How do indexes affect INSERT, UPDATE, and DELETE?
- Why does a large number of indexes slow down writes?
- How do you determine that an index is redundant?
- How do you find unused indexes in MySQL?
- Which metrics and tools do you use to find unused indexes?
- When do duplicate indexes appear in a schema and why is that bad?
- What are redundant indexes?
- How do you determine that an index `(a, b)` makes an index `(a)` unnecessary, and when does it not?
- What is a hot spot in an index?
- Why is a monotonically increasing primary key useful for inserts, but can create a hot page?
- How does page split affect index performance?
- What is index fragmentation in MySQL?
- How can you tell that an index or table is fragmented?
- Is “index defragmentation” in MySQL needed in the same way as in some other DBMSs?
- What is online index creation in MySQL?
- What are the risks of adding an index to a large table in production?
- How do you safely add an index to a heavily loaded table?
- What are pt-online-schema-change and gh-ost, and when are they used?
- How does MySQL use an index for MIN/MAX?
- How does MySQL optimize COUNT(*)?
- Why is COUNT(*) in InnoDB not as cheap as many people think?
- How do you speed up COUNT on large tables?
- How do you design indexes for queries of the form `WHERE ... ORDER BY ... LIMIT ...`?
- How do you design indexes for multi-tenant tables?
- Why is `(tenant_id, created_at)` often more useful than separate indexes?
- How do you design an index for soft delete?
- Do you need an index on `deleted_at` or `is_deleted`?
- How do you deal with bad plans after data growth?
- Why can a query that was fast in test become slow in production?
- How would you look for the cause of degradation after statistics changed?
- How would you explain to a developer why “there is an index, so the query is fast” is a false statement?
- What questions would you ask before creating a new index?
- What indexing anti-patterns in MySQL have you seen most often?
- How would you design an indexing strategy for an OLTP system with a large number of writes and a moderate number of analytical queries?

#### 7.3.3 Differences Between MySQL and PostgreSQL
- What are the key differences between MySQL and PostgreSQL at a high level?
- What philosophy is usually associated with MySQL, and what philosophy with PostgreSQL?
- When is MySQL more commonly chosen?
- When is PostgreSQL more commonly chosen?
- Which of these DBMSs is usually better suited for classic OLTP?
- Which one is usually more flexible in terms of SQL capabilities?
- How do MySQL and PostgreSQL differ in how strictly they follow the SQL standard?
- Which one usually provides richer capabilities for complex queries?
- What differences are there in the transaction model of MySQL and PostgreSQL?
- How does MVCC differ in InnoDB and MVCC in PostgreSQL?
- Why can REPEATABLE READ behave differently in MySQL and PostgreSQL?
- Which isolation levels do both DBMSs support, and how does their implementation differ?
- How do MySQL and PostgreSQL differ in handling phantom reads?
- How are locking mechanisms implemented differently in MySQL and PostgreSQL?
- Why are gap locks characteristic of InnoDB, but not PostgreSQL in the same form?
- What are next-key locks and is there a direct analogue in PostgreSQL?
- How does the behavior of SELECT ... FOR UPDATE differ in MySQL and PostgreSQL?
- What differences are there in deadlock detection between MySQL and PostgreSQL?
- How does autocommit behavior differ by default?
- Why does DDL behavior in transactions differ so much between MySQL and PostgreSQL?
- Which DDL operations can be rolled back in PostgreSQL but cannot or are difficult to roll back in MySQL?
- Why is transactional DDL in PostgreSQL considered a strong point?
- How does index implementation differ in MySQL and PostgreSQL?
- What is a clustered index in InnoDB and why does PostgreSQL not have it in the same form?
- How does the storage of secondary indexes differ in InnoDB and PostgreSQL?
- Why does the primary key in InnoDB affect all secondary indexes, while in PostgreSQL it is different?
- Which DBMS usually provides more index types out of the box?
- Which index types does PostgreSQL have that MySQL does not have or barely has?
- How do partial indexes in PostgreSQL differ from what MySQL offers?
- How do expression indexes in PostgreSQL differ from functional indexes in MySQL?
- Why is PostgreSQL often more convenient for complex indexing for non-standard queries?
- How do the optimizer and planner differ in MySQL and PostgreSQL?
- Which DBMS usually provides a more detailed and convenient EXPLAIN?
- How does EXPLAIN ANALYZE in PostgreSQL differ in maturity and informativeness from MySQL?
- Why is plan tuning and plan reading often considered easier/harder in each DBMS?
- How do optimizer statistics differ in MySQL and PostgreSQL?
- What is autovacuum in PostgreSQL and is there a direct analogue in MySQL?
- Why are vacuum and bloat important topics in PostgreSQL, while the emphasis is often different in MySQL?
- What in InnoDB can be considered an “analogue” of vacuum problems, and what cannot?
- What differences are there in working with tables that have many UPDATEs and DELETEs?
- Which DBMS is usually more resilient to write-heavy workloads, and in which scenarios?
- How does working with JSON differ in MySQL and PostgreSQL?
- Why is PostgreSQL JSONB often considered a more powerful solution?
- When is JSON in MySQL sufficient and not problematic?
- What differences are there in full-text search in MySQL and PostgreSQL?
- Why do many teams still move search to Elasticsearch/OpenSearch regardless of the choice of primary DBMS?
- What differences are there in support for arrays, ENUMs, custom types, and domain types?
- Why is PostgreSQL considered a more “feature-rich” DBMS in terms of data types?
- What differences are there in window functions and CTEs in MySQL and PostgreSQL?
- Historically, which DBMS provided more mature support for window functions earlier?
- What differences are there in recursive queries?
- Which DBMS is better suited for complex analytics without moving data to a separate warehouse?
- What differences are there in replication between MySQL and PostgreSQL?
- How does binlog-based replication in MySQL differ from WAL-based replication in PostgreSQL?
- How do approaches to logical replication differ?
- In which DBMS is it usually easier to set up read replicas, and in which is logical replication more flexible?
- What differences are there in failover and the HA ecosystem of MySQL and PostgreSQL?
- How does the ecosystem of tools around MySQL and PostgreSQL differ?
- What differences are there in managed services for MySQL and PostgreSQL among cloud providers?
- How does sharding support differ in both DBMSs?
- Is there “native” sharding in MySQL and PostgreSQL?
- Why are external solutions often used for sharding with both MySQL and PostgreSQL?
- What differences are there in partitioning?
- How do approaches to foreign keys and referential integrity differ?
- Is it true that MySQL was historically more often used in projects where strict schema constraints were weaker?
- How do NULL handling and sorting differ in MySQL and PostgreSQL?
- What differences are there in collations and string handling?
- How does default case sensitivity differ in MySQL and PostgreSQL?
- How does working with timestamptz, time zones, and dates differ?
- Why can migration from MySQL to PostgreSQL break on types, strings, and auto-generated IDs?
- How does AUTO_INCREMENT in MySQL differ from sequences / identity in PostgreSQL?
- What needs to be considered when migrating tables with AUTO_INCREMENT to PostgreSQL?
- What differences are there in UPSERT behavior?
- How does `INSERT ... ON DUPLICATE KEY UPDATE` in MySQL differ from `INSERT ... ON CONFLICT` in PostgreSQL?
- What differences are there in locking behavior during UPSERT?
- Which is more convenient for geospatial data: MySQL or PostgreSQL?
- Why is PostGIS considered an important argument in favor of PostgreSQL?
- What differences are there in extensibility?
- Why are PostgreSQL extensions considered a strong point?
- How do stored procedures, functions, and triggers differ?
- Which DBMS more often seems “easier to start with” for developers?
- Which DBMS more often gives an experienced team more control?
- Why were many older web projects traditionally built on MySQL?
- Why do many modern backend teams often prefer PostgreSQL?
- When would choosing MySQL be completely rational and correct?
- When would choosing PostgreSQL be completely rational and correct?
- In which cases is the practical difference between them almost insignificant for an application?
- What common mistakes do teams make when choosing MySQL or PostgreSQL “because it is trendy”?
- If you have a typical CRUD service with moderate load, what would you base the choice between MySQL and PostgreSQL on?
- If you have many complex SQL queries, analytics, non-standard types, and extensibility requirements, what would you choose and why?
- If you already have deep expertise in MySQL, should you migrate to PostgreSQL just for “trendiness”?
- What are the risks of migrating from MySQL to PostgreSQL?
- What are the risks of migrating from PostgreSQL to MySQL?
- What most often breaks during migration from MySQL to PostgreSQL?
- How would you plan a migration from MySQL to PostgreSQL for a production service?
- How would you explain to the business that one DBMS is “theoretically better,” but migration may not be worth it?
- In what case would you explicitly defend MySQL before a team that wants to switch to PostgreSQL?
- In what case would you explicitly insist on PostgreSQL instead of MySQL?
- What questions would you ask before choosing between MySQL and PostgreSQL for a new project?

### 7.4 SQL: Basic and Intermediate Level
- What is SQL and what is it used for?
- How is SQL different from general-purpose programming languages?
- What major SQL sublanguages do you know?
- What are DDL, DML, DCL, and TCL?
- Which commands belong to DDL?
- Which commands belong to DML?
- Which commands belong to TCL?
- What is an SQL dialect?
- How can SQL dialects differ across DBMSs?
- What is a relational table?
- What is a row and a column in a table?
- What is a database schema?
- What is a primary key?
- Can a primary key be composite?
- Can a primary key contain NULL?
- What is a foreign key?
- What is a foreign key used for?
- What is a UNIQUE constraint?
- What is a NOT NULL constraint?
- What is a CHECK constraint?
- What is DEFAULT?
- What is the difference between PRIMARY KEY and UNIQUE?
- What are surrogate keys and natural keys?
- When is it better to use a surrogate key, and when a natural key?
- What is an auto-increment key?
- What are the downsides of auto-increment keys?
- What is normalization?
- Why is normalization needed?
- Which normal forms do you know at a basic level?
- When does normalization help, and when does it get in the way?
- What is denormalization?
- When can denormalization be justified?
- What does the SELECT command do?
- What is SELECT * and why is it often not recommended?
- How do you select specific columns from a table?
- What does DISTINCT do?
- When is DISTINCT useful, and when does it hide problems in data or queries?
- What does WHERE do?
- What is the difference between WHERE and HAVING?
- Which comparison operators are used in WHERE?
- How do AND, OR, and NOT work?
- What is NULL in SQL?
- Why is NULL not the same as an empty string or 0?
- Why does comparing with NULL via = not work?
- How do you correctly check for NULL?
- What do IS NULL and IS NOT NULL do?
- What is three-valued logic in SQL?
- How does NULL affect conditions in WHERE?
- What does the IN operator do?
- When is IN more convenient than multiple ORs?
- What does BETWEEN do?
- Does BETWEEN include the boundaries or not?
- What does LIKE do?
- What is the difference between % and _ in LIKE patterns?
- When can LIKE use an index, and when can it not?
- What does ORDER BY do?
- How do you sort by multiple columns?
- What is the default sort order — ASC or DESC?
- How do you sort data in descending order?
- What does LIMIT do?
- What does OFFSET do?
- Why can OFFSET on large datasets be expensive?
- How do you select the first N rows from a table?
- How do you select rows with an offset?
- What is pagination using LIMIT/OFFSET?
- What problems does OFFSET-based pagination have?
- What is keyset pagination?
- When is keyset pagination better than OFFSET?
- What does INSERT do?
- How do you insert one row?
- How do you insert multiple rows in one query?
- Can you insert values into only some columns?
- What happens if you omit a value for a NOT NULL field without a DEFAULT?
- What does UPDATE do?
- Why is UPDATE without WHERE dangerous?
- What does DELETE do?
- Why is DELETE without WHERE dangerous?
- What is the difference between DELETE and TRUNCATE?
- How is TRUNCATE different from DROP?
- What does CREATE TABLE do?
- What does ALTER TABLE do?
- What does DROP TABLE do?
- What is an alias in SQL?
- Why use aliases for tables and columns?
- What are aggregate functions?
- Which aggregate functions do you know?
- How is COUNT(*) different from COUNT(column)?
- How does COUNT behave with NULL?
- What do SUM, AVG, MIN, and MAX do?
- What is GROUP BY?
- Why is GROUP BY needed?
- Why can’t you select non-aggregated columns that are not included in GROUP BY?
- What does HAVING do?
- When should you use HAVING, and when WHERE?
- What is a JOIN?
- Why are JOINs needed?
- Which main types of JOINs do you know?
- What is the difference between INNER JOIN and LEFT JOIN?
- What does LEFT JOIN return if no match is found?
- What is RIGHT JOIN?
- Why is RIGHT JOIN often avoided?
- What is FULL OUTER JOIN?
- Is FULL OUTER JOIN supported in all DBMSs?
- What is CROSS JOIN?
- What is SELF JOIN?
- When might SELF JOIN be needed?
- What condition should be used to join tables in a JOIN?
- How is a JOIN on primary/foreign keys different from a JOIN on arbitrary fields?
- What happens if you write a JOIN without a correct ON condition?
- What is a Cartesian product?
- Why can an incorrect JOIN explode the number of rows?
- What are duplicate rows after a JOIN and why do they appear?
- When does a JOIN produce duplicates, and when does it not?
- What are one-to-one, one-to-many, and many-to-many relationships?
- How is a many-to-many relationship modeled in a relational database?
- What is a junction table / associative table?
- What is a subquery?
- Which types of subqueries do you know?
- What is a scalar subquery?
- What is a correlated subquery?
- How is a correlated subquery different from a regular subquery?
- When is it better to use a JOIN, and when a subquery?
- What does EXISTS do?
- How is EXISTS different from IN?
- When is EXISTS more efficient than IN?
- What is CASE in SQL?
- How do you use CASE for conditional logic in a query?
- What is the difference between UNION and UNION ALL?
- Why can UNION be more expensive than UNION ALL?
- What requirements apply to queries used in a UNION?
- What is a view?
- How is a view different from a table?
- When is it useful to use a view?
- What risks are there in overusing views?
- What is a materialized view?
- How is a materialized view different from a regular view?
- What is an index at a basic level?
- Why are indexes needed?
- Why does an index speed up reads but can slow down writes?
- How do you determine whether a column needs an index?
- Which fields are usually indexed?
- Why does an index not always help?
- What is a composite index?
- Why is the order of columns in a composite index important?
- How does the SQL engine use an index in a WHERE condition?
- Can an index be used for sorting?
- Can an index be used for GROUP BY?
- When is an index useless?
- What is a full table scan?
- Why is a full table scan not always bad?
- How do you tell whether an index was used?
- What is EXPLAIN at a basic level?
- Why look at a query execution plan?
- What does it mean that a query is “poorly written”?
- What are the basic signs of an inefficient SQL query?
- What is the N+1 problem at the SQL and application level?
- Why is SELECT inside a loop a bad idea?
- What is a prepared statement?
- How is a prepared statement different from a regular SQL query?
- What is a parameterized query?
- How do parameterized queries protect against SQL injection?
- What is SQL injection?
- Why is string concatenation for queries dangerous?
- How do you protect against SQL injection?
- What is a transaction at a basic level?
- Why are transactions needed?
- What is COMMIT?
- What is ROLLBACK?
- Which typical operations should be combined into a transaction?
- What happens if half the operations in a business scenario succeed and half do not?
- What does ACID mean in practical terms?
- What is atomicity?
- What is consistency?
- What is isolation?
- What is durability?
- Why is a transaction not just “several queries in a row”?
- What is a lock at a basic level?
- Why are locks needed?
- How do reads differ from writes in terms of concurrency?
- What can happen if two users update the same row at the same time?
- What is a lost update?
- How do SQL and DBMSs help avoid lost update?
- What is a connection pool?
- Why does an application need a connection pool?
- Why shouldn’t you open a new database connection for every request?
- What is an ORM at a basic level?
- Why is an ORM needed?
- What advantages of ORM do you know?
- What disadvantages of ORM do you know?
- When do SQL queries generated by an ORM become a problem?
- Which is better: writing SQL by hand or using an ORM?
- In which cases is it better to write “plain SQL”?
- What is a schema migration?
- Why are migrations needed?
- Why can’t you just change tables manually in production?
- What is an idempotent migration?
- How do you safely add a new column to a table?
- How do you safely drop a column or table?
- How would you design tables for users and orders?
- How would you design the relationship between an order and products?
- How would you store order statuses in SQL?
- How would you implement soft delete?
- What is soft delete?
- What are the pros and cons of soft delete?
- How do you select all active records if soft delete is used?
- How do you find duplicates in a table?
- How do you remove duplicates from a table?
- How do you select the second-highest salary?
- How do you find users without orders?
- How do you find orders without a corresponding user?
- How do you count the number of orders per user?
- How do you calculate the total order amount per user?
- How do you select the top 5 users by revenue?
- How do you select records for a specific date range?
- Why is it important to define boundaries carefully when working with dates?
- How is DATE different from DATETIME / TIMESTAMP?
- What pitfalls are there when filtering by date and time?
- How do you correctly write a time-range condition so that you do not lose index usage?
- What are string, numeric, and temporal sorting?
- Why can sorting strings and numbers produce unexpected results?
- What is collation?
- How does collation affect string comparison and sorting?
- What is a charset and why is it important?
- Why can encoding in the database become a problem?
- What is a transaction-safe query?
- What is an idempotent operation at the SQL level?
- How would you check an SQL query for correctness and performance before production?
- What typical mistakes do beginner developers make in SQL?
- Which SQL questions would you ask a junior candidate yourself?
- Which SQL questions would you ask a middle candidate yourself?
- What common anti-patterns have you seen in SQL code?
- How would you explain SQL to a non-technical person?
- How would you explain the difference between JOIN, GROUP BY, and a subquery to a beginner?
- Why is a good SQL developer not just someone who knows the syntax?
- What practical signs show that a candidate has really worked with SQL and not just read theory?

### 7.5 SQL: Advanced Level
- What is a CTE?
- What is the difference between a regular subquery and a CTE?
- When does a CTE improve query readability?
- When can a CTE hurt performance?
- What is a recursive CTE?
- What kinds of tasks use recursive CTEs?
- How do you traverse hierarchies using a recursive CTE?
- How do you build a category tree or org chart using a recursive CTE?
- What are window functions?
- How are window functions different from aggregate functions?
- What does OVER() mean in SQL?
- What is PARTITION BY in a window function?
- What does ORDER BY do inside a window function?
- How is ORDER BY inside OVER() different from the regular ORDER BY of a query?
- What does ROW_NUMBER() do?
- What does RANK() do?
- What does DENSE_RANK() do?
- What is the difference between ROW_NUMBER(), RANK(), and DENSE_RANK()?
- What does NTILE() do?
- What do LAG() and LEAD() do?
- What are FIRST_VALUE() and LAST_VALUE() useful for?
- What is a window frame?
- What do ROWS BETWEEN and RANGE BETWEEN mean?
- What is the difference between ROWS and RANGE?
- Why does LAST_VALUE() often behave differently than beginners expect?
- How do you find the previous and next row within a group?
- How do you calculate a running total over time?
- How do you calculate a moving average?
- How do you select the top-N rows within each group?
- How do you select the latest row per user?
- How do you select each user’s first purchase?
- How do you find “gaps” in sequences of values?
- How do you find sessions or intervals between events using window functions?
- How do you solve deduplication with window functions?
- What are pivot and unpivot?
- How do you implement pivot in SQL if the DBMS does not support a dedicated operator?
- What are GROUPING SETS?
- What is ROLLUP?
- What is CUBE?
- When are GROUPING SETS more useful than several UNION ALLs?
- What is GROUPING() and why is it needed?
- What is a lateral join?
- What are CROSS APPLY / OUTER APPLY and which DBMSs support them?
- How is a lateral join different from a regular JOIN?
- In which cases does a lateral join make a query more expressive?
- What is a correlated subquery and when does it become expensive?
- How do you rewrite a correlated subquery as a JOIN or a window function?
- What are semi join and anti join?
- How do you implement an anti join in SQL?
- How is NOT EXISTS different from NOT IN?
- Why can NOT IN with NULL behave unexpectedly?
- When is EXISTS preferable to JOIN?
- When is JOIN preferable to EXISTS?
- What is set-based thinking in SQL?
- Why is it important to think in sets rather than loops in SQL?
- What is relational algebra and how does it manifest in SQL?
- What is predicate pushdown?
- Why is it important to filter data as early as possible?
- What is a sargable query?
- What makes a condition sargable?
- Why do functions on columns in WHERE often kill index usage?
- Why can CAST in WHERE break index usage?
- Why does OR sometimes make the plan worse?
- When can OR be replaced with UNION ALL?
- What is a covering index?
- What is an index-only scan?
- Under what conditions can a DBMS perform an index-only scan?
- What is a composite index and how does the leftmost prefix rule work?
- How does the order of columns in a composite index affect queries?
- Which index is better to create for WHERE a = ? AND b = ? ORDER BY c?
- Why does an index on (a, b) not always help for conditions only on b?
- What is index selectivity?
- Why can an index on a low-cardinality column be useless?
- What is cardinality in the context of the optimizer?
- Why are table statistics important for the optimizer?
- What happens if statistics are stale?
- What is a cost-based optimizer?
- What is rule-based optimization and why do modern DBMSs more often use a cost-based approach?
- How does the optimizer choose join order?
- Is the order of tables in an SQL query always important for the actual plan?
- What is a nested loop join?
- What is a hash join?
- What is a merge join?
- In which cases is each join type most efficient?
- Why can the absence of an index lead to a poor join type?
- What is a full table scan and when can it be the best plan?
- What is a bitmap scan?
- What is filesort?
- What does temporary mean in a query plan?
- Why can sort and hash aggregation suddenly become a bottleneck?
- How do you read EXPLAIN at an advanced level?
- What is the difference between an estimated plan and an actual plan?
- Why can estimated rows and actual rows differ drastically?
- What is cardinality misestimation?
- How does an incorrect cardinality estimate break the whole query plan?
- What common causes of bad execution plans do you know?
- What is parameter sniffing?
- In which DBMSs is parameter sniffing a noticeable problem?
- How does parameter sniffing affect performance stability?
- What are bind variables / prepared statements from the optimizer’s perspective?
- When do prepared statements help, and when do they hurt the plan?
- What is a query plan cache?
- Why can the same query perform differently for different parameter values?
- How do you diagnose sudden query performance degradation?
- What is subquery materialization?
- When can the optimizer inline a CTE, and when does it materialize it?
- Why are the same SQL constructs optimized differently in PostgreSQL, MySQL, and Oracle?
- What is partition pruning?
- How does partitioning affect execution plans?
- When does partitioning help, and when does it only complicate the system?
- What is a partition-wise join?
- What is shard-aware query planning?
- How do you optimize queries on huge time-series tables?
- What indexing patterns are useful for time-series data?
- What is a partial index?
- What is a functional index / expression index?
- When is an expression index useful?
- What is a filtered index?
- What are BRIN, GIN, GiST, and bitmap indexes at a conceptual level?
- When does a B-tree index stop being the best choice?
- How do you optimize search on JSON fields?
- How do you optimize text search?
- What is full-text search and how is it different from LIKE '%...%'?
- What is an inverted index?
- How does full-text search work at a high level?
- How does storing JSON in a relational database affect SQL and indexing?
- What are generated columns and why are they needed?
- When do generated columns help index computed values?
- What is upsert?
- Which upsert variants exist in different SQL dialects?
- How is UPSERT different from MERGE?
- What pitfalls does MERGE have?
- What is a race condition during insert or update and how does SQL help solve it?
- How do you implement an idempotent write at the SQL level?
- What is optimistic concurrency control in SQL?
- How do you implement a version column for optimistic locking?
- What is write skew?
- What is a phantom read?
- What is a lost update?
- Why does REPEATABLE READ not always mean full protection from all anomalies?
- How does SERIALIZABLE in real DBMSs differ from the “ideal serialization” from textbooks?
- What is predicate locking?
- What is gap locking?
- Why can serialization sharply reduce throughput?
- How do you design transactions for high concurrency?
- Why are short transactions almost always better than long ones?
- How does batching affect SQL performance?
- When is bulk insert better than single inserts?
- What risks do large batch operations have?
- What is write amplification at the database level?
- How do mass UPDATE/DELETE operations affect performance and vacuum/compaction?
- What is soft delete and why does it complicate SQL?
- How do you design queries in a system with soft delete?
- What are temporal tables / system-versioned tables?
- When should data historization be done at the application level, and when at the database level?
- How do you solve slowly changing dimensions in SQL and DWH?
- What are SCD Type 1, Type 2, and Type 3?
- How do you implement event deduplication in SQL?
- How do you find anomalies in data using SQL?
- How do you find gaps, interval overlaps, and scheduling conflicts?
- How do you find the “latest known state” of an object from an event log?
- How do you implement event-sourcing-like queries in SQL?
- How do you build incremental marts in SQL?
- What are materialized aggregates?
- How do you maintain aggregates without a full recomputation?
- What is a star schema?
- What is a snowflake schema?
- How are OLTP queries different from OLAP queries?
- Why does the same SQL approach work poorly for both OLTP and analytics at the same time?
- What is columnar storage at a conceptual level?
- Why does a columnar DB work well for analytics?
- Why is a row store usually better for transactional workloads?
- What is late materialization?
- What is vectorized execution?
- How do modern analytical SQL engines work at a high level?
- What are pushdown filters in storage/query engines?
- How does SQL work over distributed systems?
- What difficulties arise with JOINs across shards?
- Why is distributed SQL more complex than regular SQL?
- What is two-phase commit at a conceptual level?
- What alternatives to distributed transactions are often used in practice?
- What is data skew and how does it affect SQL analytics?
- How does skew break distributed aggregations and joins?
- What is approximate query processing?
- When are approximate distinct count, percentiles, and sampling acceptable?
- What is HyperLogLog at a conceptual level?
- What is a bloom filter and where is it used in query systems?
- How would you optimize a query that suddenly became 100 times slower?
- How would you investigate a performance regression after a deployment?
- How would you explain why an SQL query cannot be evaluated only by its text without an execution plan?
- Which SQL questions would you ask a middle developer?
- Which SQL questions would you ask a senior developer?
- Which SQL questions would you ask a data engineer or analytics engineer?
- Which SQL anti-patterns are most common in production?
- What complex SQL tasks have you solved in practice and why were they difficult?
- When should you abandon “smart SQL” in favor of intermediate tables, ETL, or application logic?
- When, on the contrary, is it better to move logic from the application into SQL?
- Where is the boundary between “using SQL capabilities well” and “creating an unmaintainable monster query”?
- How do you know a query has become too complex and should be decomposed?
- What trade-offs would you discuss with an architect when choosing between simplicity, performance, and maintainability of an SQL solution?

### 7.6 Transactions, Isolation, and Concurrent Access
- What is a transaction in a database?
- Why are transactions needed at all?
- Which ACID properties do you know?
- What does atomicity mean?
- What does consistency mean?
- What does isolation mean?
- What does durability mean?
- How is ACID different from BASE?
- What is autocommit?
- When is autocommit convenient, and when is it harmful?
- What happens if an error occurs inside a transaction?
- What is COMMIT?
- What is ROLLBACK?
- What is a SAVEPOINT and why is it needed?
- Can you roll back part of a transaction?
- What is a read-only transaction?
- What is a long-running transaction and why is it dangerous?
- What problems can long-lived transactions cause?
- What is transaction isolation?
- Which isolation levels do you know?
- How does READ UNCOMMITTED differ from READ COMMITTED?
- How does READ COMMITTED differ from REPEATABLE READ?
- How does REPEATABLE READ differ from SERIALIZABLE?
- Which isolation level is usually the default in different DBMSs?
- What is a dirty read?
- What is a non-repeatable read?
- What is a phantom read?
- What is a lost update?
- What is write skew?
- What is read skew?
- At which isolation levels are dirty read, non-repeatable read, and phantom read possible?
- Why isn’t SERIALIZABLE used everywhere by default?
- What is snapshot isolation?
- How is snapshot isolation different from serializable isolation?
- Does REPEATABLE READ always protect against phantom reads?
- Why can the behavior of isolation levels differ across DBMSs?
- What is concurrent access to data?
- What are the main problems that arise under concurrent access?
- What is a race condition in the context of a database?
- What is resource contention?
- What is a lock in a database?
- Which types of locks do you know?
- What is a shared lock?
- What is an exclusive lock?
- What is the difference between a shared lock and an exclusive lock?
- What is a row-level lock?
- What is a page-level lock?
- What is a table-level lock?
- What are the pros and cons of row-level and table-level locks?
- What is an intention lock?
- What is a metadata lock?
- What is a predicate lock?
- What is optimistic locking?
- What is pessimistic locking?
- When is it better to use optimistic locking?
- When is it better to use pessimistic locking?
- How do you implement optimistic locking in practice?
- How do you implement pessimistic locking in practice?
- What is a version column and why is it needed?
- How does optimistic locking help avoid lost update?
- What is the downside of optimistic locking?
- What is the downside of pessimistic locking?
- What is MVCC?
- How does MVCC help reduce the number of locks?
- How is MVCC different from a locking-based concurrency model?
- Why is MVCC useful for systems with many reads?
- Can MVCC completely replace locks?
- What data does MVCC need to work?
- What is a snapshot in MVCC?
- Why can MVCC lead to the accumulation of garbage/old row versions?
- How does a long-running transaction affect MVCC?
- What is a deadlock?
- How does a deadlock occur?
- How is a deadlock different from ordinary lock waiting?
- How does a database detect a deadlock?
- What happens after a deadlock is detected?
- How can you reduce the probability of deadlocks?
- Why does updating entities in a consistent order help reduce the risk of deadlocks?
- What is lock wait timeout?
- How is lock wait timeout different from a deadlock?
- How do you diagnose locks in production?
- Which queries or system views let you see locks?
- How do you diagnose which transaction is blocking which?
- Why does the lack of a suitable index increase the probability of locks?
- How can an UPDATE without an index affect concurrency?
- How does a DELETE over a large range affect the system?
- What is FOR UPDATE?
- What is FOR SHARE / LOCK IN SHARE MODE?
- When should you use SELECT ... FOR UPDATE?
- What happens if two processes simultaneously run SELECT ... FOR UPDATE on the same rows?
- What are NOWAIT and SKIP LOCKED?
- When is SKIP LOCKED useful?
- How do you use a database as a job queue with SKIP LOCKED?
- What are the risks of using a database as a queue?
- What is serializability?
- What is conflict serializability?
- What is view serializability?
- Why is a serializable schedule considered “correct”?
- What is a recoverable schedule?
- What is cascading rollback?
- What is a strict schedule?
- What is two-phase locking (2PL)?
- What is strict 2PL?
- What are the pros and cons of 2PL?
- How is 2PL related to serializability?
- Why can 2PL hurt performance?
- What is timestamp ordering?
- What concurrency control approaches exist besides locking?
- What is OCC (optimistic concurrency control)?
- What is the validation phase in OCC?
- How is OCC different from MVCC?
- What is a write-write conflict?
- What is a read-write conflict?
- What is write-write contention?
- What is a latch and how is it different from a lock?
- What is the difference between logical and physical locking?
- How does the isolation level affect performance?
- How does the isolation level affect the readability and correctness of business logic?
- Why can using a “too weak” isolation level break business invariants?
- Why can using a “too strong” isolation level kill throughput?
- When should you raise the isolation level, and when is it better to rewrite the business logic?
- How do you ensure a correct transfer of money between accounts?
- How do you implement booking a limited resource without double spending?
- How do you avoid overselling in e-commerce?
- How do you deal with lost update in an application?
- Can you rely only on transactions to guarantee all business invariants?
- When do you need unique constraints rather than just checks in the application?
- Why can checking “does the record exist” before INSERT be incorrect without constraints?
- How do unique indexes help under concurrent access?
- How do you implement idempotency through the database?
- How do you ensure the same external event is not processed twice?
- How do transactions work together with external services?
- Why does a database transaction not solve consistency with an external API?
- What is a distributed transaction?
- Why are distributed transactions difficult?
- What is two-phase commit?
- What problems does two-phase commit have?
- What is a saga?
- How is a saga different from a normal transaction?
- When should you use compensation instead of rollback?
- How do you ensure consistency across services without a distributed transaction?
- What is transactional outbox?
- How does transactional outbox help solve the dual write problem?
- How do you achieve exactly-once semantics in real systems?
- Why is exactly-once usually achieved through a combination of idempotency and careful design?
- When should part of concurrency logic be moved from the database to the application?
- When should you, on the contrary, rely on DBMS guarantees?
- What common mistakes do developers make when working with transactions?
- Why is “one big transaction scope” often a bad idea?
- Why is it dangerous to keep a transaction open during a network call?
- Why can reading from one replica and writing to the master break application expectations?
- How does eventual consistency affect concurrent access?
- How do you test understanding of isolation using a real-world case?
- Describe a situation where READ COMMITTED was insufficient.
- Describe a situation where SERIALIZABLE was excessive.
- How would you choose an isolation level for a payment system?
- How would you choose an isolation level for a news feed or analytics?
- How would you design tables and queries to reduce contention?
- Which metrics and signals indicate that a system suffers from locks and contention?
- Which approaches do you use to test concurrency and transactional correctness?
- How do you model concurrency bugs in tests?
- Which production incidents related to transactions and locks can you describe?

### 7.7 Indexes and Query Optimization
- What is an index in a database?
- Why are indexes needed?
- How does an index help speed up queries?
- What downsides do indexes have?
- Why can’t you index everything blindly?
- Which operations benefit from indexes?
- Which operations suffer from having too many indexes?
- What is a B-tree index?
- Why is B-tree the standard index in many DBMSs?
- What other types of indexes do you know?
- What is a hash index?
- When is a hash index useful, and when is it not?
- What is a bitmap index?
- Where is a bitmap index useful?
- What are GiST, GIN, BRIN, R-tree, and inverted indexes?
- What tasks are full-text indexes suitable for?
- What tasks are geospatial indexes suitable for?
- What is a clustered index?
- What is a non-clustered index?
- How is a clustered index different from a secondary index?
- Why is a table in some DBMSs physically organized around the primary key?
- What is a covering index?
- What is a composite index?
- How does a composite index work?
- What does the leftmost prefix rule mean?
- When will a composite index `(a, b)` help for conditions on `a`, `a,b`, `a,b,c`?
- When will a composite index `(a, b)` not help for searching only by `b`?
- Why is the order of columns in a composite index critical?
- How do you choose the order of columns in a composite index?
- What is a selective column?
- Why is selectivity important for indexes?
- What is cardinality?
- How does low cardinality affect the usefulness of an index?
- Is it worth creating an index on a boolean field?
- When can an index on a low-cardinality column still be useful?
- What is a unique index?
- How is a unique index different from a regular index?
- How is a primary key different from a unique index?
- What is a partial index / filtered index?
- When is a partial index useful?
- What is a functional index / expression index?
- When does it make sense to index an expression rather than a column?
- What is a prefix index?
- When does a prefix index help, and when can it give false efficiency?
- What is an invisible index?
- Why make an index invisible?
- How do you test whether an index is needed without dropping it immediately?
- What is an index-only scan?
- Under what conditions can a database perform an index-only scan?
- What is a range scan?
- What is a full index scan?
- How is an index scan different from a sequential scan / full table scan?
- Why does the optimizer sometimes choose a full table scan even when an index exists?
- Why might an index not be used for a small table?
- Why might an index not be used if the query returns too many rows?
- How does a function on a column affect index usage?
- Why can `WHERE LOWER(name) = ...` prevent a regular index from being used?
- How can type casting break index usage?
- Why is `%abc` poorly indexed in LIKE?
- When does LIKE use an index, and when does it not?
- Why do `LIKE 'abc%'` and `LIKE '%abc%'` behave differently?
- How do you use full-text search instead of LIKE?
- What is a sargable condition?
- Which conditions are considered non-sargable?
- How do you rewrite a non-sargable query into a sargable one?
- What is a query execution plan?
- What is a cost-based optimizer?
- How is a cost-based optimizer different from a rule-based optimizer?
- What statistics does the optimizer need?
- Why do stale statistics lead to bad plans?
- What is a histogram and why does the optimizer need it?
- What is a selectivity estimate?
- What is join order optimization?
- Why does join order affect the plan?
- Which join algorithms do you know?
- What is a nested loop join?
- What is a hash join?
- What is a merge join?
- In which cases is nested loop join good?
- In which cases is hash join good?
- In which cases is merge join good?
- Why can the absence of an index on a join key make a join very expensive?
- What is a lookup / key lookup / bookmark lookup?
- Why can a key lookup become a bottleneck?
- What is a sort spill?
- Why can ORDER BY be expensive?
- How does an index help ORDER BY?
- When can the database avoid sorting because of an index?
- How does LIMIT affect the plan?
- Why does OFFSET 1000000 often perform badly?
- How can you paginate more efficiently than OFFSET/LIMIT?
- What is keyset pagination?
- How do indexes help keyset pagination?
- Why can COUNT(*) be expensive?
- When is COUNT(*) fast, and when is it slow?
- How do indexes help GROUP BY?
- How do indexes help DISTINCT?
- Why can SELECT * be a bad practice?
- How does selecting unnecessary columns affect performance?
- Why does row width matter for performance?
- How do large TEXT/BLOB columns affect queries and indexes?
- What is a hot spot in an index?
- Why can a monotonically increasing key both help and hurt?
- How does a random key affect inserts into a B-tree?
- What is a page split?
- Why can a page split hurt performance?
- What is index fragmentation?
- When should an index be rebuilt?
- Is fragmentation always actually important?
- How do indexes affect INSERT?
- How do indexes affect UPDATE?
- How do indexes affect DELETE?
- Why is updating an indexed column especially expensive?
- What is write amplification in the context of indexes?
- Why are too many indexes harmful for write-heavy tables?
- How do you determine that an index is unused?
- How do you find unused indexes?
- How do you find missing indexes?
- Why shouldn’t you blindly create an index for every slow query?
- How do you evaluate the real benefit of an index?
- How can you tell that one index duplicates another index?
- What is a redundant index?
- What problems do duplicate indexes create?
- How should you approach index design for OLTP?
- How should you approach index design for OLAP?
- How does indexing in OLTP differ from indexing in analytical systems?
- What is partition pruning?
- How is partitioning related to indexes?
- Do you need indexes inside partitions?
- How does sharding affect indexing strategy?
- What is a materialized view, and when is it useful for optimization?
- When is caching better than an index, and when is it worse?
- When is denormalization better than complex query optimization?
- What are query anti-patterns related to indexes?
- Why does OR sometimes get in the optimizer’s way?
- When is UNION ALL better than OR?
- Why can IN and EXISTS behave differently?
- When does a correlated subquery become a problem?
- When can a CTE improve readability but hurt performance?
- How do window functions affect performance?
- How can you optimize a slow query without changing business logic?
- How can you optimize a query if you are not allowed to add an index?
- How would you investigate the cause of performance degradation for a specific query?
- What steps would you take when analyzing a slow query in production?
- What tools and views do you use to find the most expensive queries?
- What is a slow query log, and how do you use it?
- What is query fingerprinting?
- How does a prepared statement work, and does it affect performance?
- How does parameterization affect the query plan?
- What is parameter sniffing?
- In which DBMSs is parameter sniffing especially noticeable?
- How can attempts to mitigate parameter sniffing affect performance?
- What are optimizer hints, and when should they be used?
- Why are hints often the last tool rather than the first?
- What is plan regression?
- Why might a query become slower after updating statistics or upgrading the DBMS version?
- How would you design indexes for a users table?
- How would you design indexes for an orders table filtered by status and date?
- How would you design indexes for a high-write events table?
- How would you design indexes for product catalog search?
- What indexing mistakes have you most often seen in real projects?
- What matters more for optimization: indexes, query rewriting, changing the data model, or caching?
- How would you explain to a non-technical stakeholder that “let’s add an index” does not always solve the problem?

### 7.8 Replication, partitioning, and sharding
- What is replication in a database?
- Why is replication needed?
- What problems are usually solved with replication?
- What is the difference between high availability and read scaling?
- What is primary-replica replication?
- What is master-slave, and why do people now more often say primary-replica?
- What types of replication do you know?
- What is synchronous replication?
- What is asynchronous replication?
- What is semi-synchronous replication?
- What are the pros and cons of synchronous replication?
- What are the pros and cons of asynchronous replication?
- What is replication lag?
- Why does replication lag occur?
- Why is replication lag dangerous for an application?
- How does replication lag affect read-after-write consistency?
- Why might a user fail to see data they just wrote when reading from a replica?
- What strategies are there to solve the read-after-write problem?
- What is failover?
- What is switchover?
- What is the difference between failover and switchover?
- What is leader election in the context of fault tolerance?
- What risks arise with automatic failover?
- What is split-brain?
- Why is split-brain dangerous?
- How is replication related to RPO and RTO?
- What are WAL/binlog/transaction logs, and how are they used in replication?
- What is statement-based replication?
- What is row-based replication?
- What is mixed replication?
- How does row-based replication differ from statement-based replication?
- Why can statement-based replication be dangerous?
- In what cases is row-based replication more reliable?
- What is logical replication?
- What is physical replication?
- How does logical replication differ from physical replication?
- When is logical replication needed?
- When is physical replication preferable?
- What is hot standby?
- What is cascading replication?
- When is cascading replication useful?
- How does replication help in geo-distributed systems?
- What are the limitations of replication in multi-region scenarios?
- Can replication be used for analytics?
- Why is it better to move heavy analytical queries off the primary?
- What replication is not?
- Why does replication not replace backups?
- Why does replication not protect against logical errors?
- If an operator drops a table on the primary, will replication save you?
- What replication metrics would you monitor?
- How can you tell that replication is lagging?
- How do you diagnose replication problems?
- What is a replication conflict?
- In what scenarios can replication break?
- What is multi-primary replication?
- What are the pros and cons of multi-primary / multi-master?
- Why is multi-master harder to operate?
- What conflicts arise in multi-master systems?
- What is conflict resolution?
- What conflict resolution strategies do you know?
- When is last-write-wins acceptable, and when is it not?
- What is table partitioning?
- How does partitioning differ from sharding?
- What problems does partitioning solve?
- When is partitioning actually useful?
- When does partitioning fail to deliver the expected benefits?
- What types of partitioning do you know?
- What is range partitioning?
- What is list partitioning?
- What is hash partitioning?
- What is composite partitioning?
- How do you choose a partitioning key?
- Why can the wrong partition key make the system worse?
- What is partition pruning?
- Why is partition pruning so important?
- How should queries be written so that partition pruning works?
- Do you need indexes inside partitions?
- How does partitioning affect unique constraints?
- How does partitioning affect foreign keys?
- How does partitioning affect UPDATE and DELETE?
- What happens if a row changes the value of its partition key?
- How do you archive data using partitions?
- Why are rolling partitions convenient for time-series data?
- How do you remove old data with detach/drop partition?
- How does partitioning help reduce vacuum/reindex/maintenance volume?
- What problems arise when there are too many partitions?
- What are local indexes and global indexes in the context of partitioning?
- What limitations do global indexes have?
- How does partitioning differ from just having several tables?
- What is vertical partitioning?
- What is horizontal partitioning?
- How does vertical partitioning differ from normalization?
- What is sharding?
- Why is sharding needed?
- When does sharding become necessary?
- What are the signs that a single database is no longer enough?
- Why is sharding not the first scaling tool?
- What alternatives to sharding should you try first?
- What is a shard key?
- How do you choose a shard key?
- What properties does a good shard key have?
- Why does a bad shard key lead to hotspots?
- What sharding strategies do you know?
- What is range-based sharding?
- What is hash-based sharding?
- What is directory-based sharding?
- What are the pros and cons of range-based sharding?
- What are the pros and cons of hash-based sharding?
- What are the pros and cons of directory-based sharding?
- What is consistent hashing?
- Why is consistent hashing useful when the number of shards changes?
- How do you rebalance shards?
- Why is shard rebalancing a complex operation?
- What are the risks of live shard rebalancing?
- How do you ensure global uniqueness of identifiers in a sharded system?
- What ID generation approaches are suitable for sharded systems?
- What do you do with JOINs between sharded tables?
- Why are cross-shard JOINs problematic?
- How do you aggregate data from multiple shards?
- What is a scatter-gather query?
- Why does scatter-gather scale poorly?
- What do you do with transactions across shards?
- Can ACID be guaranteed across shards?
- When are distributed transactions across shards justified?
- What trade-offs are usually accepted in sharded systems?
- How does sharding affect backup and restore?
- How does sharding affect observability and debugging?
- How does sharding affect schema migrations?
- How do you roll out a schema migration to a sharded cluster?
- How do you monitor uneven load across shards?
- What is a hot shard?
- How do you deal with a hot shard?
- How is tenant-based sharding used in multi-tenant systems?
- When is shard-by-tenant useful?
- What are the pros and cons of shard-by-tenant?
- How does sharding by user differ from sharding by time?
- How does time-based sharding work for logs and events?
- How do TTL/archiving work together with sharding?
- What is geo-sharding?
- When is data sharded by region?
- What legal and product reasons can influence geographic data placement?
- When is replication better, and when is partitioning better?
- When is partitioning better, and when is sharding better?
- Can replication and sharding be used together?
- How are replication, partitioning, and sharding usually combined in large systems?
- How would you design a system with many reads, few writes, and a large amount of historical data?
- How would you design a system with high write load and strict latency requirements?
- How would you design an order system with billions of records and analytics requirements?
- What mistakes are most commonly made when introducing sharding?
- Why is premature sharding a bad idea?
- How would you explain to the business that sharding will significantly complicate development and operations?
- What trade-offs would you discuss before moving to sharding?
- Describe step by step how you would migrate a monolithic database to a sharded architecture.
- Describe how you would build a DB HA architecture with primary, replicas, backups, and DR.
- How would you test failover, backup restore, and disaster recovery in a real system?

### 7.9 MongoDB

#### 7.9.1 Data model
- What is MongoDB, and what type of DBMS is it?
- How does a document database differ from a relational database?
- What is a document in MongoDB?
- What is a collection in MongoDB?
- What is BSON, and how does it differ from JSON?
- Why does MongoDB use BSON instead of plain JSON?
- What data types does BSON support?
- What is ObjectId?
- What is ObjectId made of?
- What are the pros and cons of ObjectId compared to an auto-incrementing integer id?
- When does it make sense to use your own _id instead of ObjectId?
- Is _id mandatory in a document?
- Can _id be composite?
- What does schema-less mean in MongoDB?
- Is it true that MongoDB has no schema at all?
- What advantages does a flexible schema provide?
- What disadvantages does a flexible schema create?
- Why does a “flexible schema” not mean “no schema needed”?
- How do you ensure document structure consistency in MongoDB?
- What is schema validation in MongoDB?
- How do you use JSON Schema validation in MongoDB?
- When is schema validation useful, and when does it get in the way?
- What are embedding and referencing in MongoDB?
- When is it better to store nested documents, and when references to other documents?
- What are the advantages of embedding?
- What are the disadvantages of embedding?
- What are the advantages of referencing?
- What are the disadvantages of referencing?
- In which cases is a one-to-one relationship better modeled by embedding?
- In which cases is a one-to-many relationship better modeled as an array inside a document?
- In which cases is a one-to-many relationship better modeled as a separate collection?
- How do you model many-to-many relationships in MongoDB?
- How do you model hierarchical data in MongoDB?
- How do you model a category tree in MongoDB?
- How do you model comments with nested replies?
- How do you model tags and labels in MongoDB?
- How do you model an entity’s change history in MongoDB?
- How do you model events and an event log in MongoDB?
- How do you model a shopping cart in MongoDB?
- How do you model orders and order items in MongoDB?
- How do you model users and roles in MongoDB?
- How do you model “favorites” or “likes” in MongoDB?
- How do you model view counters or like counters in MongoDB?
- When is denormalization in MongoDB a normal practice?
- How does denormalization in MongoDB differ from denormalization in relational databases?
- When does excessive duplication of data in MongoDB become a problem?
- How do you update denormalized data without it getting out of sync?
- What is document growth, and why is it important in MongoDB?
- Why can document growth after an update be a problem?
- What is the document size limit in MongoDB?
- What is the maximum size of a single document in MongoDB?
- Why are large documents often harmful to performance?
- When do arrays inside a document become a problem?
- What is an unbounded array, and why is it an anti-pattern?
- How do you store very large lists of related objects in MongoDB?
- When should you split one collection into several by document type?
- When, on the contrary, is it better to keep several similar document types in one collection?
- How does the choice of data model affect query complexity?
- How does the choice of data model affect the number of required indexes?
- How does the choice of data model affect operation atomicity?
- Which operations in MongoDB are atomic at the document level?
- Why is modeling “everything needed in one document” often considered a strength of MongoDB?
- How do you design a data model if one aggregate is most often read as a whole?
- How do you design a data model if one part of an entity is often updated separately?
- How do you design a data model for read-heavy workloads?
- How do you design a data model for write-heavy workloads?
- How do you design a data model for analytical queries?
- Is MongoDB suitable for OLTP scenarios?
- Is MongoDB suitable for OLAP scenarios?
- In which cases is MongoDB a poor choice?
- When is MongoDB better than PostgreSQL or MySQL?
- When is MongoDB worse than a relational database?
- What is a capped collection?
- When should capped collections be used?
- What is a time series collection in MongoDB?
- What scenarios are time series collections suitable for?
- What is a TTL collection or TTL behavior in MongoDB?
- How do you organize automatic deletion of old data?
- How do you store polymorphic entities in MongoDB?
- How do you organize document schema versioning?
- How do you migrate documents after changing the data model?
- What are typical MongoDB data modeling anti-patterns?
- Why does porting a relational schema “as is” to MongoDB often lead to poor design?
- How do you choose between embed and reference in practice?
- How would you explain to a junior developer how to design a data model in MongoDB?
- What trade-offs would you discuss if an architect proposes storing almost everything in one document?
- How would you design a data model for a blog, a product catalog, a chat, an event feed?
- How would you explain when MongoDB fits a microservices architecture and when it does not?
- How would you choose MongoDB over PostgreSQL for a new project?
- What question about MongoDB data modeling would you ask a senior candidate?
- What MongoDB data modeling mistakes have you most often seen in production?
- How would you review a MongoDB data model in a new system?
- What trade-offs between read simplicity, update simplicity, and storage simplicity do you have to make in MongoDB?

#### 7.9.2 Indexes and aggregations
- What is an index in MongoDB?
- Why are indexes needed in MongoDB?
- How do you check what indexes a collection has?
- How do you create an index in MongoDB?
- How do you drop an index in MongoDB?
- What is the _id index, and is it created automatically?
- What types of indexes does MongoDB support?
- What is a single-field index?
- What is a compound index?
- What is a multikey index?
- What is a text index?
- What is a hashed index?
- What is a wildcard index?
- What is a TTL index?
- What is a sparse index?
- What is a partial index?
- What is a unique index?
- When do you need a compound index instead of several single-field ones?
- How does field order work in a compound index?
- What is the prefix rule in MongoDB compound indexes?
- Which queries will be able to use the index {a:1, b:1, c:1}, and which will not?
- Why is field order in a compound index critically important?
- How do you choose the field order in a compound index?
- What matters more when designing a compound index: equality, sort, or range?
- Why do range conditions in a compound index limit the use of subsequent fields?
- How does a multikey index work for arrays?
- What limitations do multikey indexes have?
- How are arrays of documents indexed?
- Can you use a compound index if one of the fields is an array?
- What is a covered query in MongoDB?
- How do you make a query a covered query?
- Why can a covered query be faster than a regular query?
- What is index intersection in MongoDB?
- Is MongoDB always able to efficiently combine several indexes?
- Why is one good compound index often better than several single-field indexes?
- What is index selectivity?
- Why is an index on a low-cardinality field often useless?
- When can an index hurt performance?
- Why do too many indexes hurt writes?
- Why do indexes consume a lot of memory?
- How do indexes affect insert/update/delete?
- What is rebuilding an index, and when is it needed?
- What is background index creation, and how does it work in modern MongoDB versions?
- Can indexes be created without downtime?
- What is a hidden index?
- Why hide an index before deleting it?
- How do you tell whether an index is used by real queries?
- What is explain() in MongoDB?
- What query execution stages does explain show?
- What is the difference between queryPlanner, executionStats, and allPlansExecution?
- What do COLLSCAN and IXSCAN mean in explain?
- Why is COLLSCAN a warning sign?
- Does IXSCAN always mean a good plan?
- What is a winning plan?
- What are rejected plans?
- Why might the optimizer choose a different index than the one you expected?
- When does it make sense to use hint()?
- What are the risks of forcing hint()?
- What is a text index, and when is it needed?
- What limitations do text indexes have in MongoDB?
- How does MongoDB text search differ from Elasticsearch/OpenSearch?
- When is a MongoDB text index enough, and when do you need a separate search engine?
- What is a TTL index, and how does it work?
- Can a TTL index be used for strict guarantees that data will be deleted exactly at a specific time?
- Why does TTL cleanup not always happen immediately?
- What is a partial index, and when is it useful?
- What is a sparse index, and how does it differ from a partial index?
- How does a partial index help reduce index size?
- What is a unique index, and what pitfalls exist with nullable fields?
- How does a unique index work for documents without the field?
- How does a unique multikey index work?
- What is a hashed index, and when is it needed?
- Why is a hashed index useful for sharding?
- Can a hashed index be used for range queries?
- What is a wildcard index, and when is it appropriate?
- What trade-offs do wildcard indexes have?
- How do you index nested fields?
- How do you index a field inside an array of embedded documents?
- What is collation, and how does it affect an index?
- How do sorts use an index in MongoDB?
- Why might a sort fail to use an index even if the right field exists?
- When does sorting trigger an in-memory sort?
- How does limit affect the query plan?
- How does skip affect performance?
- Why can pagination with skip/limit on large datasets be inefficient?
- How do you implement keyset pagination in MongoDB?
- How do you find duplicates in MongoDB?
- How does an index help upsert operations?
- What is write amplification caused by indexes?
- How do you choose the minimum sufficient set of indexes for a collection?
- What metrics and tools would you use to analyze MongoDB indexes?
- How do you profile slow queries in MongoDB?
- What is the profiler in MongoDB?
- How do you enable profiling, and what levels exist?
- What is the aggregation framework?
- Why is an aggregation pipeline needed?
- How does an aggregation pipeline differ from a regular find?
- What aggregation pipeline stages do you know?
- What does $match do?
- What does $project do?
- What does $group do?
- What does $sort do?
- What does $limit do?
- What does $skip do?
- What does $unwind do?
- What does $lookup do?
- What does $facet do?
- What does $addFields / $set do?
- What does $unset do?
- What does $count do?
- What does $bucket do?
- What does $bucketAuto do?
- What does $sortByCount do?
- What does $replaceRoot / $replaceWith do?
- What does $merge do?
- What does $out do?
- What does $graphLookup do?
- What does $setWindowFields do?
- How does $lookup work in MongoDB?
- How does $lookup differ from a JOIN in SQL?
- Why can $lookup be an expensive operation?
- When is $lookup acceptable, and when is it better to denormalize data?
- How does the order of stages in a pipeline affect performance?
- Why should $match usually be placed as early as possible?
- Why does an early $project sometimes help, and sometimes not matter?
- How can an index be used inside an aggregation pipeline?
- Which stages can use an index?
- Why does $group usually not use an index directly?
- When does an aggregation pipeline start writing to temporary files?
- What is allowDiskUse?
- What risks and limitations does allowDiskUse have?
- How do you aggregate data by time in MongoDB?
- How do you group by day, week, or month?
- How do you compute rolling aggregates in MongoDB?
- How do you implement window functions in MongoDB?
- What tasks are conveniently solved with $facet?
- When does an aggregation pipeline become too complex and hard to maintain?
- When is it better to move aggregation into the application, and when should it stay in MongoDB?
- How would you explain the difference between OLTP queries and aggregation pipelines in MongoDB?
- How would you design indexes for an orders collection with frequent filters by userId, status, and createdAt?
- What indexes would you create for a chat where you search by roomId and sort by createdAt?
- How would you optimize a slow pipeline with $lookup, $unwind, and $group?
- What questions about MongoDB indexes would you ask a middle-level candidate?
- What questions about the aggregation pipeline would you ask a senior-level candidate?
- What typical MongoDB index design mistakes have you seen?
- What typical mistakes when working with the aggregation framework have you seen?
- How would you conduct a performance review of a MongoDB collection with many queries and indexes?

#### 7.9.3 Consistency and transactions
- What consistency guarantees does MongoDB provide?
- What does consistency mean in the context of MongoDB?
- What is eventual consistency, and where does it appear in MongoDB?
- What is strong consistency, and can it be achieved in MongoDB?
- What is a replica set in MongoDB?
- Why is a replica set the basic fault-tolerance unit in MongoDB?
- What is a primary in a replica set?
- What is a secondary in a replica set?
- How is a primary elected in MongoDB?
- What is an election, and when does it happen?
- What happens to reads and writes during an election?
- Why are write errors possible during a primary switch?
- What is write concern?
- What write concern values exist?
- What does w:1 mean?
- What does w:majority mean?
- What does j:true mean?
- What is the difference between a write acknowledgment and actual replication?
- When should you use write concern majority?
- What trade-offs does write concern majority have?
- What is read concern?
- What read concerns does MongoDB support?
- What does local read concern mean?
- What does majority read concern mean?
- What does linearizable read concern mean?
- What does snapshot read concern mean?
- When do you need a linearizable read?
- Why are linearizable reads used rarely?
- What is read preference?
- What read preference modes exist in MongoDB?
- What is the difference between primary, primaryPreferred, secondary, secondaryPreferred, and nearest?
- When is it dangerous to read from a secondary?
- What problems can arise when reading from a secondary?
- What is replication lag?
- How does replication lag affect read consistency?
- Can you read stale data in MongoDB?
- How do you achieve read-your-writes semantics?
- How do read concern and write concern affect each other?
- What is causal consistency?
- How does MongoDB support causal consistency?
- When is causal consistency important for applications?
- What guarantees does a session provide in MongoDB?
- What is a session in MongoDB?
- What is a logical session id?
- What problems do client sessions help solve?
- What are retryable writes?
- Which operations are retryable writes?
- What risks exist with write retries?
- How is idempotency related to retryable writes?
- Does MongoDB support transactions?
- When did multi-document transactions appear in MongoDB?
- Why are transactions needed in a document database?
- When are transactions in MongoDB really necessary, and when is it better to change the data model?
- Why does “atomicity at the single-document level” often make transactions unnecessary?
- What limitations do MongoDB transactions have?
- What overhead do multi-document transactions introduce?
- How do transactions in MongoDB differ from transactions in relational DBMSs?
- What isolation levels are effectively available for MongoDB transactions?
- What is snapshot isolation in MongoDB?
- What anomalies does snapshot isolation prevent?
- What anomalies may still remain even with snapshot isolation?
- How do you start a transaction in MongoDB?
- How do you end a transaction in MongoDB?
- What happens if a transaction is too long?
- Why are long transactions harmful in MongoDB?
- How do transactions affect memory and cluster performance?
- Can you use transactions in a sharded cluster?
- What additional complexities do distributed transactions introduce in MongoDB?
- How does transaction commit work in a sharded cluster?
- What is two-phase commit, and does MongoDB use it in some scenarios?
- What is a transient transaction error?
- How should an application handle transient transaction errors?
- What is a write conflict in MongoDB?
- Why can concurrent updates lead to write conflicts?
- How should an application react to a write conflict?
- How do transactions work when updating multiple collections?
- Can you read and write across different databases in one transaction?
- How does read concern work inside a transaction?
- How does write concern work inside a transaction?
- Why can operations inside a transaction be slower than regular operations?
- When is it better to replace a transaction with denormalization and an atomic document update?
- How would you implement a money transfer between two accounts in MongoDB?
- How would you implement inventory reservation in MongoDB?
- How would you implement reliable event publishing alongside a MongoDB write?
- How does the outbox pattern fit with MongoDB?
- How do you ensure consistency between MongoDB and an external system?
- What is the distributed consistency problem when integrating MongoDB with Kafka, Redis, or an external API?
- How would you explain to the business that “the write was acknowledged” does not always mean “all replicas are already in sync”?
- What read and write modes would you choose for a banking system?
- What read and write modes would you choose for a product catalog?
- What read and write modes would you choose for a news feed or analytics?
- In what scenarios is eventual consistency in MongoDB fully acceptable?
- In what scenarios is eventual consistency in MongoDB dangerous?
- How does MongoDB behave during a network partition?
- What happens if the primary becomes unavailable but some secondaries remain available?
- What is a majority quorum, and why is it important?
- Why is a two-node replica set bad for reliable elections?
- Why is an arbiter needed, and what are its downsides?
- When is an arbiter a bad idea?
- What problems can occur with split-brain, and how does MongoDB prevent them?
- How do you monitor replica set health?
- What signs point to consistency or replication problems?
- What metrics and commands do you use to diagnose transactions and replication?
- How would you debug a complaint like “after writing, the user sometimes sees old data”?
- How would you debug a complaint like “some transactions sometimes roll back for no obvious reason”?
- What questions about consistency and transactions would you ask a middle-level candidate?
- What questions about consistency and transactions would you ask a senior-level candidate?
- What typical mistakes in using MongoDB transactions have you seen in practice?
- When is using transactions in MongoDB a sign of a poor data model?
- How would you compare MongoDB’s consistency model with PostgreSQL’s?
- How would you explain the trade-off between performance, availability, and consistency in MongoDB?

### 7.10 Redis

#### 7.10.1 Data structures and usage scenarios
- What is Redis, and what class of systems does it belong to?
- Is Redis a database, a cache, a message broker, or all of them at once?
- In which scenarios is Redis the best fit?
- In which scenarios should Redis not be used?
- How does Redis differ from Memcached?
- How does Redis differ from a relational database?
- How does Redis differ from MongoDB?
- Why is Redis often called an in-memory datastore?
- What does it mean that Redis stores data in memory?
- Can Redis be used as the primary data store?
- What trade-offs arise when Redis is used as the primary datastore?
- What core data structures does Redis support?
- What data abstractions does Redis provide beyond simple key-value?
- What is the key space in Redis?
- What restrictions apply to keys?
- How should you choose good key names?
- Why is key naming important in Redis?
- What key naming schemes are commonly used in production?
- What is the maximum size of a value in Redis?
- What is TTL in Redis?
- How do you set a TTL on a key?
- How does EXPIRE differ from PEXPIRE?
- What happens to TTL when the key’s value is updated?
- How do you check how much lifetime a key has left?
- What is the difference between TTL and PTTL?
- What happens if you set a TTL on a non-existent key?
- What is a persistent key in Redis?
- How do you remove a TTL from a key?
- What is lazy expiration?
- What is active expiration?
- How does Redis delete expired keys?
- Why does an expired key not always disappear immediately?
- How does TTL affect memory and performance?
- What eviction policies does Redis support?
- What is the difference between noeviction, allkeys-lru, volatile-lru, allkeys-lfu, volatile-ttl, and random policies?
- When should you use LRU, and when LFU?
- What is approximate LRU/LFU in Redis?
- Why does Redis not always use a “perfect” LRU?
- What is a string in Redis?
- When is a string the best choice?
- Can JSON be stored in a string?
- What are the pros and cons of storing JSON in a string?
- What are INCR/DECR, and where are they used?
- Why are INCR/DECR operations considered atomic?
- How do you use strings for counters?
- How do you use strings for state flags?
- How do you use strings to cache query results?
- What is a hash in Redis?
- When is a hash better than a string?
- How does a hash help save memory?
- What commands are used to work with hashes?
- When should you store a user object in a hash, and when in JSON/string?
- What is a list in Redis?
- When is a list most suitable?
- What commands are used to work with lists?
- What is the difference between LPUSH/RPUSH and LPOP/RPOP?
- How do you use a list as a queue?
- How do you use a list as a stack?
- What limitations and pitfalls do lists have?
- What is a set in Redis?
- When is a set more useful than a list?
- What commands are used to work with sets?
- How do you use a set to store unique values?
- How do you use a set for tags, subscriptions, and membership checks?
- What are set intersection, union, and difference operations in Redis?
- Where are SINTER, SUNION, and SDIFF useful in practice?
- What is a sorted set (zset)?
- How does a sorted set differ from a set?
- What problems are solved with a sorted set?
- How are score and member structured in a sorted set?
- How do you use a sorted set for a leaderboard?
- How do you use a sorted set to store prioritized tasks?
- How do you use a sorted set to store events by time?
- What commands are used to work with sorted sets?
- Why are sorted sets useful for delayed jobs?
- What is a bitmap in Redis?
- When is a bitmap better than a set?
- How do you use a bitmap for tracking boolean states?
- What are bit operations in Redis, and where are they useful?
- What is HyperLogLog?
- What is HyperLogLog used for?
- How accurate is HyperLogLog, and what trade-offs does it make?
- Where is HyperLogLog useful in practice?
- What are geospatial commands in Redis?
- How does Redis store geodata?
- For what tasks are GEOADD, GEORADIUS, and GEOSEARCH best suited?
- Can Redis be used to find nearby objects?
- What are the limitations of Redis geo functionality?
- What are Redis Streams?
- How do Streams differ from Lists?
- For what tasks are Streams more suitable than Pub/Sub?
- Which structure would you choose for a task queue: list, stream, or sorted set?
- When should you use a set instead of a hash?
- When should you use a sorted set instead of a list?
- When should you use a string instead of a hash?
- When should you use RedisJSON or serialized JSON in a string?
- What is a module in Redis?
- What popular Redis modules do you know?
- What is RedisJSON?
- What is RediSearch?
- What are Bloom filters in Redis Stack?
- When should Redis Stack be used?
- What is a pipeline in Redis?
- How does a pipeline differ from a transaction?
- How does a pipeline affect latency?
- When is a pipeline especially useful?
- What pitfalls does a pipeline have?
- What is Lua scripting in Redis?
- Why use Lua scripts in Redis?
- When is a Lua script better than several separate commands?
- How does EVAL differ from regular commands?
- What risks does a heavy Lua script create in Redis?
- Why are long operations dangerous in Redis?
- What does it mean that Redis is mostly single-threaded?
- How does single-threaded execution affect Redis performance?
- What types of operations can block Redis?
- Why can commands with high complexity be dangerous?
- What is a big key in Redis?
- Why are big keys harmful?
- How do you find big keys in Redis?
- What is a hot key?
- Why are hot keys dangerous?
- How do you identify hot keys?
- What ways of dealing with hot keys do you know?
- What is cache warming?
- What is a cache stampede?
- What is a cache avalanche?
- What is cache penetration?
- How does Redis help build caches?
- What caching patterns are commonly used with Redis?
- What is cache-aside?
- What is read-through cache?
- What is write-through cache?
- What is write-behind cache?
- What are the pros and cons of each caching pattern?
- How do you invalidate cache in Redis?
- When is it better to delete a key, and when to update it?
- How do you build a cache for a user profile?
- How do you build a cache for a list/feed?
- Why is caching lists and aggregates harder than caching single objects?
- How do you cache an expensive SQL query with Redis?
- How do you cache session storage in Redis?
- What data is often stored in Redis in web applications?
- What data should not or should preferably not be stored in Redis?
- How do you use Redis for feature flags?
- How do you use Redis for idempotency keys?
- How do you use Redis for anti-fraud / anti-spam scenarios?
- How do you use Redis to store temporary tokens?
- How do you use Redis for OTP / verification codes?
- How do you use Redis for a shopping cart?
- How do you use Redis for presence / online status?
- How do you use Redis for event deduplication?
- How do you use Redis as a distributed cache in microservices?
- How is Redis used in a high-load backend?
- How does Redis work with memory?
- What is maxmemory?
- What happens if Redis runs out of memory?
- What eviction policy would you choose for caching API responses, and why?
- How do you estimate the required amount of memory for Redis?
- What is fragmentation in Redis?
- What is the memory fragmentation ratio?
- How does memory fragmentation affect Redis?
- What role does jemalloc play in Redis?
- What is persistence in Redis?
- What is the difference between RDB and AOF?
- How does RDB snapshotting work?
- How does AOF work?
- What are the pros and cons of RDB?
- What are the pros and cons of AOF?
- When is it better to use only RDB?
- When is it better to use only AOF?
- When does it make sense to enable both RDB and AOF?
- What is the fsync policy in AOF?
- How do appendfsync always, everysec, and no differ from each other?
- How does persistence affect latency?
- What is AOF rewrite?
- Why is AOF rewrite needed?
- How does snapshotting affect Redis performance?
- How does Redis process forking affect memory?
- Why can persistence unexpectedly increase memory usage in Redis?
- How does Redis behave after a restart?
- Can data be lost in Redis?
- Which data loss scenarios are the most likely?
- What is replication in Redis?
- How does master-replica work in Redis?
- What is asynchronous replication in Redis?
- Can zero data loss be guaranteed in Redis?
- What is Redis Sentinel?
- What is Sentinel used for?
- How does Sentinel differ from Redis Cluster?
- What is Redis Cluster?
- How does Redis Cluster shard data?
- What are hash slots in Redis Cluster?
- How does Redis decide on which node to store a key?
- What is a hash tag in Redis Cluster?
- When are hash tags useful?
- What limitations do multi-key operations have in Redis Cluster?
- Why do not all multi-key commands work transparently in Cluster?
- What happens during failover in Redis?
- What problems can occur during failover?
- What is split-brain in the context of Redis?
- How do you ensure high availability for Redis?
- What Redis deployment strategies would you use in production?
- What should be monitored in Redis?
- What key Redis metrics would you track?
- What is ops/sec in Redis?
- What is cache hit rate?
- Why is hit rate not enough to look at in isolation from latency?
- What should be monitored regarding Redis memory?
- What should be monitored regarding Redis replication?
- What should be monitored regarding Redis persistence?
- Which Redis commands are dangerous in production?
- Why is KEYS considered dangerous?
- How is SCAN better than KEYS?
- What are the limitations of SCAN?
- Why are FLUSHALL and FLUSHDB dangerous?
- Which commands can unexpectedly block Redis?
- How do you safely perform mass key deletion?
- How do you debug a slow Redis?
- What is SLOWLOG?
- How do you use MONITOR, and why should it be used carefully?
- What are latency spikes in Redis?
- What causes latency spikes in Redis?
- What is persistence-induced latency?
- How do you investigate the cause of increased latency in Redis?
- How is Redis used together with application-level sharding?
- How is Redis integrated with CDN and application cache?
- What typical anti-patterns of using Redis do you know?
- Why should Redis not be used as a replacement for a full relational database without a strong reason?
- When does Redis turn from an accelerator into a source of problems?
- What questions would you ask the team before introducing Redis into a new system?
- What architectural trade-offs would you discuss before making Redis a critical part of the system?

#### 7.10.2 Rate limiting, locks, pub/sub, streams
- What is rate limiting, and why is it needed?
- What types of rate limiting do you know?
- How does rate limiting differ from throttling?
- How does rate limiting differ from a quota?
- What rate limiting scenarios exist in backend systems?
- Where is it better to apply rate limiting: at the API gateway, in the application, in a reverse proxy, or in Redis?
- Why is Redis often used for rate limiting?
- What advantages does Redis provide for rate limiting?
- What disadvantages does Redis introduce for rate limiting?
- How do you implement a simple request counter in Redis?
- How do you implement a fixed window rate limiter in Redis?
- What are the drawbacks of the fixed window algorithm?
- How do you implement a sliding window rate limiter in Redis?
- How does a sliding window log differ from a sliding window counter?
- What are the pros and cons of a sliding window log?
- How do you implement a token bucket in Redis?
- How do you implement a leaky bucket in Redis?
- What is the difference between a token bucket and a leaky bucket?
- Which rate limiting algorithm would you choose for a public API, and why?
- Which algorithm would you choose for a login endpoint, and why?
- Which algorithm would you choose for brute-force protection?
- How do you use INCR and EXPIRE for rate limiting?
- What is the problem with the INCR + EXPIRE pattern if they are done as two separate commands?
- How do you make rate limiting atomic in Redis?
- When does rate limiting require a Lua script?
- How do you use a sorted set for a sliding window rate limiter?
- What pitfalls exist in sorted set rate limiting?
- How do you clean up old elements from a sorted set in a sliding window implementation?
- What key should be used for a per-user rate limit?
- How do you implement a per-IP rate limit?
- How do you implement a per-endpoint rate limit?
- How do you combine multiple levels of rate limiting?
- What do you do if the rate limiter itself becomes a bottleneck?
- How do you deal with hot keys in rate limiting?
- How do you account for burst traffic?
- How should you properly return limit information to the client?
- Which HTTP headers are commonly used for rate limiting?
- What is distributed rate limiting?
- What problems exist with distributed rate limiting?
- How does clock skew affect rate limiting?
- How does server time affect a Redis-based limiter?
- How do you test rate limiting in production?
- How do you monitor rate limiting?
- How do you avoid a rate limiter accidentally blocking legitimate users?
- What business and UX trade-offs come with rate limiting?
- How do you implement rate limiting for background jobs?
- How do you implement rate limiting for sending email/SMS?
- How do you implement an idempotency window with Redis?
- How do you implement anti-spam limits on user actions with Redis?
- How do you implement a limit on failed login attempts?
- How do you build a rate limiter if Redis is temporarily unavailable?
- What is better: fail-open or fail-closed behavior for a rate limiter?
- What security vs availability trade-off arises here?
- What is a distributed lock, and why is it needed?
- What problems are solved in practice with a distributed lock?
- When is a distributed lock really needed, and when is it an anti-pattern?
- Why is Redis often used for distributed locks?
- How does a Redis distributed lock differ from a row lock in a database?
- How do you implement a simple lock in Redis?
- Why was SETNX historically used for locks?
- Why is SET key value NX PX ttl more commonly recommended today?
- Why should the lock value be unique?
- Why is it not enough to simply do DEL key when releasing a lock?
- How do you safely release a lock in Redis?
- Why is a Lua script often used to release a lock?
- What happens if the lock expired, but the old owner still executes DEL?
- What TTL should you choose for a lock?
- What happens if the TTL is too short?
- What happens if the TTL is too long?
- How do you extend a lock if the operation runs longer than expected?
- What risks arise when extending a lock?
- What do you do if the process dies before releasing the lock?
- How do you avoid eternal locks in Redis?
- What is a fencing token, and why is it needed?
- Why might one Redis lock be insufficient for strict correctness?
- In which scenarios is a Redis lock suitable, and in which is it better to use a DB, ZooKeeper, or etcd?
- What is Redlock?
- How does the Redlock algorithm work?
- What assumptions underlie Redlock?
- What are the pros and cons of Redlock?
- Why is there controversy around Redlock?
- In which cases can Redlock be used, and in which should it not?
- Why does a distributed lock not replace idempotency?
- What typical mistakes are made when using Redis locks?
- How do you use a lock for cron jobs?
- How do you use a lock for leader election?
- How do you use a lock to protect against duplicate message processing?
- How do you use a lock to synchronize access to a shared resource?
- How do you monitor lock contention?
- How do you debug the problem of “stuck” locks?
- How can you tell that there is too much contention for a lock in the system?
- What alternatives to a distributed lock exist?
- When is it better to use optimistic concurrency instead of a distributed lock?
- When is it better to use queue-based serialization instead of a distributed lock?
- What question would you ask before approving the use of a Redis lock in the architecture?
- What is Pub/Sub in Redis?
- How does Pub/Sub work in Redis?
- What entities participate in Pub/Sub?
- What is a channel in Redis Pub/Sub?
- Can Redis Pub/Sub store message history?
- What happens if a subscriber is disconnected at the time a message is published?
- Does Redis Pub/Sub guarantee message delivery?
- How does Pub/Sub differ from a message queue?
- How does Redis Pub/Sub differ from Kafka?
- How does Redis Pub/Sub differ from RabbitMQ?
- In which scenarios does Redis Pub/Sub work well?
- In which scenarios should Redis Pub/Sub not be used?
- How do you use Pub/Sub for real-time notifications?
- How do you use Pub/Sub for cache invalidation between services?
- How do you use Pub/Sub in a chat or presence system?
- What scalability limitations does Redis Pub/Sub have?
- Why is Pub/Sub a poor fit for critical business events?
- Can fan-out be implemented with Redis Pub/Sub?
- What are pattern subscriptions in Redis?
- What are the risks of using wildcard subscriptions?
- How does Pub/Sub behave in Redis Cluster?
- What pitfalls does Pub/Sub have during failover?
- How do you monitor load on Pub/Sub?
- How do you test Redis Pub/Sub?
- What should you choose for inter-service events: Pub/Sub, Stream, RabbitMQ, or Kafka?
- What is the trade-off between simplicity and reliability in Redis Pub/Sub?
- What are Redis Streams?
- How do Streams differ from Pub/Sub?
- How do Streams differ from Lists?
- For what tasks are Redis Streams best suited?
- What is an append-only log in the context of Streams?
- How is a message ID structured in a Stream?
- Why does a Stream ID contain a timestamp and a sequence?
- What commands are used to write to and read from a Stream?
- What does XADD do?
- What does XRANGE do?
- What does XREAD do?
- How do you read from a Stream starting from a specific ID?
- How do you read only new messages?
- What is a consumer group in Redis Streams?
- Why are consumer groups needed?
- How does a consumer group in Streams differ from a regular Pub/Sub subscriber?
- What is XGROUP?
- What is the pending entries list (PEL)?
- What is XREADGROUP?
- How does Redis know which message has already been delivered to a consumer group?
- What happens if a consumer receives a message but does not acknowledge it?
- What does XACK do?
- Why is XACK needed?
- What happens if you never call XACK?
- How do you reprocess stuck messages?
- What does XCLAIM do?
- What does XAUTOCLAIM do?
- How does XCLAIM differ from XAUTOCLAIM?
- How do you find messages that got “stuck” on a dead consumer?
- What is an idempotent consumer, and why is it important for Streams?
- Can you get at-least-once delivery with Redis Streams?
- Can you get exactly-once delivery with Redis Streams?
- What delivery guarantees do Redis Streams provide?
- What is fan-out processing through consumer groups?
- How do you scale consumer groups?
- What happens if one consumer is slower than the others?
- How do Streams behave as the amount of data grows?
- Do you need to manually trim a Stream?
- What are MAXLEN and approximate trimming?
- What are the risks of uncontrolled Stream growth?
- How do you choose a retention length for a Stream?
- How do you use Streams for a task queue?
- How do you use Streams for lightweight event sourcing scenarios?
- How do you use Streams for an audit trail?
- When are Streams better than Kafka, and when are they worse?
- What limitations do Streams have compared to Kafka?
- Why are Streams convenient for smaller systems and internal pipelines?
- What pitfalls do consumer groups have?
- What happens if a consumer group falls far behind?
- How do you monitor lag in Redis Streams?
- How do you monitor the size of pending entries?
- What metrics are important for Streams in production?
- How do you build a retry policy on top of Redis Streams?
- How do you implement a dead-letter queue on top of Streams?
- How do you build delayed retries in Redis: with Streams or a sorted set?
- What is better for a task queue: Streams, Lists, or Kafka?
- When should Redis Streams not be used as a full replacement for a message broker?
- How would you design a background task processing system on Redis Streams?
- What questions about reliability, data growth, and operations would you discuss before choosing Streams?

### 7.11 Memcached
- What is Memcached?
- What is Memcached typically used for?
- How does Memcached differ from a full-fledged database?
- How does Memcached differ from Redis?
- In which scenarios is Memcached a better fit than Redis?
- In which scenarios is Memcached a worse fit than Redis?
- Why is Memcached often chosen specifically as a cache rather than as a general-purpose store?
- What core properties of Memcached are important to understand before using it?
- Is Memcached a persistent store?
- What happens to data in Memcached after the process restarts?
- Why is the lack of persistence sometimes an advantage rather than a drawback?
- What data types does Memcached support?
- Can you store complex structures in Memcached without serialization?
- How are objects usually serialized before being stored in Memcached?
- What pitfalls exist when serializing objects for Memcached?
- What is a key-value cache, and how does Memcached implement this model?
- What restrictions apply to keys in Memcached?
- What restrictions apply to value size in Memcached?
- Why can very large values be a problem in Memcached?
- What Memcached commands do you know?
- How do set, add, and replace differ?
- What is the difference between get and gets?
- What are CAS and check-and-set operations used for?
- What does delete do?
- What do incr and decr do?
- What do append and prepend do?
- What is TTL in Memcached?
- How does key lifetime work in Memcached?
- What is the difference between relative and absolute expiration time in Memcached?
- What happens if you set a very large TTL value?
- What is expiration and how is it different from eviction?
- Are expired records removed immediately after their TTL expires?
- What happens if you read a key whose lifetime has already expired?
- What is lazy expiration?
- How does Memcached manage memory?
- What are slabs in Memcached?
- Why does Memcached use a slab allocator?
- How does the slab allocator help fight memory fragmentation?
- What is a slab class?
- How does Memcached choose a slab class for a new object?
- Why does object size affect how much memory will actually be used in Memcached?
- What is internal fragmentation in the context of Memcached?
- Why can many objects of slightly different sizes lead to inefficient memory usage?
- What happens if Memcached runs out of memory?
- Which eviction algorithm does Memcached use?
- What is LRU in Memcached?
- Does Memcached always use pure LRU?
- What is segmented LRU / modern LRU in Memcached?
- Why should old and rarely used keys be evicted first?
- What are hot keys and how do they affect Memcached?
- How does Memcached behave during a cache stampede?
- What is a cache stampede and how can it be prevented when using Memcached?
- How do you implement dogpile protection with Memcached?
- What is a cache miss?
- What is hit rate?
- Which Memcached metrics are important in production?
- How do you measure Memcached efficiency?
- How do you tell that Memcached is too small in terms of memory?
- How do you tell that the problem is not cache size but bad keys or access patterns?
- Which commands or tools are used to inspect Memcached statistics?
- What can you see in stats?
- What does stats items show?
- What does stats slabs show?
- What does evictions show?
- What do curr_items and total_items show?
- What do bytes and limit_maxbytes show?
- How do you interpret a high eviction rate?
- Why does a low hit rate not always mean Memcached is useless?
- How do you choose what exactly to cache in Memcached?
- What data should not be stored in Memcached?
- Should you cache the results of heavy SQL queries in Memcached?
- Should you cache user sessions in Memcached?
- What are the pros and cons of storing sessions in Memcached?
- How do you use Memcached for caching HTML fragments or API responses?
- What are the risks of caching entire domain model objects?
- How do you design keys for Memcached?
- Why is a key naming convention important?
- How do you organize key namespaces?
- How do you invalidate a group of keys in Memcached if there is no tag support?
- How do you implement versioned keys in Memcached?
- What is cache key design?
- Why can poor key design destroy the value of a cache?
- How do you avoid key collisions between different services?
- What do you do if the same value needs to be cached under multiple keys?
- How do you synchronize the cache and the primary data store?
- How does cache-aside differ from write-through when using Memcached?
- Is Memcached suitable for write-through scenarios?
- When is cache-aside a better choice?
- How do you invalidate cache after writing to the database?
- Which is better: invalidating the cache or immediately overwriting the value?
- What are the risks of stale data in Memcached?
- How do you reduce the probability of reading stale data?
- What is eventual consistency between the database and the cache?
- How does Memcached behave in a distributed environment?
- Does Memcached have built-in replication?
- Does Memcached have built-in clustering?
- How does Memcached scale horizontally?
- What is client-side sharding for Memcached?
- How does a client choose the Memcached server for a specific key?
- What is consistent hashing in the context of Memcached?
- Why is consistent hashing important for distributed caching?
- What will happen to hit rate if you add a new Memcached node without consistent hashing?
- What happens to the cache if one of the nodes goes down?
- How does rebalancing keys between nodes affect application performance?
- Why is Memcached usually not used where strict consistency matters?
- How do you provide high availability when using Memcached?
- Can you use multiple Memcached instances for fault tolerance?
- What limitations does this approach have?
- How do client libraries work with a pool of Memcached servers?
- What mistakes are often made when operating Memcached in production?
- What happens if all keys have the same TTL and expire at the same time?
- How do you mitigate the effect of a large number of keys expiring simultaneously?
- What is TTL jitter and why is it needed?
- Why can a TTL that is too long be harmful?
- Why can a TTL that is too short be harmful?
- How do you choose TTL for different types of data?
- How do you use Memcached in a read-heavy application?
- How do you use Memcached in a high-throughput API?
- Can Memcached be used as a primary store?
- Why is using Memcached as the main storage usually a bad idea?
- What security limitations does Memcached have?
- Does Memcached have built-in authentication and authorization?
- Why should Memcached not simply be exposed to a public network?
- What well-known attack types have been associated with exposed Memcached instances?
- What is a Memcached amplification attack?
- How do you protect Memcached at the network and infrastructure level?
- Which ports does Memcached use?
- How is the Memcached text protocol different from the binary protocol?
- What are the pros and cons of the text protocol?
- What is CAS in Memcached and how can it be used to combat race conditions?
- Can you implement a distributed lock with Memcached?
- Why is a lock on Memcached usually less reliable than on Redis or specialized systems?
- How do you implement atomic counters in Memcached?
- In which scenarios are incr/decr useful?
- What limitations do incr/decr have?
- Can you decrement a value below zero?
- How does Memcached handle multithreading?
- What does it mean that Memcached is a multithreaded server?
- Which Memcached configuration parameters are the most important?
- How do you choose the amount of memory for Memcached?
- How do you choose the number of Memcached threads?
- When does it make sense to run multiple Memcached instances on one machine?
- How do NUMA and hardware configuration affect Memcached?
- What kind of workload is a good fit for Memcached, and what kind is not?
- In which cases is Redis clearly preferable to Memcached?
- In which cases is a CDN or a local in-process cache more useful than Memcached?
- How do you combine Memcached with a local application cache?
- What problems arise with multi-level caching?
- What matters most for Memcached: latency, bandwidth, or CPU?
- How do you diagnose a situation where Memcached suddenly stops being effective?
- How would you design caching for product cards using Memcached?
- How would you cache an expensive SQL JOIN using Memcached?
- How would you use Memcached to protect a database during a traffic spike?
- How would you explain to a junior developer when Memcached is a good idea and when it is not?
- How would you migrate part of the load from Memcached to Redis or vice versa?
- What questions would you ask the team before introducing Memcached into a critical service?
- What trade-offs would you discuss when choosing between Memcached, Redis, and an application-side cache?
- How would you investigate a situation where Memcached hit rate suddenly dropped after a release?
- How would you look for memory waste in Memcached?
- How would you explain to the business why increasing RAM for Memcached does not always solve performance problems?
- What Memcached usage anti-patterns have you seen?
- Which is worse: using Memcached without a clear invalidation strategy or not using a cache at all? Why?
- How would you design keys and TTLs for a system where the cost of reading stale data is high?
- What Memcached question would you ask a candidate for a senior backend role, and why?

### 7.12 Elasticsearch / OpenSearch
- What is Elasticsearch?
- What is OpenSearch?
- Why did OpenSearch emerge as a separate project?
- What are the high-level similarities and differences between Elasticsearch and OpenSearch?
- What tasks are Elasticsearch / OpenSearch typically used for?
- How is a search engine different from a relational database?
- Why should Elasticsearch not be used as a replacement for PostgreSQL or MySQL for all tasks?
- In which scenarios is Elasticsearch actually better than an SQL database?
- When is Elasticsearch overkill?
- What is full-text search?
- How is full-text search different from searching with LIKE in SQL?
- What is an inverted index?
- How does an inverted index speed up text search?
- How is a document structured in Elasticsearch?
- What is an index in Elasticsearch?
- What is a shard?
- What is a replica shard?
- What are primary shards and replica shards for?
- How is a shard in Elasticsearch different from a shard in a relational database?
- What is a node in Elasticsearch?
- Which node roles do you know?
- What is a cluster in Elasticsearch?
- How does the cluster distribute shards across nodes?
- What is cluster state?
- Why is cluster state important for cluster operation?
- What is a master-eligible node?
- What is a coordinating node?
- What is a data node?
- What is an ingest node?
- What is mapping?
- Why is mapping needed?
- What happens if you let Elasticsearch dynamically create mappings for all fields?
- What is dynamic mapping?
- What risks does dynamic mapping create?
- What is mapping explosion?
- Why is having too many fields in an index a problem?
- Which field types are available in Elasticsearch?
- What is the difference between text and keyword?
- When should you use text and when should you use keyword?
- Why should a text field not be used thoughtlessly for aggregations and sorting?
- What are numeric, date, boolean, geo_point, nested, and object fields?
- What is the difference between object and nested?
- Why does nested often cause confusion?
- In which cases do you need nested mapping?
- What are analyzers?
- Why are analyzers needed?
- What are the parts of an analyzer?
- What is a tokenizer?
- What are token filters?
- What are character filters?
- What is the difference between an analyzer and a normalizer?
- What is the standard analyzer?
- What is a custom analyzer?
- Why does text analysis affect search results?
- What happens if you index text with one analyzer and search with another?
- What is stemming?
- What are stop words?
- What are synonyms in search?
- What problems can arise when using synonyms?
- What is relevance in full-text search?
- How does Elasticsearch calculate relevance?
- What is BM25?
- How is BM25 better or worse than TF-IDF?
- What is scoring?
- How does a bool query work?
- What is the difference between must, should, filter, and must_not?
- Why is filter usually faster than must?
- What is a term query?
- How is a term query different from a match query?
- When should you use term, and when should you use match?
- Why does a term query on a text field often not produce the expected result?
- What is match_phrase?
- What is a wildcard query?
- Why can wildcard queries be dangerous?
- What is a prefix query?
- What is fuzzy search?
- How does fuzzy search work?
- What are the trade-offs of fuzzy search?
- What is autocomplete in Elasticsearch?
- How can autocomplete be implemented?
- How is edge n-gram different from completion suggester?
- When is it better to use completion suggester, and when n-grams?
- What is a multi_match query?
- How do you search across multiple fields at once?
- What is boosting?
- How do you increase the weight of one field relative to another?
- What is a function_score query?
- What is rescore?
- How does sorting affect relevance?
- How is search different from filtering?
- What are aggregations?
- What are aggregations used for?
- Which types of aggregations do you know?
- What is a terms aggregation?
- What is a date histogram?
- What is a range aggregation?
- What are metric aggregations?
- What are pipeline aggregations?
- Why can aggregations be expensive?
- What is a cardinality aggregation?
- Why is approximate cardinality both useful and dangerous?
- What are doc values?
- Why are doc values needed?
- Why do sorting and aggregations often rely on doc values?
- What is fielddata and why can it be dangerous?
- Why is enabling fielddata on a text field usually a bad idea?
- What is _source?
- Why is _source needed?
- When can disabling _source be justified, and when not?
- What is refresh in Elasticsearch?
- How is refresh different from flush?
- How is refresh different from commit in relational databases?
- Why are documents not always immediately visible after indexing?
- What is near real-time search?
- What is translog?
- How is translog related to write durability?
- What is a segment?
- Why does the number of segments affect search performance?
- What is segment merging?
- Why is merge an expensive operation?
- What is force merge and when can it be used?
- Why is force merge dangerous on an actively used index?
- What is bulk indexing?
- Why is bulk ingestion usually more efficient than single inserts?
- How do you choose a bulk batch size?
- What risks does an overly large bulk batch create?
- What is backpressure during indexing?
- How does Elasticsearch behave under heavy write load?
- What is refresh interval and how does it affect ingest throughput?
- How do you speed up bulk loading into Elasticsearch?
- What is an ingest pipeline?
- What are ingest pipelines used for?
- Which processors in an ingest pipeline do you know?
- When is an ingest pipeline more convenient than transforming data in the application?
- What is an index template?
- Why are index templates needed?
- What are component templates?
- What are aliases?
- How do you use aliases for zero-downtime reindexing?
- What is reindex?
- When is reindex needed?
- Why does changing mapping often require reindexing?
- How do you safely reindex a large index?
- How do you minimize downtime during reindexing?
- What is a rollover index?
- What are data streams?
- When are data streams useful?
- What is ILM / Index Lifecycle Management?
- Why is ILM needed?
- Which stages of the index lifecycle do you know?
- What are hot, warm, cold, and frozen tiers?
- When is a tiered storage strategy useful?
- What is a snapshot in Elasticsearch / OpenSearch?
- What are snapshots used for?
- Is a snapshot a full backup mechanism?
- How are snapshots stored?
- What limitations do snapshots have?
- How do you restore an index from a snapshot?
- What is a snapshot repository?
- Which types of snapshot repositories exist?
- How do you verify that a snapshot is actually restorable?
- What is cluster health?
- What is the difference between green, yellow, and red cluster states?
- What does yellow cluster health mean?
- What does red cluster health mean?
- What should you do if the cluster is red?
- What are common causes of unassigned shards?
- How do you investigate the cause of unassigned shards?
- What is shard allocation?
- How do disk watermarks affect shard allocation?
- What is a hot shard?
- Why is shard hotspotting a problem?
- How do you choose the number of shards for an index?
- Why are too many shards bad?
- Why are too few shards also bad?
- How does shard size affect recovery and performance?
- What is oversharding?
- How do you tell that you have oversharding?
- What are split and shrink index?
- When do you need split, and when shrink?
- How do you choose an indexing strategy for time-series data?
- How do you organize logs in Elasticsearch / OpenSearch?
- Why is Elasticsearch often used for logs, tracing, and observability data?
- How is a search index for logs different from an index for a product catalog?
- What are the specifics of log indexing?
- How do you build search over logs?
- What are structured logs and why are they useful for Elasticsearch?
- How does high field cardinality affect storage and search?
- What are high-cardinality fields?
- Why are high-cardinality aggregations expensive?
- What is a circuit breaker in Elasticsearch?
- Which circuit breakers do you know?
- What happens when a circuit breaker is triggered?
- How does Elasticsearch use heap?
- Why is heap tuning important?
- Why is “the more heap, the better” not always true?
- What is the role of the filesystem page cache in Elasticsearch?
- Why does Elasticsearch like fast SSDs?
- What usually becomes the bottleneck: CPU, RAM, disk, or network?
- How do you monitor Elasticsearch / OpenSearch?
- Which key cluster metrics do you track?
- Which metrics are important for search?
- Which metrics are important for indexing?
- Which metrics are important for the JVM?
- Which metrics are important for shard allocation and cluster state?
- How do you find slow queries in Elasticsearch?
- What are slow logs?
- How do you analyze search latency?
- How do you analyze indexing latency?
- What is query cache?
- What is request cache?
- When does caching help, and when does it not?
- Why can full-text queries cache poorly?
- What common index design mistakes have you seen?
- Why should you not blindly index “all fields as text”?
- Why should you not blindly enable aggregations on any field?
- What Elasticsearch anti-patterns have you seen?
- Can Elasticsearch be used as the source of truth?
- Why is Elasticsearch usually used as a secondary store rather than a system of record?
- How do you synchronize data between PostgreSQL/MySQL and Elasticsearch?
- What problems arise with dual-write to a database and Elasticsearch?
- What is the outbox pattern in the context of populating a search index?
- How do you organize reliable indexing of changes from a database into Elasticsearch?
- How do you deal with drift between the primary database and the search index?
- What is eventual consistency between a database and Elasticsearch?
- How would you design search for a product catalog in e-commerce?
- How would you design faceted search?
- How would you implement filters, sorting, and full-text search together?
- How would you implement typo-tolerant search?
- How would you implement search in both Russian and English at the same time?
- How would you implement synonyms for product search?
- How would you organize relevance for a catalog of jobs / documents / logs?
- How would you debug a situation where “search returns something different from what the business expects”?
- How would you debug a situation where the index suddenly grew a lot in size?
- How would you debug a situation where indexing became very slow after a mapping change?
- How would you debug a situation where the cluster has been yellow for several hours?
- How would you debug a situation where search latency increased after adding several aggregations?
- How would you explain to a junior developer the difference between term, match, and filter?
- What Elasticsearch / OpenSearch question would you ask a candidate for a senior backend role, and why?

### 7.13 ORM and object-relational mismatch

#### 7.13.1 JPA / Hibernate
- What is ORM and why is it needed?
- What problems does ORM solve in application development?
- What problems does ORM not solve and sometimes even make worse?
- What is object-relational mismatch?
- Why is there a mismatch between the object model and the relational model?
- Which typical manifestations of object-relational mismatch do you know?
- Why does a `one-to-many` relationship look different in code and in a database?
- Why does inheritance in OOP map poorly to the relational model?
- What trade-offs do you usually have to make when using ORM?
- When is ORM useful, and when is it better to write SQL manually?
- In which scenarios is ORM especially convenient?
- In which scenarios is ORM especially dangerous?
- What alternatives to ORM do you know?
- How does Data Mapper differ from Active Record?
- Which approach is JPA/Hibernate closer to?
- What is JPA?
- What is Hibernate?
- What is the difference between JPA and Hibernate?
- Can Hibernate be used without JPA?
- Can JPA be used without Hibernate?
- What is an entity in JPA?
- What requirements are imposed on an entity class?
- Why does an entity need an identifier?
- What is `@Entity`?
- What is `@Table`?
- When do you need to specify `@Table` explicitly, and when not?
- What is `@Id`?
- Which ID generation strategies exist in JPA?
- What is the difference between `IDENTITY`, `SEQUENCE`, `TABLE`, and `AUTO`?
- What problems can `GenerationType.IDENTITY` cause?
- Why can `IDENTITY` hurt batch inserts?
- When is it better to use a sequence?
- What are the pros and cons of sequence-based IDs?
- What are hi/lo and pooled sequence strategies?
- What are natural keys and surrogate keys, and how does this affect ORM?
- Why is primary key choice important for Hibernate?
- What is a persistence context?
- What is a first-level cache?
- What exactly is stored in the persistence context?
- What are the managed, detached, transient, and removed states of an entity?
- How does an entity move between those states?
- What does `persist()` do?
- What does `merge()` do?
- What is the difference between `persist()` and `merge()`?
- What does `remove()` do?
- What does `detach()` do?
- What does `flush()` do?
- What is the difference between `flush()` and `commit()`?
- When does Hibernate flush automatically?
- What is dirty checking?
- How does Hibernate know that an entity has changed?
- What are the costs of dirty checking?
- When can dirty checking lead to unexpected UPDATEs?
- What is write-behind in Hibernate?
- Why can an SQL query be executed later rather than at the moment an object is changed?
- What is a transactional write-behind cache?
- What is unit of work in the context of ORM?
- How does the entity lifecycle work in JPA?
- Which lifecycle callbacks exist in JPA?
- What are `@PrePersist`, `@PostPersist`, `@PreUpdate`, `@PostUpdate`, `@PreRemove`, and `@PostLoad` used for?
- When are callbacks convenient, and when do they become a source of hidden logic?
- What are embeddable and embedded objects?
- When should you use `@Embeddable`, and when a separate entity?
- What is a value object and how do you map it in JPA?
- How do you model `one-to-one` in JPA?
- How do you model `one-to-many` in JPA?
- How do you model `many-to-one` in JPA?
- How do you model `many-to-many` in JPA?
- Why is it often not recommended to use `many-to-many` directly?
- When is it better to replace `many-to-many` with a separate join entity?
- What are the owning side and inverse side of a relation?
- What is `mappedBy`?
- Why is it important to understand the owning side of a relationship?
- What happens if you update only the inverse side?
- How do you properly maintain bidirectional relationships in code?
- Why is it convenient to create helper methods for bidirectional associations?
- What is orphan removal?
- How is `orphanRemoval = true` different from `CascadeType.REMOVE`?
- What is cascade in JPA?
- Which cascade types exist?
- When is cascade useful, and when is it dangerous?
- What problems can arise from overly aggressive cascade usage?
- Why can cascade on large object graphs be risky?
- What is fetch type?
- What is the difference between `EAGER` and `LAZY`?
- Why is `EAGER` almost always a bad default?
- Why does `LAZY` not solve all problems automatically?
- What is a proxy in Hibernate?
- How do lazy proxies work?
- When is Hibernate unable to create a proxy?
- What is `LazyInitializationException`?
- Why does `LazyInitializationException` occur?
- How do you properly solve `LazyInitializationException`?
- Why is Open Session in View considered a controversial practice?
- What alternatives to Open Session in View exist?
- What is the N+1 problem?
- How does N+1 arise in Hibernate?
- Why do neither `LAZY` nor `EAGER` by themselves save you from N+1?
- How do you detect an N+1 problem?
- How do you solve an N+1 problem?
- What is a fetch join?
- What is the difference between a regular `join` and a `fetch join` in JPQL?
- When does a fetch join help, and when can it hurt?
- Why can’t you blindly do fetch joins on all relations?
- What is the cartesian product problem with fetch joins?
- What is `EntityGraph`?
- When should you use `EntityGraph` instead of a fetch join?
- What is batch fetching in Hibernate?
- How does `@BatchSize` work?
- When is batch fetching useful?
- What is subselect fetching?
- When does the subselect fetch strategy help?
- What is a second-level cache?
- How is the second-level cache different from the first-level cache?
- When is a second-level cache useful?
- When is a second-level cache harmful?
- What is query cache?
- Why is query cache more complex and dangerous than entity caching?
- What risks does caching in ORM introduce?
- How are cache and data consistency related?
- What is optimistic locking in JPA?
- How does `@Version` work?
- What is an entity version?
- What problems does optimistic locking solve?
- What happens if two transactions simultaneously modify the same entity with `@Version`?
- What is `OptimisticLockException`?
- When is optimistic locking better than pessimistic locking?
- What is pessimistic locking in JPA?
- Which pessimistic lock modes exist?
- What are `PESSIMISTIC_READ`, `PESSIMISTIC_WRITE`, and `PESSIMISTIC_FORCE_INCREMENT`?
- When should you use pessimistic locks?
- What are the risks of pessimistic locks?
- What is versionless optimistic locking?
- Why is optimistic locking especially useful in web applications?
- What is a transaction boundary in JPA/Hibernate?
- Why is the transaction boundary critical for ORM?
- What is `@Transactional` and how is it related to the persistence context?
- What happens if you load an entity inside a transaction and use it outside the transaction?
- What is a read-only transaction?
- Does `readOnly = true` actually help Hibernate run faster?
- What optimizations are possible for read-only scenarios?
- What is JPQL?
- How is JPQL different from SQL?
- What is HQL?
- What is the difference between HQL and JPQL?
- When is it better to use JPQL, and when native SQL?
- What is the Criteria API?
- What are the pros and cons of the Criteria API?
- What are type-safe query builders, and what alternatives to the Criteria API do you know?
- What is a native query in JPA?
- When is using a native query justified?
- What risks do native queries have?
- What is a projection in JPA?
- When should you return DTOs instead of entities?
- Why is it not always a good idea to expose entities outside the service layer?
- What is DTO projection?
- What is the difference between interface-based projection, class-based projection, and entity loading?
- Why is DTO projection often faster than entity loading?
- What is a constructor expression in JPQL?
- What is pagination in JPA?
- What problems can occur with pagination and join fetch?
- Why can offset pagination be expensive?
- How do you implement keyset pagination with ORM?
- What ORM limitations arise with keyset pagination?
- What are bulk update and bulk delete in JPQL?
- How is bulk update different from modifying entities through the persistence context?
- Why can bulk update leave the persistence context in an inconsistent state?
- What should you do after bulk update / bulk delete?
- What does `clear()` on EntityManager do and when is it needed?
- What is flush mode?
- Which flush modes exist?
- How is `FlushModeType.AUTO` different from `FlushModeType.COMMIT`?
- When can changing flush mode be useful?
- What are batch insert and batch update in Hibernate?
- How do you enable batching in Hibernate?
- What conditions prevent batching from working?
- Why can `IDENTITY` break batching?
- How do you properly save large volumes of data through Hibernate?
- Why do you need to periodically flush/clear during massive data loads?
- What is memory leak-like behavior during large batch inserts with ORM?
- What problems arise when loading an overly large entity graph into memory?
- How does Hibernate work with equals and hashCode?
- Why are equals/hashCode for entities a tricky topic?
- Should ID be included in equals/hashCode?
- What problems arise if equals/hashCode are implemented incorrectly?
- Why do proxy objects complicate equals/hashCode?
- What is inheritance mapping in JPA?
- Which inheritance mapping strategies exist?
- What is the difference between `SINGLE_TABLE`, `JOINED`, and `TABLE_PER_CLASS`?
- What are the pros and cons of `SINGLE_TABLE`?
- What are the pros and cons of `JOINED`?
- Why is `TABLE_PER_CLASS` rarely used?
- What is a discriminator column?
- When is inheritance mapping justified, and when is composition better?
- How do you map enums in JPA?
- Why is `EnumType.STRING` usually better than `EnumType.ORDINAL`?
- What risks does `ORDINAL` have?
- How do you map JSON/JSONB fields through Hibernate?
- When is JSON in a relational database a good idea, and when is it not?
- How do you map custom types in Hibernate?
- What is `AttributeConverter`?
- When should you use `AttributeConverter`, and when a custom Hibernate type?
- What is soft delete and how is it implemented in Hibernate?
- What pitfalls does soft delete have?
- How does soft delete affect unique constraints?
- How does soft delete affect indexes and performance?
- What is multitenancy in Hibernate?
- Which multitenancy strategies do you know?
- What is the difference between database-per-tenant, schema-per-tenant, and discriminator-based multitenancy?
- When does ORM help with multitenancy, and when does it get in the way?
- What is auditing in JPA/Hibernate?
- How do you implement created_at, updated_at, created_by, updated_by?
- What is Envers?
- When is versioning historical changes of entities useful?
- What limitations does ORM have for complex reporting?
- Why is ORM inconvenient for analytical queries?
- When is it better to switch to SQL, jOOQ, or a separate read model?
- What is the impedance mismatch between a DDD model and ORM?
- Why can a rich domain model be a poor fit for Hibernate?
- How do you avoid leaking ORM details into the domain model?
- What anti-patterns do you know when working with Hibernate?
- Why is an “anemic” model sometimes easier to live with in ORM?
- What is the repository pattern and how is it related to ORM?
- Should a repository return entities or DTOs?
- Where should business logic live: in the entity, service, or repository?
- What are transactional script and domain model, and how does ORM influence the choice?
- How do you decide where ORM stops being useful and starts becoming harmful?
- In which cases would you отказаться from Hibernate in a project?
- What typical production problems have you seen because of Hibernate?
- How do you debug slow queries generated by ORM?
- How do you enable SQL logging in Hibernate?
- Why is it useful to log not only SQL but also bind parameters?
- How do you tell that ORM is making extra queries?
- Which Hibernate metrics would you track in production?
- How do you assess ORM impact on application throughput and latency?
- What would you choose for a new project: plain JPA/Hibernate, Hibernate + native SQL, jOOQ, or JDBC? Why?
- When is JPA/Hibernate a good choice for a CRUD system?
- When is JPA/Hibernate a bad choice for high-load or data-heavy systems?
- What questions would you ask the team to understand whether Hibernate is suitable for their scenario?
- What rules for using Hibernate would you introduce in a team to avoid typical mistakes?
- What would you check in code review in a project using JPA/Hibernate?
- How would you explain to a junior developer that ORM is not “a database that magically solves everything”?
- How would you explain to a manager that ORM speeds up development but does not eliminate the need to understand SQL and databases?
- When using Hibernate, what matters more: knowing the Hibernate API or understanding the SQL that actually gets executed? Why?

### 7.14 Schema migrations and database operations
- What is a database schema migration?
- Why are schema migrations needed?
- Why is it dangerous to change a database schema manually without migrations?
- What tasks do migrations usually solve besides creating tables?
- What is schema evolution?
- How is a schema migration different from a data migration?
- What types of migrations do you know?
- What is a DDL migration?
- What is a data migration?
- How is a structural migration different from a backfill migration?
- Why should migrations be stored in version control?
- Why does a team need a single migration history?
- Which migration tools have you used?
- What is Flyway?
- What is Liquibase?
- What is the difference between Flyway and Liquibase?
- When is Flyway more convenient, and when Liquibase?
- What is a versioned migration?
- What is a repeatable migration?
- What is a migration checksum and why is it needed?
- What happens if you modify a migration that has already been applied?
- Why should you not edit old migrations after they have been applied in production?
- What should you do if you discover an error in an old migration?
- How should migrations be named properly?
- How should migration files be organized in a project?
- What is a baseline in migrations?
- When is a baseline needed?
- How do you integrate migrations into CI/CD?
- When should migrations run: before deployment, during deployment, or after deployment?
- Who should run migrations: the application, a separate job, a DBA, or the pipeline?
- What risks arise when migrations are run automatically at application startup?
- What risks arise when migrations are run manually?
- How do you ensure migrations are idempotent?
- Should migrations be re-runnable?
- What do you do if a migration partially succeeded and then failed?
- How do you roll back a failed migration?
- Is rollback always realistic for migrations?
- Why is rolling back a schema often more difficult than it seems?
- In which cases is rollback impossible without data loss?
- How is rollback different from a roll-forward strategy?
- Why is roll-forward often preferred over rollback in production?
- What is a backward-compatible migration?
- What is a forward-compatible migration?
- Why is schema compatibility critical for zero-downtime deployment?
- What is the expand-and-contract pattern?
- How does the expand → migrate → contract approach work?
- Why is dropping a column in production rarely done in one step?
- How do you safely rename a column without downtime?
- How do you safely change a column type in a large table?
- How do you safely split one column into two?
- How do you safely merge several columns into one?
- How do you safely move a field into a new table?
- How do you safely add a NOT NULL column to a large table?
- Why can `ALTER TABLE ADD COLUMN ... NOT NULL` be dangerous?
- How do you add a new column with a default value to a large table without a long lock?
- What is backfill?
- How do you safely backfill large volumes of data?
- Why is backfill often done in batches?
- What risks does backfill create in production?
- How do you limit load during a data migration?
- How do you monitor a long-running backfill?
- What do you do if the backfill competes with user traffic?
- How do you safely drop a column?
- How do you tell that a column or table is no longer used?
- Why is it useful to introduce a “write off / read off” stage before dropping a field?
- How do you safely drop an index?
- How do you safely add an index on a large table?
- Why is creating an index in production dangerous if you do not understand its locking behavior?
- What are the specifics of online index creation in different databases?
- Why can `ALTER TABLE` be dangerous on large tables?
- Which DDL operations can block writes?
- Which DDL operations can block reads?
- How do you assess the impact of a migration before running it in production?
- Why do migrations need to be tested on a copy of the data or a staging environment?
- How do you validate migrations against real data volumes?
- Which metrics would you watch during a migration?
- What should be logged when running migrations?
- How do you detect that a migration is degrading database performance?
- What do you do if a migration hangs?
- What do you do if a migration holds locks and breaks production?
- What do you do if a migration runs longer than the release window?
- What is a migration window?
- When should migrations only be run during a low-traffic window?
- What is schema drift?
- How do you detect that production schema has diverged from the schema in the repository?
- How do you prevent schema drift?
- What is drift detection in migration tools?
- How is migration in a monolith different from migration in microservices?
- Who owns the database schema in a microservice architecture?
- Why does a shared database between services make migrations harder?
- How do you manage migrations when several services use the same database?
- How do you coordinate migrations across multiple teams?
- What is a contract-first approach to data migrations between services?
- How do you migrate a schema if old and new code must work at the same time?
- How do you deploy multiple versions of an application against the same schema?
- What matters most in a migration: atomicity, speed, compatibility, or simplicity? Why?
- How do you use feature flags in schema migrations?
- How are feature flags and staged rollout of migrations related?
- Should data migrations be stored separately from schema migrations?
- How do you document complex migrations?
- What should be included in a runbook for a risky migration?
- Which pre-check / during-check / post-check steps would you describe for a migration?
- How do you perform a dry run of a migration?
- What is a canary migration?
- Can migrations be rolled out gradually?
- How does dual-write work in schema migration?
- What are the risks of dual-write?
- How do you verify consistency after dual-write?
- What is a shadow table migration?
- When are temporary tables, shadow tables, or new versions of tables used?
- How do you migrate a very large table without full locking?
- Which migration strategies are used for tables with hundreds of millions of rows?
- When is it worth copying into a new table instead of altering the existing one?
- How do you migrate a partitioned table?
- What additional complexities arise with migrations in a sharded database?
- How do you migrate a schema across all shards at once?
- How do you verify migration success on each shard?
- What do you do if a migration succeeded on some shards but failed on others?
- How do migrations affect replication?
- Can a heavy migration cause replica lag?
- How do you account for replication lag during migration rollout?
- What is a data correction migration?
- How do you safely fix corrupted data in production?
- How do you separate business logic from one-off data fix migrations?
- Should one-off hotfix scripts be committed to the repository?
- When is it better to use a separate admin job instead of a migration?
- What migration anti-patterns do you know?
- Why is “one migration that does everything at once” a bad idea?
- Why is it dangerous to mix a heavy backfill and DDL in the same migration?
- Why is it dangerous to put complex application logic into migration scripts?
- What problems do migrations create when they depend on the current application state?
- How are migrations related to backward compatibility of the API and the application?
- Why should you first deploy code that is compatible with the new schema, and only then remove the old one?
- How do migrations affect ORM and generated schema tools?
- Why is it dangerous to rely on auto-ddl (`hibernate ddl-auto`, auto schema sync) in production?
- When can automatic schema generation be acceptable?
- What is your view on the idea that “the ORM will create the tables for us”?
- What do you do if developers often forget to write migrations?
- What migration rules would you introduce in a team?
- What would you check in migration code review?
- What questions would you ask before a production migration of a critical table?
- How would you explain to a junior developer that a migration is not just an SQL file, but part of a system’s operational reliability?
- How would you explain to a manager why “simply adding a column” can sometimes require several releases?
- What does database operations mean in the broad sense?
- Which daily operational database tasks do you know?
- What is included in regular database maintenance?
- Which key database metrics should be monitored?
- Which metrics are important for performance?
- Which metrics are important for reliability?
- Which metrics are important for capacity planning?
- What would you monitor first in MySQL/PostgreSQL in production?
- What would you monitor on replicas?
- How do you track database growth?
- How do you predict running out of disk?
- How do you track index growth and bloat?
- How do you track slow queries?
- What is a slow query log?
- How do you analyze a slow query log?
- What is query digest and why is it needed?
- How do you find the most expensive queries in production?
- How do you prioritize query optimization?
- How do you organize rotation of database logs?
- Should the number of database connections be monitored?
- What is a connection pool and what problems does it solve?
- What problems arise from incorrect connection pool configuration?
- What is a connection storm?
- What is database saturation?
- How do you tell that the database has become the bottleneck of the system?
- How do you tell that the problem is in the queries rather than the hardware?
- How do you tell that the problem is in locks rather than CPU?
- How do you tell that the problem is in disk rather than SQL?
- What are maintenance operations in a DBMS?
- Which regular maintenance tasks does a database need?
- What is reindex and when is it needed?
- When is analyze needed?
- When is vacuum / optimize / purge needed, depending on the DBMS?
- How do you maintain indexes and statistics?
- What is statistics freshness and why does it matter for the optimizer?
- How do stale statistics ruin query plans?
- How does database operations change as traffic grows?
- What signs indicate that it is time to scale the database?
- Which options for database scaling do you know?
- When does vertical scaling help?
- When are replicas needed?
- When do you already need sharding or partitioning?
- What operational risks appear with replicas?
- What operational risks appear with sharding?
- What is read-after-write consistency and how does it affect operating replicas?
- Why can an application read “old” data from a replica?
- How do you build alerts for critical database conditions?
- Which alerts are noise, and which are actually useful?
- How do you organize on-call for database issues?
- What should be in a runbook for the database on-call engineer?
- Which common database incidents do you know?
- How would you react to a sudden increase in database latency?
- How would you react to a rise in lock waits?
- How would you react to exhausting the connection limit?
- How would you react to a replica failure?
- How would you react to disk filling up?
- How would you react to corruption or suspected corruption?
- How would you react to human error and accidental data deletion?
- Which operational tasks are often underestimated by development teams?
- What boundaries of responsibility between developers, DBA, SRE, and platform teams do you consider reasonable?
- Which things should a developer understand about database operations even if the company has a DBA?

### 7.15 Data stores and OLAP

#### 7.15.1 ClickHouse
- What is ClickHouse and what tasks is it used for?
- How is ClickHouse different from classic OLTP databases?
- Why is ClickHouse classified as a columnar database?
- In which scenarios is ClickHouse a good fit, and in which is it not?
- How is ClickHouse different from PostgreSQL or MySQL?
- How is an OLAP workload different from an OLTP workload?
- What are typical use cases for ClickHouse?
- For which types of analytical queries is ClickHouse especially effective?
- Why is ClickHouse often used for logs, metrics, events, and product analytics?
- What limitations does ClickHouse have as the main operational database of an application?
- What is columnar storage and why is it effective for analytics?
- How is columnar storage different from row-based storage?
- Why is columnar storage well suited for aggregations?
- Why is reading a small number of columns in ClickHouse usually fast?
- What are the drawbacks of columnar storage?
- How does ClickHouse store data on disk?
- What is a part in ClickHouse?
- What is a granule in ClickHouse?
- What is a mark in ClickHouse?
- How does the sparse index work in ClickHouse?
- Why is the primary key in ClickHouse not the same as the primary key in an OLTP DBMS?
- What is ORDER BY in a ClickHouse table definition and why is it so important?
- How is ORDER BY in ClickHouse DDL different from ORDER BY in an SQL query?
- What is a partition key in ClickHouse?
- When should you use PARTITION BY?
- What problems arise from overly fine-grained partitioning?
- What problems arise from overly coarse partitioning?
- How do you choose a good sort key for a MergeTree table?
- How do you choose a good partition key?
- What is an engine in ClickHouse?
- Which families of table engines do you know in ClickHouse?
- What is MergeTree?
- Why is MergeTree the basic and most important engine in ClickHouse?
- Which variants of MergeTree engines exist?
- What is ReplacingMergeTree?
- What is SummingMergeTree used for?
- What is AggregatingMergeTree used for?
- What is CollapsingMergeTree?
- In which scenarios is VersionedCollapsingMergeTree used?
- When would you choose regular MergeTree, and when a specialized engine?
- What is the Distributed engine?
- What is ReplicatedMergeTree used for?
- How is the Distributed engine different from shard-local tables?
- What is the MergeTree merge process?
- Why is data in ClickHouse “finished” by background merge operations?
- What is a mutation in ClickHouse?
- Why are UPDATE and DELETE considered expensive operations in ClickHouse?
- Why does ClickHouse not like frequent point updates?
- What happens under the hood during ALTER TABLE ... DELETE WHERE?
- How is a lightweight delete different from a mutation delete?
- When can DELETE be unexpectedly expensive in ClickHouse?
- What happens under the hood during UPDATE in ClickHouse?
- Why is ClickHouse better suited for append-heavy workloads?
- How do you organize “updates” of data in ClickHouse if rows change?
- How do you implement upsert-like behavior in ClickHouse?
- How does ReplacingMergeTree help with deduplication?
- Why is deduplication in ReplacingMergeTree not immediate?
- What is FINAL in ClickHouse?
- Why can SELECT ... FINAL be expensive?
- When is FINAL justified, and when is it better to avoid it?
- What is the primary index in ClickHouse?
- How does a skip index work?
- Which types of data skipping indexes exist?
- When is a bloom filter index useful in ClickHouse?
- When is a minmax index useful, and when is it useless?
- What are projections in ClickHouse?
- How are projections different from materialized views?
- When do projections actually help?
- What is a materialized view in ClickHouse?
- How is a materialized view used for pre-aggregation?
- What pitfalls do materialized views in ClickHouse have?
- How is a regular view different from a materialized view?
- What is TTL in ClickHouse?
- How can TTL be used for automatic deletion of old data?
- How does TTL help with log and metric retention?
- Can TTL be used to move data between storage tiers?
- What are cold storage and hot storage in the context of ClickHouse?
- How is data compression implemented in ClickHouse?
- Why does ClickHouse often show very good compression ratios?
- Which codecs does ClickHouse support?
- When does it make sense to tune codecs manually?
- How do data types affect storage size and performance?
- Why is it important to choose the smallest suitable data type?
- When should you use LowCardinality?
- What are the pros and cons of LowCardinality(String)?
- What is Nullable in ClickHouse and why is it sometimes avoided?
- Why can excessive use of Nullable hurt performance?
- When is Decimal better than Float in ClickHouse?
- What are the specifics of working with Date, DateTime, and DateTime64?
- What is the difference between count(), count(column), and countDistinct()?
- Why can countDistinct be expensive?
- Which approximate aggregate functions exist in ClickHouse?
- When should you use uniq, uniqExact, uniqCombined, and uniqHLL12?
- What are aggregate state functions?
- How do -State and -Merge functions work?
- What are AggregatingMergeTree and aggregate states used for?
- How is the query execution pipeline structured in ClickHouse?
- How does ClickHouse parallelize query execution?
- Why does ClickHouse scale well for reads?
- How does the number of parts in a table affect performance?
- Why is “too many parts” a bad sign?
- What causes too many parts to appear?
- How do you deal with the too many parts problem?
- Why are very frequent small inserts harmful for ClickHouse?
- How do batch inserts affect performance?
- What batch size is usually reasonable for inserting into ClickHouse?
- Why is inserting one row at a time a bad idea?
- Which insert formats does ClickHouse support?
- When should you use Native format, and when JSONEachRow/CSV/Parquet?
- What are the ways to load data into ClickHouse?
- How do you organize ingestion from Kafka into ClickHouse?
- What is the Kafka engine in ClickHouse?
- What risks arise when ingesting through the Kafka engine?
- How do you ensure idempotency when loading events into ClickHouse?
- How do you deal with duplicates in ClickHouse?
- How do you organize near-real-time analytics on ClickHouse?
- What is the difference between a shard and a replica in ClickHouse?
- What is sharding in ClickHouse?
- What is replication in ClickHouse?
- How does ReplicatedMergeTree use ZooKeeper / ClickHouse Keeper?
- What is ClickHouse Keeper?
- How is ClickHouse Keeper different from ZooKeeper?
- What components are needed for a fault-tolerant ClickHouse cluster?
- How does Distributed query execution work?
- When is a query executed on each shard, and when centrally?
- What is distributed_product_mode?
- What problems can occur with JOINs in a distributed ClickHouse cluster?
- Why can JOIN be a heavy operation in ClickHouse?
- Which types of JOINs does ClickHouse support?
- How are JOINs in ClickHouse different from JOINs in OLTP databases?
- When is it better to denormalize data instead of using JOINs?
- Why is denormalization often justified in analytics?
- What are the trade-offs of denormalization in ClickHouse?
- What is a dictionary in ClickHouse?
- When are dictionaries more convenient than regular JOINs?
- Which types of dictionaries exist?
- How do dictionaries help speed up lookup operations?
- When can a dictionary become a bottleneck?
- How does ClickHouse work with Parquet, ORC, and other analytical formats?
- Can ClickHouse be used as a query engine over files in S3?
- What table functions exist in ClickHouse?
- What is the S3 table function?
- What are external tables and file engines in ClickHouse?
- When does it make sense to store data in object storage rather than locally?
- How does ClickHouse integrate with BI tools?
- What problems arise when connecting BI systems to ClickHouse?
- How do you design a table schema in ClickHouse for product analytics?
- How do you design a schema for logs?
- How do you design a schema for time-series metrics?
- How do you model an event stream in ClickHouse?
- What is a wide table in analytics and when is it justified?
- When is one wide table better, and when several related tables?
- How do you organize storage for events with dynamic attributes?
- When is a JSON column in ClickHouse justified, and when not?
- What problems arise from excessive use of JSON fields?
- What is sampling in ClickHouse?
- How does SAMPLE help speed up analytics?
- When is a sampled result acceptable, and when is it not?
- What is EXPLAIN in ClickHouse and what does it show?
- What query profiling tools are available in ClickHouse?
- How do you find slow queries in ClickHouse?
- What does system.query_log show?
- Which system tables are especially useful for diagnosing ClickHouse?
- How do you analyze load through system.parts?
- How do you analyze merges and mutations?
- How do you determine that a table is poorly designed?
- What common table design mistakes in ClickHouse do you know?
- What anti-patterns are there when using ClickHouse?
- Why do OLTP patterns often translate poorly to ClickHouse?
- Why are frequent DELETE/UPDATE operations a bad pattern for ClickHouse?
- Why is “normalize it like in PostgreSQL” often a bad idea for ClickHouse?
- How do you choose ORDER BY for a table with logs?
- How do you choose ORDER BY for user events?
- How do you choose ORDER BY for multidimensional analytics?
- What happens if ORDER BY is chosen poorly?
- How does partition pruning affect performance?
- How does primary key pruning affect performance?
- Why can a query without conditions on the sorting key read a lot of data?
- How do you estimate how much data a query actually reads?
- What do read rows / read bytes mean in the query profile?
- How do you reduce the volume of data being read?
- When is materializing aggregations justified?
- When is pre-aggregation harmful?
- What are late arriving events and how do you handle them in ClickHouse?
- How do you account for delayed events in aggregates?
- How do you recalculate aggregates retroactively?
- How do you store historical versions of data in ClickHouse?
- How would you implement an analytical event store for e-commerce on ClickHouse?
- How would you implement a log store on ClickHouse?
- How would you implement a dashboard with near-real-time metrics on ClickHouse?
- How would you organize a retention policy for 30 days of hot data and 1 year of cold data?
- How would you migrate analytics from PostgreSQL to ClickHouse?
- Which metrics would you monitor in a production ClickHouse cluster?
- What production issues occur most often in ClickHouse?
- What would you check if ClickHouse suddenly became slow to respond?
- What would you check if ingestion stopped keeping up with the stream?
- What would you check if the number of parts grew significantly?
- How would you explain to the business why ClickHouse is needed specifically as an analytical store rather than a transactional one?
- When should you not choose ClickHouse?
- When is it better to choose PostgreSQL, BigQuery, Snowflake, or Elasticsearch instead of ClickHouse?
- Which ClickHouse limitations is it important for an architect to understand before adoption?
- What trade-offs would you discuss before building a central analytical store on ClickHouse?

#### 7.15.2 Data warehouse basics
- What is a data warehouse?
- Why does a company need a data warehouse?
- How is a data warehouse different from a regular operational database?
- How is a data warehouse different from a data lake?
- How is a data warehouse different from a data mart?
- What are OLTP and OLAP?
- Why are analytical and transactional workloads usually separated?
- What typical data sources feed into a data warehouse?
- What business problems does a data warehouse solve?
- Why can’t you just build analytics directly on the production database?
- What do subject-oriented, integrated, time-variant, and non-volatile data mean in the context of a DWH?
- What is a fact and what is a dimension?
- What is a fact table?
- What is a dimension table?
- What types of fact tables do you know?
- What types of dimensions do you know?
- What is the grain of a fact table?
- Why must the grain be defined before designing the schema?
- What is a star schema?
- How is a star schema different from a snowflake schema?
- What are the pros and cons of a snowflake schema?
- When is denormalization in a warehouse justified?
- What is a surrogate key and why is it needed in a DWH?
- How is a surrogate key different from a business key / natural key?
- When is a natural key not enough?
- What are slowly changing dimensions?
- What types of SCD do you know?
- What is SCD Type 1?
- What is SCD Type 2?
- What is SCD Type 3?
- When do you choose SCD Type 1 and when Type 2?
- How do you store the history of changes to dimension attributes?
- What are conformed dimensions?
- Why are conformed dimensions needed in an enterprise DWH?
- What is a junk dimension?
- What is a degenerate dimension?
- What is a bridge table?
- When is a bridge table needed in an analytical model?
- What is a snapshot fact table?
- What is an accumulating snapshot?
- What is a transaction fact table?
- What is a semi-additive measure?
- What are additive, semi-additive, and non-additive metrics?
- Why can’t all metrics simply be summed?
- What is dimensional modeling?
- How is dimensional modeling different from 3NF for warehouses?
- When do you choose 3NF for a DWH, and when a dimensional model?
- What is Data Vault?
- How is Data Vault different from a star schema?
- In what kinds of organizations is Data Vault especially useful?
- What are the pros and cons of Data Vault?
- What is ETL?
- How is ETL different from ELT?
- When is ETL preferable to ELT?
- When is ELT preferable to ETL?
- What is a staging layer?
- Why do you need staging in a DWH?
- What are raw layer, refined layer, and serving layer?
- Which data layers are usually выделated in a modern analytics platform?
- What are bronze, silver, and gold layers?
- How do they differ in data quality and readiness?
- What is batch processing?
- What is streaming / near-real-time ingestion?
- When does the business truly need a near-real-time DWH?
- What trade-offs between batch and streaming do you know?
- What is CDC (Change Data Capture)?
- How does CDC work?
- What ways of replicating changes into a DWH do you know?
- How does logical replication of changes differ from periodic full reload?
- When is a full refresh acceptable, and when is it not?
- What is incremental load?
- How do you design incremental loading?
- How do you handle late arriving data?
- How do you handle out-of-order events?
- What is idempotency in data loading pipelines?
- Why is idempotency critical for ETL/ELT?
- How do you deal with duplicates in a DWH?
- What is data quality?
- What dimensions of data quality do you know?
- How do you validate data completeness?
- How do you validate data accuracy?
- How do you validate data uniqueness and consistency?
- What is data lineage?
- Why is data lineage needed?
- What is a data catalog?
- What is a data catalog used for?
- What is metadata management?
- Why does a DWH quickly turn into a “swamp” without good metadata?
- What is a semantic layer?
- Why do you need a semantic layer on top of a warehouse?
- What is a metrics layer?
- Why is it important to define business metrics centrally?
- What is a single source of truth?
- Why is it difficult to achieve a single source of truth in practice?
- What are KPI drift and metric inconsistency?
- Why do different teams often calculate the same metric differently?
- How do you organize governance in a data warehouse?
- What is data ownership?
- Who should own data marts and data models?
- How do you organize role-based access to data?
- What are row-level security and column-level security?
- Which data cannot simply be moved into a warehouse?
- How do you account for PII and privacy requirements when designing a DWH?
- What is a data retention policy in analytics?
- How do you determine the retention period for raw and aggregated data?
- What is partitioning in a DWH?
- When is partitioning truly needed?
- How does partitioning affect cost and performance?
- What are clustering / sorting keys in analytical warehouses?
- How does the choice of sorting keys affect queries?
- What are materialized views in analytics?
- When should they be used?
- What risks do materialized views carry?
- What is pre-aggregation?
- When is pre-aggregation needed?
- When does pre-aggregation hurt analytics flexibility?
- What is a BI layer?
- How is a warehouse related to BI systems?
- What problems arise when BI works on top of a warehouse?
- Why can “one bad heavy dashboard” take down an analytics system?
- How do you limit heavy analytical queries?
- What is workload management in analytical systems?
- How do you organize multiple data marts on top of a single warehouse?
- How is a data mart different from just a separate data mart table?
- Should you build one enterprise warehouse or several domain warehouses?
- What is a centralized vs decentralized data platform?
- What is data mesh?
- How is data mesh different from a classic centralized DWH?
- When does data mesh make sense, and when is it unnecessary complexity?
- What is domain-oriented data ownership?
- How are data warehouses related to product analytics?
- How are data warehouses related to ML / feature engineering?
- Why does a warehouse often become the source for a feature store?
- What is reverse ETL?
- When is reverse ETL useful?
- How is a warehouse used for customer 360?
- How is a warehouse used for financial reporting?
- How is a warehouse used for marketing analytics?
- How is a warehouse used for event analytics?
- How would you design a DWH for e-commerce?
- How would you design a DWH for a SaaS product?
- How would you design a DWH for an advertising platform?
- How would you design a retention mart?
- How would you design a revenue / LTV / CAC mart?
- How would you model orders, payments, refunds, and discounts in a DWH?
- How would you model users, sessions, and events?
- How would you model a slowly changing customer profile?
- How would you store currencies, exchange rates, and historical conversion?
- How would you ensure reproducibility of analytical calculations?
- How do you test ETL/ELT pipelines?
- How do you test business logic in data transformations?
- What kinds of unit / integration / data quality tests exist for pipelines?
- How do you monitor data loading pipelines?
- What alerts should be set for a DWH?
- What SLA/SLO can an analytics platform have?
- What do you do if a load is late but the report is needed now?
- What do you do if you discover that a metric has been calculated incorrectly for months?
- How do you roll back errors in data pipelines?
- What is a backfill?
- How do you safely backfill large volumes of data?
- What problems does backfill cause in production?
- How do you estimate the cost of a warehouse architecture?
- What does the cost of an analytics platform usually consist of?
- How do you reduce storage and compute costs?
- What are hot / warm / cold data tiers?
- When is data archiving justified?
- What is the trade-off between data granularity and cost?
- When is a full-fledged DWH not needed?
- When is PostgreSQL plus a few marts enough?
- When is a data lake better, and when is a warehouse better?
- When should you choose a lakehouse approach?
- What common mistakes in building a DWH do you know?
- Why do many DWH projects fail organizationally rather than technically?
- What anti-patterns exist in analytical models?
- Why is “dump everything in and sort it out later” a bad approach?
- Why does the absence of a clear grain and data owners destroy the model?
- How would you explain to the business the difference between operational reporting and an analytical warehouse?
- What questions would you ask before designing the first data warehouse in a company?
- What should a software engineer understand about DWH even if they are not a data engineer?
- What should an architect understand about DWH when designing a large system?

### 7.16 Backups, restoration, and disaster recovery

#### 7.16.1 Backup/restore strategies
- What is a backup in the context of databases?
- Why are database backups needed?
- How is a backup different from replication?
- Why does having a replica not replace having a backup?
- What are the main goals of database backups?
- What main risks does a backup cover?
- What types of data loss can occur without a backup?
- What is restore and how is it different from recovery?
- What are the main types of backups?
- What is a full backup?
- What is an incremental backup?
- What is a differential backup?
- What is the difference between incremental and differential backup?
- When does it make sense to use only full backups?
- When is it beneficial to use full + incremental?
- When is it beneficial to use full + differential?
- How do you choose a backup strategy for a small database?
- How do you choose a backup strategy for a large database?
- How do you choose a backup strategy for a high-load system?
- What is a logical backup?
- What is a physical backup?
- What is the difference between logical and physical backup?
- When is it better to use a logical backup?
- When is it better to use a physical backup?
- What are the pros and cons of logical backup?
- What are the pros and cons of physical backup?
- Why can a logical backup be slow on large volumes of data?
- Why is physical backup usually faster to restore?
- What is a hot backup?
- What is a warm backup?
- What is a cold backup?
- What is the difference between hot, warm, and cold backup?
- When is only a cold backup acceptable?
- When do you need a hot backup specifically?
- What is an online backup?
- What is a consistent backup?
- What makes a backup consistent?
- Why is backup consistency more important than simply copying files?
- Can you just copy database files at the file-system level and consider that a backup?
- In what cases can a file copy of a database be useless?
- How does the transaction log help with backup and restore?
- What is a point-in-time recovery backup strategy?
- What is a base backup?
- Why save WAL / binlog / redo logs together with a backup?
- How does a full backup + transaction log strategy work?
- How often should you do a full backup?
- How often should you do an incremental backup?
- How does backup frequency depend on RPO?
- How does backup strategy depend on the data growth rate?
- How does backup strategy depend on the maintenance window?
- What is a backup window?
- Why is it important to consider the backup window when designing a strategy?
- What is a restore window?
- Why is the restore window no less important than the backup window?
- How do you assess the impact of backups on production load?
- Can you back up from a replica instead of the primary?
- What are the advantages of backing up from a replica?
- What are the risks of backing up from a replica?
- Why is backing up from a replica not always safe?
- Can you lose data if you back up only from a lagging replica?
- How do backups affect CPU, disk, and network?
- How do you reduce the impact of backups on a production system?
- Which resources most often become the bottleneck during backup?
- What is backup compression?
- What are the pros and cons of backup compression?
- When does compression help, and when does it hurt?
- What is backup encryption?
- Should backups be encrypted?
- Where is it better to encrypt backups: on the application side, in the backup tool, or in storage?
- What risks does an unencrypted backup carry?
- How do you store encryption keys for backups?
- What is a retention policy for backups?
- How do you determine the backup retention period?
- Why can’t you store backups forever?
- What legal or regulatory requirements can affect retention policy?
- How do you reconcile retention policy with privacy and data deletion requirements?
- What is the grandfather-father-son strategy?
- In what cases does a multi-level backup rotation make sense?
- What is backup rotation?
- How do you avoid a situation where new backups overwrite the only working copies?
- What are immutable backups?
- Why are immutable backups needed?
- How do immutable backups help against ransomware?
- What is an air-gapped backup?
- When is an air-gapped backup needed?
- What is an offsite backup?
- Why shouldn’t you store backups only on the same server where the database runs?
- Why shouldn’t you store backups only in the same data center?
- What is the 3-2-1 backup rule?
- How do you apply the 3-2-1 rule to database backups?
- How do you organize backup storage in the cloud?
- What risks arise when storing backups in S3-compatible storage?
- What should you consider when storing backups in object storage?
- How do you verify backup integrity?
- What is a backup checksum?
- Should a backup be validated after creation?
- How do you know that a backup is not corrupted?
- How is “backup created successfully” different from “backup is guaranteed to be restorable”?
- What is backup verification?
- What levels of backup verification exist?
- Should you periodically bring up a restored database from backup?
- What is a test restore?
- Why can’t a backup be considered reliable without a test restore?
- How often should test restores be performed?
- How do you automate test restores?
- Which backup process metrics should be monitored?
- What alerts should be set for backup processes?
- What should you do if backups start taking too much space?
- What should you do if backups no longer fit into the backup window?
- How do you scale a backup strategy as data volume grows?
- What mistakes do teams most often make when setting up backups?
- What anti-patterns in backup strategy do you know?
- What is more dangerous: no backup or no regular restore testing?
- What backup requirements exist for financial systems?
- What backup requirements exist for medical or personal data?
- How do you back up DBMS configuration together with the data?
- Should users, roles, grants, and schema be backed up separately?
- Should external files, object storage, and queues be included in backups if the application depends on more than just the database?
- How do you design a backup strategy for a system where state is stored in multiple data stores?
- How do you make a consistent backup for a microservice system with multiple databases?
- What do you do if you need a single recovery point across several services and databases?
- How do you design a backup strategy for a multi-tenant system?
- How do you design a backup strategy for very large tables?
- How do you design a backup strategy for a geo-distributed system?
- What should you consider in a backup strategy for containerized databases?
- What risks are there in backing up inside Kubernetes?
- Can a disk snapshot be considered a full backup?
- What is the difference between a storage snapshot and a database-consistent backup?
- When is a volume snapshot useful, and when is it dangerous?
- How do snapshots work at the cloud provider level?
- Can a file-system snapshot be used for online backup?
- What needs to be done before a snapshot to make it consistent?
- How do you choose between pg_dump / mysqldump, physical backups, and snapshots?
- How do you back up NoSQL databases compared to relational ones?
- How is backing up Redis different from backing up PostgreSQL / MySQL?
- How is backing up MongoDB different from backing up a relational database?
- How are backup strategy and disaster recovery strategy related?
- What should be documented in a backup runbook?
- What steps should a database recovery runbook contain?
- Who should have access to backups?
- How do you restrict access to backups from a security perspective?
- How do you log backup read and restore operations?
- How do you organize emergency restore at night or on weekends?
- What would you ask a team if you joined a project and wanted to assess the maturity of its backup strategy?
- What interview questions would you ask a candidate to understand whether they have really worked with backup/restore rather than just know the theory?
- Tell me about a case when a backup formally existed, but restore from it failed. Why did that happen?
- What trade-offs between storage cost, backup frequency, and restore speed would you discuss with the business?
- If the budget is limited, what can you not afford to save on in a backup strategy?
- What decisions would you make as a lead/architect when designing a backup strategy for a critical system?
- How would you explain to a non-technical stakeholder why a “replica” and a “backup” are not the same thing?
- How would you design a backup strategy for an early-stage startup?
- How would you design a backup strategy for an enterprise system with regulatory requirements?
- How would you migrate an old informal backup strategy to a more reliable one without downtime?

#### 7.16.2 RPO/RTO and restoration verification
- What is RPO?
- What is RTO?
- What is the difference between RPO and RTO?
- How would you explain RPO in simple terms?
- How would you explain RTO in simple terms?
- Why should RPO and RTO be defined by the business, not just engineers?
- What factors affect the acceptable RPO?
- What factors affect the acceptable RTO?
- Why can different systems within the same company have different RPO and RTO?
- How do you determine the target RPO for a specific service?
- How do you determine the target RTO for a specific service?
- How are RPO/RTO related to infrastructure cost?
- Why does reducing RPO almost always make the solution more expensive?
- Why does reducing RTO almost always make the architecture more complex?
- How does backup frequency affect RPO?
- How does restore speed affect RTO?
- How does replication affect RPO and RTO?
- Can replication improve RTO but fail to guarantee an acceptable RPO?
- Why doesn’t asynchronous replication guarantee zero RPO?
- When is near-zero RPO required?
- When is an RPO measured in hours acceptable rather than minutes?
- When is a large RTO acceptable?
- Which systems require a minimal RTO?
- What is a DR target?
- What is a disaster recovery plan?
- What is a business continuity plan and how is it different from a DR plan?
- What levels of disaster scenarios should be considered in DR?
- What common disaster scenarios do you know?
- What do you do if one database server is lost?
- What do you do if an entire availability zone is lost?
- What do you do if an entire region is lost?
- What do you do if data is logically corrupted but the infrastructure is still alive?
- What do you do if a massive erroneous DELETE/UPDATE operation occurs?
- Why is disaster recovery not just about “hardware burned down”?
- Why doesn’t backup always save you from logical corruption without point-in-time recovery?
- What is point-in-time recovery?
- How does PITR help achieve a stricter RPO?
- What data and artifacts are needed for PITR?
- What must be preserved to be able to recover to a specific point in time?
- How do you choose the target recovery point after an incident?
- How do you recover “not too early and not too late” after an accidental data deletion?
- What is a recovery drill?
- How is a recovery drill different from a test restore?
- Why are regular recovery drills mandatory for a mature team?
- How often should recovery drills be conducted?
- What types of recovery drills are there?
- What is checked during a recovery drill?
- How do you measure the actual RTO during drills?
- How do you measure the actual RPO during drills?
- What do you do if the actual RTO is much worse than the stated target?
- What do you do if the actual RPO is worse than business expectations?
- Why can’t you trust only the calculated RTO without real drills?
- How do you build a process for regular restoration verification?
- How do you automate recovery in a test environment?
- Besides the fact that the instance starts, what should you verify after restoring a database?
- How do you verify data consistency after restore?
- How do you make sure schema, users, permissions, extensions, jobs, and configuration were restored?
- How do you verify that the application actually works with the restored database?
- Should smoke tests be run after restore?
- Should data integrity checks be run after restore?
- What business checks should be done after restoration?
- What are the acceptance criteria for a successful recovery?
- How do you organize a sandbox for regular restore verification?
- Who should participate in DR exercises: only DBA or the whole team?
- Why should DR checks involve not only DBA, but also backend, DevOps, security, and business stakeholders?
- What should be in a disaster recovery runbook?
- How do you document failover and fallback steps in a runbook?
- How is failover different from restore from backup?
- When is failover enough, and when do you specifically need restore from backup?
- What is switchover and how is it different from failover?
- How are RTO/RPO related to architectural choices like active-passive and active-active?
- How does an active-passive architecture affect RTO and RPO?
- How does an active-active architecture affect RTO and RPO?
- Why does a multi-region architecture complicate recovery?
- What additional risks exist in multi-region DR?
- How do you assess whether the team can handle a real recovery at night under stress?
- What is a game day for disaster recovery?
- How do you run a game day for database failure?
- What metrics and logs should be collected during a recovery drill?
- How do you document the results of a recovery drill?
- What findings should be recorded after a recovery test?
- What do you do if the recovery process depends on one specific person?
- Why are knowledge silos dangerous for DR?
- How do you reduce bus factor in database recovery matters?
- How do you test restoration onto new infrastructure rather than only “the same type of server”?
- How do you verify restoration after a DBMS version upgrade?
- What are the risks of restoring a backup made on one DB version into another version?
- How do you test compatibility of restore procedures after schema changes?
- How do restore procedures depend on data volume?
- Why does RTO on a small test database say almost nothing about production?
- How do you extrapolate restore time from a test environment to production?
- Should restoration of very large tables be tested separately?
- Should restoration of roles, users, and privileges be tested separately?
- What do you do if a DR drill shows that the backup works, but the recovery process is too complex and manual?
- How do you simplify and automate recovery processes?
- How are RPO/RTO related to a system’s SLA?
- Can an SLA be met with a poor RPO?
- How would you explain to the business why a stated SLA does not guarantee no data loss?
- How would you explain the difference between uptime and recoverability?
- What mistakes do teams most often make when formalizing RPO and RTO?
- What anti-patterns occur in disaster recovery?
- What is worse: infrequent backups or never testing restores?
- What do you do if backups exist but nobody has ever measured the real restoration time?
- What do you do if teams argue about target RPO/RTO and the business cannot formulate requirements?
- What questions would you ask product or business stakeholders to agree on RPO and RTO?
- What solutions would you propose for a system with RPO = 0 and a very small RTO?
- Is it realistic to achieve RPO = 0? Under what conditions?
- Why is “we want zero RPO and zero RTO” often an unreasonable requirement?
- What trade-offs between consistency, cost, and recovery would you discuss as an architect?
- How would you design DR for a startup with a limited budget?
- How would you design DR for a critical enterprise system?
- How would you assess the maturity of a disaster recovery process in a new company?
- How would you convince management to invest in recovery drills if “nothing has ever gone down”?
- Tell me about a real incident where a formal backup existed, but the DR process turned out to be inadequate. What lessons were learned?
- What is more important for a mature team: having backups, having a runbook, regular drills, or clear business requirements? How would you prioritize them and why?
