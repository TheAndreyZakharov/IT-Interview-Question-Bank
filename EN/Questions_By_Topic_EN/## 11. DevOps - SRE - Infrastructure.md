## 11. DevOps / SRE / Infrastructure

### 11.1 General questions
- What is DevOps?
- How is DevOps different from simply administering servers?
- Why is DevOps considered not only a set of tools but also a culture?
- What are the main goals of the DevOps approach?
- What is SRE?
- How is SRE different from DevOps?
- What is the difference between a DevOps engineer, a system administrator, a platform engineer, and an SRE?
- What tasks does a DevOps engineer usually solve in a team?
- What tasks does an SRE usually solve in a team?
- At which stages of the product lifecycle does a DevOps engineer participate?
- What problems in a team usually indicate a weak DevOps process?
- What does the abbreviation CI/CD stand for?
- What is the difference between Continuous Integration, Continuous Delivery, and Continuous Deployment?
- What is Infrastructure as Code?
- What is immutable infrastructure?
- What is idempotency and why is it important in infrastructure?
- What is configuration drift?
- What is observability?
- How is observability different from monitoring?
- What is reliability in the context of infrastructure?
- What is availability?
- What is scalability?
- What is elasticity?
- What is resilience?
- What is fault tolerance?
- How is fault tolerance different from high availability?
- What is disaster recovery?
- What is a single point of failure?
- What is blast radius?
- What is the shared responsibility model in the cloud?
- Which key metrics would you use to assess the health of infrastructure?
- What basic components of modern infrastructure would you expect to see in a typical web product?
- What components does the production infrastructure of a web application usually consist of?
- What is an environment and which environments do you usually use?
- What is the difference between dev, test, stage, and prod environments?
- Why is a staging environment needed?
- When does staging stop being useful?
- What does parity between environments mean and why is it important?
- What is the release process?
- What is the deployment process?
- What is the difference between a release and a deploy?
- What is a rollback?
- What is a rollback plan and why is it needed?
- Why is “works on my machine” a DevOps problem?
- What does “pets vs cattle” mean in infrastructure?
- Why is automation so important in DevOps?
- Which processes in a team would you automate first?
- Which routine tasks should not be automated too early?
- How do you determine that a specific process is ready to be automated?
- How do you know that an infrastructure solution has become too complex?
- What is platform engineering and how is it different from DevOps?
- What role does DevOps play in feature delivery speed?
- How does DevOps affect product quality?
- How does DevOps affect product security?
- How does DevOps affect operating costs?
- What is developer experience and why should a DevOps team care about it?
- How does DevOps affect time-to-market?
- Which DevOps anti-patterns have you encountered?
- Can it be said that DevOps is just “a person who writes YAML”? Why?
- Why can a dedicated DevOps team sometimes help and sometimes hinder?
- When does it make sense to have a separate DevOps function, and when is it better to develop DevOps competencies within product teams?
- What does shift-left mean in engineering processes?
- What does shift-right mean?
- What is policy as code?
- What is secrets management and why can’t secrets be stored just anywhere?
- Why is it important in DevOps to be able to read logs, metrics, and traces, not just “deploy containers”?
- What skills besides tools are needed for a strong DevOps engineer?
- What Linux knowledge is mandatory for DevOps?
- What networking knowledge is mandatory for DevOps?
- What security knowledge is mandatory for DevOps?
- What development knowledge is useful for a DevOps engineer?
- Does a DevOps engineer need to know how to program? At what level?
- Which languages are usually used to write auxiliary automation in DevOps?
- Why is Bash not enough for all infrastructure tasks?
- Where is it better to use shell scripts, and where a full-fledged programming language?
- What are the trade-offs between using managed services and self-managing infrastructure?
- When should you choose a managed database, and when a self-hosted database?
- When is Kubernetes justified, and when is it overengineering?
- When is Docker needed, and when can you do without it?
- When is a monolith simpler and more cost-effective to operate than microservices?
- Why do microservices often complicate DevOps and SRE work?
- What DevOps problems most often appear when moving from a monolith to microservices?
- What DevOps problems arise in a 5-person startup?
- What DevOps problems arise in a company with 100+ engineers?
- How does the role of DevOps change as a company grows?
- What do you consider good infrastructure architecture for a small product?
- What do you consider good infrastructure architecture for a rapidly scaling product?
- What signs indicate that infrastructure is “blind” and poorly observable?
- What signs indicate that the team depends too much on manual actions?
- What signs indicate that releases are too risky?
- What signs indicate that the team is poorly prepared for incidents?
- How do you assess the maturity of DevOps practices in a company?
- What DevOps team maturity metrics do you know?
- What are DORA metrics?
- Which four DORA metrics do you know?
- Why is deployment frequency important?
- What does lead time for changes show?
- What does mean time to recovery show?
- What does change failure rate show?
- Which metrics can be “improved” directly but actually make things worse in reality?
- How should a DevOps team work with developers?
- How should a DevOps team work with QA?
- How should a DevOps team work with the security team?
- How should DevOps/SRE interact with management and business?
- What is on-call and why is it needed?
- Should developers participate in on-call? Why?
- Should DevOps/SRE be the only ones responsible for incidents?
- What is a runbook?
- What is a playbook?
- What is the difference between a runbook and a playbook?
- What is a postmortem?
- How is a good postmortem different from looking for someone to blame?
- What is blameless culture?
- Why is a blameless postmortem important?
- When can blameless culture be misunderstood?
- What is toil in SRE?
- What types of toil do you know?
- How do you distinguish useful operational work from toil?
- How would you reduce the level of toil in a team?
- What is an error budget?
- Why can an error budget be more useful than an abstract “we need to make everything more stable”?
- How do you balance the speed of change and system reliability?
- How do you convince the business to invest in internal infrastructure if “it doesn’t deliver new features”?
- How do you justify spending time on automation, CI/CD improvements, or observability?
- How do you explain the value of logging, metrics, and alerting to a non-technical manager?
- How would you determine the priority between developing a new feature and eliminating an infrastructure risk?
- How do you know that infrastructure debt has become critical?
- What trade-offs between development speed and operational reliability do you consider acceptable?
- How do you determine when infrastructure should be simplified rather than expanded?
- What typical mistakes do teams make when they “introduce DevOps”?
- Why is hiring one DevOps engineer not enough for “DevOps to appear in the company”?
- What is wrong with a model where all infrastructure expertise is concentrated in one person?
- How do you build processes so that bus factor is not equal to 1?
- What is bus factor in relation to infrastructure?
- Which infrastructure knowledge should be distributed across the whole team?
- What must обязательно be documented in infrastructure?
- What should be automated first in a new team?
- What should absolutely be automatically verifiable?
- What decisions would you make in the first 30 days if you joined a chaotic team as a DevOps engineer?
- What decisions would you make in the first 90 days if a company had no mature DevOps practices at all?
- How would you assess the current state of infrastructure in a new company?
- What questions would you ask the team to understand their DevOps maturity?
- What artifacts and documents would you request from a team during an infrastructure audit?
- What would you check first in production infrastructure if you had just been hired?
- How would you realize that production is “held together with duct tape”?
- What minimum infrastructure baseline would you want to see in any production system?
- Without what would you refuse to put a system into production?
- What processes absolutely must exist around production access?
- How should access management in infrastructure be organized?
- Why are local manual changes on servers dangerous?
- Why is “we urgently fixed it manually in prod” a dangerous pattern?
- In what cases is manual intervention in production justified?
- What must be done after manual intervention in production?
- How do you approach infrastructure standardization across different services?
- What is better: flexibility for each team or unified infrastructure standards? Where is the balance?
- How do you determine which technologies should be included in the platform’s “golden path” and which should not?
- What is a golden path for engineering teams?
- How can DevOps/SRE accelerate teams rather than become a bottleneck?
- What is self-service infrastructure?
- When is self-service infrastructure useful, and when is it dangerous?
- How would you organize self-service for deploying new services?
- How would you organize self-service for issuing temporary access?
- What area of responsibility should remain with product teams, and what with the platform/DevOps team?
- How do you assess the long-term total cost of ownership of an infrastructure solution?
- What risks should be considered when choosing a new infrastructure tool?
- How do you decide whether to introduce a new tool if it looks “trendy” but does not solve an obvious pain point?
- What do you think about tool sprawl in infrastructure?
- How would you reduce a zoo of tools in a company?
- Which criteria do you use to choose between an open-source solution and a SaaS/managed solution?
- How do you assess vendor lock-in?
- When is vendor lock-in acceptable?
- When does vendor lock-in become too dangerous for a company?
- How would you prepare infrastructure for a security audit or compliance assessment?
- What practices need to be introduced so that infrastructure is convenient not only for operations but also for incident investigations?
- What does a “production-ready service” mean to you?
- What does an “operationally mature service” mean to you?
- Which non-functional requirements do you expect to see in a mature service?
- Which operational requirements would you add to a new service before development even begins?
- How do you integrate reliability requirements into the development process?
- What should be included in the Definition of Done from a DevOps/SRE perspective?
- Should infrastructure changes be evaluated as strictly as product changes? Why?
- How do you organize reviews of infrastructure changes?
- How do you verify the quality of Terraform/Ansible/Kubernetes configurations before applying them?
- What types of infrastructure testing do you know?
- What is infrastructure testing?
- How do you feel about ephemeral environments?
- When are ephemeral preview environments useful?
- What problems do ephemeral environments create in practice?
- How would you build infrastructure for a team with many pet services and prototypes?
- How would you build infrastructure for a tightly regulated industry: fintech, healthcare, govtech?
- What are the differences in DevOps/SRE approaches between a startup, an enterprise, and a highload company?
- What matters more at an early product stage: perfect reliability or speed of experiments?
- What changes when the product can no longer be “down for an hour at night”?
- When is a 24/7 duty engineer justified?
- How do you assess the need for multi-region / multi-cloud?
- When is multi-region a necessity, and when is it overengineering?
- When is multi-cloud a real strategy, and when is it an expensive illusion of independence?
- What would you choose for a new team: managed Kubernetes, VM-based deployment, serverless, or PaaS? Why?
- How would you explain to the business that infrastructure cannot scale indefinitely without architectural changes?
- How would you explain to developers why some architectural decisions sharply increase operational cost?
- Give an example of an infrastructure solution that was technically beautiful but bad for the business.
- Give an example of an infrastructure solution that was “not ideal” but right for the company’s context.
- Tell me about a case where you reduced the number of incidents not through heroics but through systemic changes.
- Tell me about a case where you automated a critical process and how you measured the result.
- Tell me about a case where you simplified infrastructure instead of adding new layers of abstraction.
- Tell me about a case where you refused to adopt a popular tool. Why?
- Tell me about a case where an infrastructure incident revealed an architectural problem in the product.
- Tell me about a case where the team wanted “Kubernetes from day one” and you proposed a simpler path.
- Tell me about a case where you had to balance release speed and reliability.
- Tell me about a case where you introduced observability and what it changed.
- Tell me about a case where you discovered a critical single point of failure.
- Tell me about a case where production kept breaking after releases. How did you find the root cause?
- Tell me about a case where the team had too much manual work. What did you do?
- Tell me about a case where engineers resisted infrastructure standardization. How did you act?
- Tell me about a case where the business did not want to invest in reliability. How did you justify the need?
- Tell me about a case where you had to choose between cost optimization and reliability.
- Tell me about a case where you reduced cloud costs without degrading the service.
- What questions would you ask a candidate for a DevOps/SRE position if you were hiring them yourself?
- What signs of a strong DevOps/SRE engineer do you look for when hiring?
- What signs of a weak DevOps/SRE engineer do you notice in interviews?
- What is more important for DevOps/SRE: deep expertise in one tool or a systemic understanding of the whole chain? Why?
- How do you distinguish an engineer who just knows kubectl/docker commands from one who understands operating systems?
- How would you evaluate a senior DevOps/SRE engineer in an interview?
- How would you evaluate a staff/principal/SRE lead in an interview?
- What architectural and organizational questions should be asked to a lead/platform engineer-level candidate?
- How should a strong DevOps/SRE lead think?
- How is a DevOps/SRE lead different from just a very strong individual contributor?
- What decisions should a Head of Infrastructure / Platform / SRE make?
- How do you measure the effectiveness of a DevOps/SRE team?
- By what metrics can you tell that a DevOps/SRE team is really helping the business?
- Which metrics can unfairly demotivate an infrastructure team?
- What should be in a one-year platform/infrastructure development strategy?
- How do you build a roadmap for an infrastructure team?
- How do you defend an infrastructure roadmap to business and product stakeholders?
- How do you prioritize reliability work, tech debt, cost optimization, and developer enablement?
- What would you do as the first Head of Platform/Infrastructure in a fast-growing startup?
- What would you do if you joined a company that has Kubernetes but no processes, monitoring, or understanding of why they need it?
- How would you act if you discovered that the production cluster and access to it were set up insecurely?
- How would you act if you found out that nobody knows how to restore the system from backup?
- How would you act if most production changes were being made manually?
- How would you act if the team frequently bypassed change management for speed?
- How would you act if engineers disabled alerts instead of fixing the causes of noise?
- How would you act if the number of incidents kept growing while management demanded only more features?
- How would you act if developers considered observability “extra overhead”?
- How would you act if two teams argued about who was responsible for the reliability of their shared system?

### 11.2 Linux and operations

#### 11.2.1 Processes, signals, systemd
- What is a process in Linux?
- How is a process different from a program?
- What is a PID?
- What is a PPID?
- How do you view the list of processes in Linux?
- What is the difference between `ps`, `top`, and `htop`?
- How do you find a specific process by name?
- What is a foreground process and a background process?
- How do you start a process in the background?
- What does the `&` symbol do in the shell?
- How do you bring a background process back to the foreground?
- What is job control in the shell?
- What is the difference between a job ID and a PID?
- What do the `jobs`, `bg`, and `fg` commands do?
- What is a daemon process?
- How is a daemon different from a regular process?
- What is a zombie process?
- What is an orphan process?
- How do zombie processes appear?
- How do orphan processes appear?
- How is a zombie different from an orphan?
- What are a parent process and a child process?
- How are child processes created in Linux?
- What is `fork()`?
- What is `exec()`?
- What is the difference between `fork()` and `exec()`?
- What does a child process inherit from its parent?
- What is a process state?
- Which main process states in Linux do you know?
- What do the states `R`, `S`, `D`, `T`, `Z` mean in `ps` output?
- What does uninterruptible sleep mean?
- Why can a process get stuck in the `D` state?
- What is a nice value?
- How do you change the priority of a process?
- What is the difference between `nice` and `renice`?
- What is the scheduler in Linux?
- What basic principles of process scheduling in Linux do you know?
- What is a CPU-bound process and an I/O-bound process?
- How do you determine that a process is bottlenecked by CPU rather than I/O?
- What is a context switch?
- Why can a large number of context switches degrade performance?
- How do you view the open files of a process?
- What is `/proc` in Linux?
- What information can be obtained from `/proc/<pid>/`?
- How do you view the command used to start a process?
- How do you view a process’s environment variables?
- How do you view the process tree?
- What does the `pstree` command do?
- How do you kill a process by PID?
- What is the difference between `kill`, `pkill`, and `killall`?
- Why does the `kill` command not always “kill” a process?
- Which Linux signals do you know?
- What is a signal in Linux?
- What are signals used for?
- What happens when a process receives a signal?
- Which signals cannot be caught or ignored?
- What is the difference between `SIGTERM` and `SIGKILL`?
- When should you use `SIGTERM`, and when `SIGKILL`?
- What is `SIGHUP` and where is it used?
- What is `SIGINT`?
- What is `SIGQUIT`?
- What are `SIGSTOP` and `SIGCONT`?
- What does `SIGCHLD` do?
- What is graceful shutdown of a process?
- How do you gracefully terminate a server process?
- What shutdown order would you implement for a production service?
- Why should you not immediately send `SIGKILL` to an application in production?
- What happens if you send `SIGKILL` to a process that is writing data to disk?
- How can a process handle an incoming signal inside an application?
- How do you diagnose a process that ignores `SIGTERM`?
- What is the init process?
- Why is PID 1 special?
- Who is usually PID 1 in modern Linux systems?
- What role does `systemd` play in modern Linux distributions?
- What is a unit in systemd?
- What types of unit files do you know?
- What is a service unit?
- What is a target unit?
- What is a timer unit?
- What is a socket unit?
- What is a mount unit?
- How do you view the status of a service via systemd?
- How do you start a service via systemd?
- How do you stop a service via systemd?
- How do you restart a service via systemd?
- What is the difference between `restart` and `reload` for a systemd service?
- What does `daemon-reload` do?
- When do you need to run `systemctl daemon-reload`?
- How do you enable a service to start automatically at boot?
- What is the difference between `enable` and `start`?
- How do you disable a service from starting automatically?
- How do you view service logs in systemd?
- What is `journalctl`?
- How do you view logs for a specific systemd service?
- How do you view logs for the current boot?
- How do you view logs in real time with `journalctl`?
- How do you filter logs by time with `journalctl`?
- How do you filter logs by priority level?
- Where are journald logs physically stored?
- How is journald different from classic syslog?
- What is a persistent journal?
- Why can `journalctl` logs disappear after a reboot?
- How do you configure a systemd service to restart automatically on failure?
- What do the `Restart=` and `RestartSec=` parameters do?
- Which `Restart=` values do you know?
- What does `ExecStart` do?
- What are `ExecStartPre` and `ExecStartPost`?
- What is `ExecStop`?
- What are `Type=simple`, `Type=forking`, `Type=oneshot`, `Type=notify`?
- When should you use `Type=oneshot`?
- When can `Type=forking` be problematic?
- What does `WorkingDirectory` do in a unit file?
- What are `Environment=` and `EnvironmentFile=` used for?
- How do you pass environment variables into a systemd service?
- What does `User=` do in a service unit?
- Why should a service be run as a non-root user?
- What does `Group=` do in a unit file?
- How do you restrict a service’s privileges with systemd?
- What do `Restart=always` and `Restart=on-failure` do?
- What is dependency management in systemd?
- What do `After=`, `Before=`, `Requires=`, and `Wants=` do?
- What is the difference between `Requires=` and `Wants=`?
- How do you make sure a service starts only after the network?
- Why is `After=network.target` not always enough?
- What is `multi-user.target`?
- What is `default.target`?
- How are systemd timers different from cron?
- When is it better to use a systemd timer, and when cron?
- How do you create a timer for periodic execution of a task?
- How do you debug a systemd service that fails to start?
- How do you validate a unit file?
- What should you do if a service starts manually but fails via systemd?
- Why can a service work in a shell but fail under systemd because of environment variables?
- What problems can arise because of an incorrect `WorkingDirectory`?
- How do you limit memory or CPU consumption of a service with systemd?
- How is systemd related to cgroups?
- What is a slice in systemd?
- How can systemd be used for resource isolation?
- How would you create a production-ready unit file for a backend application?
- Which systemd parameters would you definitely check when deploying a new service?
- How would you diagnose a situation where a service is “alive” but not responding to requests?
- What would you do if a service is constantly restarting under systemd?
- How do you determine whether a restart is caused by an application crash or an external factor?
- How would you organize a safe rolling restart of several services on a Linux host?
- What typical mistakes have you encountered in practice when working with processes and systemd?
- How is systemd more convenient than init.d / SysV init?
- In what cases can you still encounter init.d?
- How would you explain to a beginner the difference between a process, a service, and a unit file?
- How would you design the operation of a long-running process on Linux with logs, restarts, and graceful shutdown in mind?

#### 11.2.2 Filesystem, permissions, users, groups
- What is a filesystem in Linux?
- Which filesystems in Linux do you know?
- How is ext4 different from XFS?
- What is an inode?
- What is stored in an inode?
- Why can a file exist without a name in a directory?
- What is a hard link?
- What is a symbolic link?
- What is the difference between a hard link and a symbolic link?
- What limitations do hard links have?
- What happens to a symlink if the original file is deleted?
- What is a directory entry?
- What is a file descriptor?
- What is the difference between an inode and a file descriptor?
- How do you check which partition a file is on?
- How do you view the list of mounted filesystems?
- What does the `df` command do?
- What does the `du` command do?
- What is the difference between `df` and `du`?
- Why can `df` and `du` show different sizes?
- What is a mount point?
- What is mounting a filesystem?
- How do you mount a filesystem manually?
- What is `/etc/fstab`?
- How do you make a partition mount automatically after reboot?
- What is a partition UUID and why use it in `fstab`?
- Which errors in `fstab` can cause boot problems?
- What does the `noexec` option do?
- What does the `nosuid` option do?
- What does the `nodev` option do?
- When does it make sense to use a `ro` mount?
- What is tmpfs?
- How is tmpfs different from a normal disk-backed filesystem?
- What is a bind mount?
- Where and why are bind mounts used?
- What is an overlay filesystem?
- Where is overlayfs used in practice?
- What is a journaling filesystem?
- Why is a journal needed in a filesystem?
- What is fragmentation in Linux filesystems?
- How relevant is fragmentation for ext4 and XFS?
- What are permissions in Linux?
- How does the Linux access rights model work?
- What do `r`, `w`, and `x` mean for a file?
- What do `r`, `w`, and `x` mean for a directory?
- What is the difference between `w` permission on a file and on a directory?
- Why can a file be deleted without `w` permission on the file itself?
- How do you interpret the string `-rwxr-x---`?
- What do owner, group, and others mean?
- How do you view the owner and group of a file?
- How do you change the owner of a file?
- How do you change the group of a file?
- What is the difference between `chown` and `chgrp`?
- How do you change file permissions?
- What does `chmod 755` do?
- What does `chmod 644` do?
- How do you read octal notation for permissions?
- What is symbolic mode in `chmod`?
- What does `umask` do?
- How does `umask` affect permissions of new files and directories?
- Why are new files usually created without the executable bit?
- What is setuid?
- What is setgid?
- What is the sticky bit?
- Where is the sticky bit used in practice?
- What happens if the sticky bit is removed from the `/tmp` directory?
- What security risks are associated with setuid?
- Why is setuid on shell scripts considered a bad idea?
- What are ACLs in Linux?
- How are ACLs different from the classic owner/group/others model?
- When are ACLs more useful than standard Unix permissions?
- How do you view ACLs for a file?
- How do you grant ACL permissions to a specific user?
- What is a default ACL on a directory?
- What are users and groups in Linux?
- How is a user different from a group?
- What types of users exist in Linux?
- What is a system user?
- What are UID and GID?
- Why does root have UID 0?
- How do you view information about a user?
- How do you view which groups a user belongs to?
- How do you create a user in Linux?
- What is the difference between `useradd` and `adduser`?
- How do you delete a user?
- What should be done with the home directory when deleting a user?
- How do you change a user’s password?
- Where are password hashes stored in Linux?
- What is `/etc/passwd`?
- What is `/etc/shadow`?
- Why were password hashes moved from `/etc/passwd` to `/etc/shadow`?
- What is stored in `/etc/group`?
- What is stored in `/etc/gshadow`?
- How can you manually understand the structure of an entry in `/etc/passwd`?
- What is a user's login shell?
- How do you change a user's shell?
- What is the difference in intended use between `/bin/false` and `/sbin/nologin`?
- How do you create a group?
- How do you add a user to a group?
- What is the difference between a primary group and supplementary groups?
- What happens if you change a user's primary group?
- How do you temporarily run a command as another user?
- What does `sudo` do?
- How is `sudo` different from `su`?
- What does `sudo -i` do?
- Where is `sudo` configured?
- Why is it dangerous to edit sudoers directly?
- What does `visudo` do?
- How do you grant a user permission to run only specific commands via sudo?
- What is the principle of least privilege as applied to Linux users/groups?
- How would you organize access rights on a server for a team of several developers and one deployment user?
- How would you configure permissions on a directory so that a group can collaboratively edit files?
- How do you make new files in a directory automatically inherit the group?
- What is SGID on a directory and why is it needed?
- What is ownership drift and why can it be a problem on servers?
- How do you diagnose a “Permission denied” problem?
- What steps would you go through if a service cannot read its config or write to its log?
- Why can a process running as root still not have access to a file in a container or mount configuration?
- What typical permission mistakes occur during application deployment?
- How do you securely store secrets and configs on a Linux filesystem?
- Why should you not put secrets in world-readable files?
- How would you organize permissions for SSH keys?
- Why does SSH complain about overly broad permissions on `~/.ssh` and `authorized_keys`?
- What permissions should a private SSH key usually have?
- What are special files in Linux?
- What file types does the first character in `ls -l` show?
- What is a socket file?
- What is a named pipe (FIFO)?
- What is a block device and a character device?
- Why is “everything is a file in Linux” not literally true, but still practically useful?
- What is located in `/dev`?
- What is located in `/proc`?
- What is located in `/sys`?
- What are `/var`, `/etc`, `/home`, `/tmp`, `/opt`, `/usr`, and `/srv` used for?
- How would you explain the purpose of FHS (Filesystem Hierarchy Standard)?
- Where should application configs be placed correctly?
- Where should application logs be placed correctly?
- Where should runtime files, PID files, and sockets be placed correctly?
- How is `/tmp` different from `/var/tmp`?
- What is log rotation?
- How does `logrotate` work?
- What problems can occur if log rotation is not configured?
- Why does deleting a large file not always immediately free disk space?
- How do you find a process that is holding a deleted but still open file?
- How does `lsof` help diagnose file-related problems?
- How do you find the largest directories on a disk?
- How do you find files modified in the last N days?
- How do you find files with specific permissions?
- How do you find world-writable files and why is that important?
- How do you find setuid/setgid files in the system?
- What risks do world-writable directories without the sticky bit pose?
- What is quota in Linux?
- When does it make sense to enable disk quotas?
- How would you diagnose a “disk full” situation if `df` shows one thing but the directory looks small?
- What should you do if inodes run out before disk space does?
- How do you check inode usage?
- Why can a large number of small files be a problem?
- How would you design a filesystem structure for a backend service on a Linux host?
- How would you organize users, groups, and permissions on a production server for an application, CI/CD, and administrators?
- What Linux hardening practices around users/groups/permissions do you consider mandatory?
- How would you explain to a junior engineer the difference between a user, a group, a file owner, and permissions on a directory?

#### 11.2.3 Network tools
- Which Linux network utilities do you use most often and for what tasks?
- How do ping, traceroute, and tracepath differ?
- What does ping test, and what does it not test?
- Why might a host not respond to ping while a service on it is still working?
- What is ICMP and how does it relate to ping?
- Which command would you use to check whether a domain name resolves to an IP address?
- How do nslookup, dig, and host differ?
- Which command would you use to check which IP DNS currently returns for a domain?
- How do you view A, AAAA, CNAME, MX, TXT, and NS records for a domain?
- What is the TTL of a DNS record and how can you see it with dig?
- How do you check which DNS server is answering a query?
- How do you check whether the answer from an authoritative DNS server differs from a caching resolver?
- What is a recursive resolver and an authoritative DNS server?
- How do you check whether the problem is specifically in DNS rather than in the network or application?
- How can you trace the DNS delegation chain using dig?
- What do the ANSWER, AUTHORITY, and ADDITIONAL sections in dig output show?
- How do you check reverse DNS for an IP address?
- How do you check the packet route to a remote host?
- How is traceroute different from mtr?
- Why can traceroute show timeouts on intermediate hops even though the final service is reachable?
- What is mtr and in what cases is it more useful than traceroute?
- How do you interpret packet loss in mtr?
- How do you tell when packet loss in mtr is false rather than real traffic loss?
- How do curl and wget differ?
- When do you use curl, and when do you use wget?
- How do you use curl to check whether an HTTP service is reachable?
- How do you use curl to view only response headers?
- How do you use curl to send a POST request with JSON?
- How do you pass custom headers with curl?
- How do you use curl to check redirects?
- How do you use curl to measure a service’s response time?
- How do you use curl to check a TLS connection and certificate?
- How do you see which IP curl actually connected to?
- How do you use curl to force a request to a specific IP while preserving the Host header?
- How do you use curl to test connectivity through a proxy?
- How do you use curl to reproduce a client-side API issue?
- Which command do you use to check whether a TCP port is open on a remote host?
- How do nc, telnet, and curl differ when checking port availability?
- When is it more convenient to use nc (netcat) rather than curl?
- How do you use nc to check whether a remote host is listening on a specific port?
- How do you use nc to start a very simple TCP server or client?
- What is socat and when is it useful?
- How is socat different from netcat?
- How do you check which ports are listening on the local machine?
- How is ss different from netstat?
- Why is ss now more commonly recommended than netstat?
- How do you use ss to view all listening TCP sockets?
- How do you use ss to view established TCP connections?
- How do you determine which process is listening on a specific port?
- How do you see how many active connections are established to a process or port?
- How do you tell that an application has hit too many open connections?
- How do you check the number of open file descriptors for a process and why is that related to networking?
- What is lsof and what do you use it for in network diagnostics?
- How do you use lsof to find the process occupying a port?
- How do you see which network interfaces exist in the system?
- How do ip addr, ip link, and ip route differ?
- Why is ip preferred over ifconfig and route?
- How do you view IP addresses and masks on interfaces?
- How do you temporarily add an IP address to an interface?
- How do you view the routing table?
- How do you determine through which interface and gateway traffic to a specific IP will go?
- How do you use ip route get to check the route to an address?
- What is a default route?
- How do you check whether there is a problem in local routing on the host?
- How do you view the ARP/neighbor table?
- How is ip neigh different from arp?
- How do you determine that a problem is related to ARP/neighbor resolution?
- How do you clear the ARP/neighbor cache and when might that be needed?
- How do you check an interface’s MTU?
- How do you diagnose a problem related to an incorrect MTU?
- What are jumbo frames and when are they used?
- How do you determine that packet fragmentation is affecting performance?
- What is tcpdump and what is it used for?
- In which cases do you go straight to tcpdump?
- How do you use tcpdump to capture traffic on a specific interface?
- How do you filter traffic in tcpdump by port, IP, or protocol?
- How do you save a traffic capture to a pcap file?
- How do you read basic tcpdump output without Wireshark?
- How do you use tcpdump to check whether a packet reaches a host?
- How do you tell from tcpdump that the problem is at the application layer rather than the network?
- How do you see the TCP three-way handshake in tcpdump?
- How do you see retransmissions, resets, and packet loss in tcpdump?
- How do you tell from tcpdump which side closes a TCP connection first?
- What is a SYN flood, and can you notice its basic signs through tcpdump/ss?
- How do you check whether a firewall is blocking the required traffic?
- What tools do you use to diagnose iptables/nftables?
- How do you view the current iptables or nftables rules?
- How is nftables different from iptables at a basic level?
- How do you check whether SELinux or AppArmor is blocking a process’s network interaction?
- How do you view interface statistics such as dropped packets, errors, and overruns?
- How do you tell that there are physical-level problems on an interface?
- What commands do you use to diagnose a network card and link?
- What is ethtool and what is it used for?
- How do you use ethtool to check link speed and duplex?
- How do you diagnose link flapping?
- How do you check DNS, HTTP, and TCP connectivity as one chain if “the site won’t open”?
- How do you diagnose the situation: “the server responds to ping, but the application is unavailable”?
- How do you diagnose the situation: “the domain resolves, but it opens the wrong server”?
- How do you diagnose the situation: “the service is reachable locally, but not reachable from outside”?
- How do you diagnose the situation: “the API works from one machine, but times out from another”?
- What would you check first if an application cannot connect to a database over the network?
- What would you check first if a Kubernetes pod cannot reach an external service?
- How do you distinguish DNS, routing, firewall, TLS, and application-level errors?
- Which network utilities have you used in containers and Kubernetes when the usual tools are not available inside the container?
- How do you diagnose a problem with a proxy or a corporate MITM certificate?
- How do you check which certificate a server is actually presenting?
- How is openssl s_client useful in diagnosing network issues?
- How do you use openssl s_client to check a TLS handshake?
- How do you check whether a server supports the required TLS version and cipher suites?
- How do you determine that the problem is specifically in TLS rather than in TCP connect?
- Which commands do you use to quickly perform a minimal network triage on a Linux host?
- Imagine users complain about intermittent API timeouts. Which network commands would you use, and in what order?
- Imagine that some instances in one AZ can connect to Redis and some cannot. How would you investigate?
- How do you diagnose an intermittent network issue that cannot be reproduced on demand?
- Which network metrics and signs would you correlate with the output of command-line tools?
- How would you automate basic network diagnostics on a Linux host?
- What limitations do ping, traceroute, ss, tcpdump, and curl have as diagnostic tools?
- Which of these tools are most often restricted or disallowed in production, and what do you replace them with?
- Which network commands would you expect to see in a runbook for an on-call engineer?
- How would you explain to a junior engineer the difference between “the port is open,” “the service responds,” and “the application works correctly”?

#### 11.2.4 Performance tools
- Which basic Linux tools do you use to diagnose performance?
- Where do you start if a server is “slow”?
- Which 3–5 commands do you run first when investigating a performance degradation complaint?
- What does uptime show, and how is load average useful?
- What is load average and how do you interpret it?
- Why does high load average not always mean the CPU is overloaded?
- Can load average be high while CPU usage is low? Why?
- How do user time, system time, idle, iowait, and steal differ in CPU metrics?
- What is iowait and how do you determine that the system is bottlenecked on disk?
- What is steal time and what can it indicate in a virtualized environment?
- What does top show?
- How is htop more convenient than top?
- Which key fields in top/htop do you look at first?
- How do you use top to determine which process is consuming CPU?
- How do you use top to understand that a process is bottlenecked on memory?
- How do RES, VIRT, and SHR differ in top?
- What does %MEM mean and why can it be misleading?
- How do you tell that a process is multithreaded and which thread is consuming CPU?
- How do you view the most “expensive” threads inside a process?
- What does ps aux show and how is it different from top?
- How do you use ps to sort processes by CPU or memory?
- What does free show?
- How do free, used, buff/cache, and available differ in free output?
- Why does “almost no free memory” in free not always mean there is a problem?
- What is page cache and why does Linux use memory “for cache”?
- How do you tell that a system is actually suffering from a lack of RAM?
- What is swap and how do you determine that the system is actively swapping?
- How does swappiness affect system behavior?
- How do you view memory and swap statistics over time?
- What does vmstat show?
- Which vmstat fields do you consider the most useful?
- How do you use vmstat to tell whether the problem is in CPU, memory, or I/O?
- What do si/so mean in vmstat?
- What do r and b mean in vmstat?
- What does iostat show?
- How do you use iostat to determine that the disk is the bottleneck?
- What do await, svctm, util, r/s, and w/s mean in iostat?
- Why is high disk %util not always equal to a real problem?
- How do you distinguish high disk latency from simply high load?
- What does pidstat show?
- How is pidstat useful compared to top?
- How do you use pidstat to inspect CPU, memory, and I/O for a specific process?
- What does sar show?
- How is sar useful when analyzing a problem that already happened in the past?
- Which system counters would you inspect for retrospective analysis of a degradation?
- What does mpstat show?
- How do you tell that CPU load is distributed unevenly across cores?
- What are context switches and how do you know there are too many?
- Which tools show an excessive number of context switches?
- How do you determine that the problem is caused by excessive interrupts or softirqs?
- How do you view disk usage and inode usage?
- How is df different from du?
- How do you understand why “the disk is full” if df shows one thing and du shows another?
- How do you find the directories that are taking up the most space?
- How do you find large files that continue taking space after deletion?
- Why can a deleted log file continue occupying disk space?
- How do you use lsof to find deleted but still open files?
- How do you determine that an application has hit the open files limit?
- Which tools help find a memory leak in a Linux application?
- How do you notice signs of a memory leak at the OS level?
- How is RSS different from heap inside an application runtime?
- What is the OOM killer and how do you determine that it is what killed a process?
- Where do you look for information about the OOM killer firing?
- What are dmesg and journalctl, and how do they help during a performance incident?
- Which kernel messages are especially important during performance problems?
- What is perf and what is it used for?
- When is it worth using perf instead of just top/htop?
- What can you learn using perf top and perf record?
- What is a flame graph and how is it related to perf?
- What is strace and when is it useful in performance diagnostics?
- How is strace different from ltrace?
- How do you use strace to understand what a process is “stuck on”?
- How do you use strace to determine that a process is bottlenecked on network, file I/O, or locks?
- What is pmap and what is it used for?
- What is /proc and which files there are useful when analyzing performance?
- What data can be obtained from /proc/<pid>/status, /proc/<pid>/smaps, and /proc/<pid>/limits?
- How do you determine that the problem is caused by throttling inside cgroups or a container?
- Which CPU and memory metrics inside a container can differ from host metrics?
- How do you determine that Kubernetes limits/requests are affecting application performance?
- How do you notice CPU throttling in a container?
- What is pressure stall information (PSI), and have you used it?
- How do you diagnose the situation: high API latency, but low CPU?
- How do you diagnose the situation: high CPU, but throughput is not increasing?
- How do you diagnose the situation: a process is stuck, but does not crash?
- How do you diagnose the situation: application memory usage increased after a release?
- How do you diagnose the situation: system CPU increased after a release, but user CPU barely changed?
- How do you diagnose the situation: the server “freezes” for a few seconds from time to time?
- How do you distinguish an application problem from an OS or infrastructure problem?
- What signs indicate that the bottleneck is CPU?
- What signs indicate that the bottleneck is memory?
- What signs indicate that the bottleneck is disk?
- What signs indicate that the bottleneck is network?
- What signs indicate that the bottleneck is lock contention?
- What signs indicate that the bottleneck is GC or a language runtime VM?
- How do you correlate system tools with APM, application metrics, and logs?
- What minimum performance triage do you expect from a middle DevOps/SRE engineer?
- Which Linux performance tools do you consider mandatory for an on-call engineer?
- What would you include in a runbook for performance diagnostics on a Linux host?
- How do you automate collection of basic performance snapshots during an incident?
- How do you balance diagnostic depth against the risk of harming a production system?
- Which profiling tools are unsafe to run on a heavily loaded production system and why?
- How do you estimate overhead from strace, perf, tcpdump, and other diagnostic tools?
- What typical mistakes do engineers make when reading top, vmstat, iostat, and free?
- How is “the server is slow” different from “the application has a bad latency budget”?
- How do you explain the difference between utilization, saturation, and errors at the system level?
- What are the USE and RED methods, and how do they help in operations?
- What are the golden signals and how do they relate to Linux performance tools?
- How do you build hypotheses when investigating a performance problem?
- What do you do if the problem is intermittent and has already disappeared by the time you investigate?
- Which historical data should be collected in advance so that a degradation can later be analyzed?
- How do you distinguish a symptom from the root cause in a performance incident?
- Imagine CPU is 100% on one core while the others are almost idle. Which hypotheses would you test?
- Imagine load average increased, but CPU is only at 20%. Which hypotheses would you test?
- Imagine free shows little available memory, but the application works normally. What would you explain to the team?
- Imagine iowait is high, but at first glance the disk “is not busy.” What would you check?
- Imagine after enabling debug logging the application became slow. Which tools would you use, and how would you prove it?
- Imagine a container in Kubernetes periodically restarts without an obvious error in logs. Which performance/system-level hypotheses would you check?
- How would you teach a junior engineer to read top, vmstat, iostat, and journalctl so that they do not make false conclusions?

### 11.3 Docker and Containerization

#### 11.3.1 Dockerfile and best practices
- What is a Dockerfile and why is it needed?
- What main instructions does a Dockerfile consist of?
- In what order are instructions usually written in a Dockerfile and why?
- What does the `FROM` instruction do?
- How is `FROM ubuntu:22.04` different from `FROM ubuntu:latest`?
- Why is it not recommended to blindly use the `latest` tag?
- What does the `RUN` instruction do?
- How is `RUN` different from `CMD`?
- How is `CMD` different from `ENTRYPOINT`?
- When should you use `CMD`, and when `ENTRYPOINT`?
- What happens if both `ENTRYPOINT` and `CMD` are specified in a Dockerfile?
- What does the `COPY` instruction do?
- How is `COPY` different from `ADD`?
- Why is `ADD` usually recommended to be used less often than `COPY`?
- What does the `WORKDIR` instruction do?
- What does the `ENV` instruction do?
- What does the `ARG` instruction do?
- How is `ARG` different from `ENV`?
- Where is an `ARG` variable available, and where is `ENV` available?
- What does the `EXPOSE` instruction do?
- Does `EXPOSE` automatically publish a port externally?
- What does the `USER` instruction do?
- What does the `LABEL` instruction do and why is it useful?
- What does the `VOLUME` instruction do?
- What is build context?
- Why can `COPY . .` be dangerous or inefficient?
- What is `.dockerignore` and why is it needed?
- Which files should usually be added to `.dockerignore`?
- Why is it important to reduce build context?
- How does Docker use cache when building an image?
- What determines cache invalidation in Docker build?
- Why does the order of instructions in a Dockerfile affect build speed?
- Which instructions should be placed earlier and which later to make better use of cache?
- Why are dependency lock files often copied first instead of the entire project?
- What is a layer in a Docker image?
- How are image layers formed?
- Which instructions create new layers?
- Why can a large number of unnecessary layers be a problem?
- When is combining several commands into one `RUN` useful, and when does it hurt readability?
- Why are `&&` and cleanup of temporary files inside a single `RUN` used?
- Why is it important to remove package manager cache, temporary files, and unnecessary artifacts in the same layer?
- What is a multi-stage build?
- Why is a multi-stage build needed?
- In which cases is a multi-stage build especially useful?
- How does a multi-stage build help reduce the final image size?
- How does a multi-stage build help improve image security?
- How do you copy an artifact from one stage to another?
- What are a “builder image” and a “runtime image”?
- Why is it useful to separate the build stage from the runtime stage?
- How do base image, builder image, and runtime image differ?
- How do you choose a base image for an application?
- When should you use a full Linux image, and when slim/alpine/distroless?
- What are the pros and cons of Alpine images?
- Why is Alpine not always the best choice for production?
- What is a distroless image?
- What are the pros and cons of distroless images?
- What is a scratch image and when is it used?
- What is the trade-off between debug convenience and minimal image size?
- Why does Docker image size matter?
- How do you reduce Docker image size?
- What typical mistakes lead to bloated images?
- Why is it undesirable to install dev dependencies in a production image?
- How do you separate dependencies for development and for production?
- What best practices for Node.js Dockerfiles do you know?
- What best practices for Java/Spring Boot Dockerfiles do you know?
- What best practices for Python Dockerfiles do you know?
- What best practices for Go Dockerfiles do you know?
- Why is it important to explicitly specify the version of the base image?
- Why is it useful to pin the image digest, not just the tag?
- What is a reproducible build in the context of Docker?
- How do you achieve reproducible Docker image builds?
- How do you include frontend build and static file serving in a Dockerfile?
- How do you package an SPA application correctly into a container?
- How do you package a backend application correctly into a container?
- How do you run a Java application in a container optimally for memory and startup time?
- How do you package an application so that the container starts quickly?
- Why should a container, where possible, run a single main process?
- What does “one process per container” mean and how strict is this rule?
- When is it acceptable to run more than one process in a container?
- Why does launching with shell-form `CMD "python app.py"` differ from exec-form `CMD ["python", "app.py"]`?
- Why is exec form often preferable to shell form?
- How does signal handling depend on which process is PID 1 in a container?
- What is the PID 1 problem in a container?
- How do you solve the zombie process problem in a container?
- Why are tini or dumb-init used?
- Why is it a bad idea to run a container as root?
- How do you correctly add a non-privileged user in a Dockerfile?
- What problems arise if an application inside a container runs as root?
- How do you configure file permissions when building an image?
- Why is `COPY --chown=...` convenient?
- What is a healthcheck in Docker and is it needed in a Dockerfile?
- When does it make sense to add `HEALTHCHECK`?
- What pitfalls exist with `HEALTHCHECK`?
- Why can an overly heavy healthcheck harm a system?
- How do you store application configuration in a container?
- Should configuration be baked into the image?
- Why should secrets not be baked into a Dockerfile?
- Why must tokens, SSH keys, and passwords not be put into image layers?
- How do you securely pass secrets during the build process?
- What is Docker BuildKit and what capabilities does it provide?
- How does BuildKit help with secrets and caches?
- What is a target stage when building a multi-stage image?
- How do you build only the required stage?
- How do you view the history of Docker image layers?
- How do you understand why an image ended up large?
- How do you analyze the contents of a Docker image?
- Which tools can be used to analyze layer sizes?
- How do you check which vulnerabilities are present in an image?
- How do you build a pipeline for scanning Docker images for security?
- What is an SBOM and why is it needed for containers?
- Why sign container images?
- What is image immutability?
- Why is it not normal practice to “fix” a container manually in production?
- What is the difference between an immutable image and a mutable server approach?
- How do you organize versioning of container images?
- Which image tags do you consider good practice in a team?
- Why is it useful to have both a human-readable tag and an immutable digest?
- How do you add linting and quality standards to Dockerfiles?
- Which Dockerfile anti-patterns occur most often?
- What is bad about installing dependencies without pinning versions?
- What is bad about having many unnecessary packages inside a runtime image?
- What is bad about copying `.git`, `.env`, `node_modules`, test data, or local artifacts into an image?
- How would you write a production-ready Dockerfile for a web application?
- How would you rewrite a bad Dockerfile to make it smaller, safer, and faster to build?
- What would you check in a Dockerfile during code review?
- How would you explain to a junior developer why their Dockerfile is bad even if “everything works”?
- How would you standardize Dockerfile practices in a team?
- Which trade-offs between build speed, image size, security, and debugging convenience do you usually accept?
- How would you build Dockerfiles for a monorepo with several services?
- How do you cache dependencies in CI when building containers?
- How do you reduce Docker image build time in CI/CD?
- When is it justified to use separate base images maintained by your team?
- How would you organize the process of updating base images in a company?
- What risks arise if a base image has not been updated for a long time?
- How do you ensure nothing breaks after updating a base image?
- Which Dockerfile would you consider production-grade for a high-load service and why?
- How would you explain the difference between “the image builds” and “the image is suitable for production”?

#### 11.3.2 Volumes, networking, Docker Compose
- What is a volume in Docker?
- Why are volumes needed?
- How is a volume different from an image layer?
- How is a volume different from a bind mount?
- How is a bind mount different from a tmpfs mount?
- When should you use a bind mount, and when a named volume?
- What are the pros and cons of bind mounts?
- What are the pros and cons of named volumes?
- What are the pros and cons of tmpfs mounts?
- Where are named volumes stored on the host?
- What happens to container data after the container is removed without a volume?
- What happens to data in a volume after the container is removed?
- How do you see the list of volumes?
- How do you delete unused volumes?
- What are dangling volumes?
- How do you attach a volume to a container?
- How do you attach a bind mount to a container?
- What happens if you mount a bind mount over a directory that already contains files from the image?
- Why do files sometimes “disappear” from a container after attaching a volume or bind mount?
- How do permissions work when using a bind mount?
- What file permission problems often occur when using Docker on Linux?
- What filesystem problems occur when using Docker Desktop on macOS/Windows?
- Why are bind mounts often convenient in development but not always good in production?
- When is a volume better than a bind mount for production?
- How do you organize persistent data storage for a database in Docker?
- Why is a database container without a volume a bad idea?
- How do you back up data from a Docker volume?
- How do you restore data into a volume?
- How do you move a volume between hosts?
- How do you inspect what is inside a volume?
- Can the same volume be shared between multiple containers?
- What risks arise if several containers write to the same volume simultaneously?
- What is a read-only mount and when should you use it?
- How do you mount a volume read-only?
- Why use tmpfs for temporary sensitive data?
- How are volumes related to I/O performance?
- What performance problems occur with bind mounts in local development?
- How do you optimize Docker volumes for local development of a frontend/backend project?
- What is a Docker network?
- Why does Docker create separate networks?
- What types of networks in Docker do you know?
- What is a bridge network?
- What is a host network?
- What is a none network?
- When should you use a bridge network?
- When should you use a host network?
- What are the risks of host networking?
- What is an overlay network and where is it used?
- What is a macvlan network?
- How do containers communicate with each other within one Docker network?
- How does a container learn the IP of another container?
- Why is it better to use service names in Docker rather than IP addresses?
- How does Docker’s built-in DNS work?
- How is the default bridge network different from a user-defined bridge network?
- Why is a user-defined network usually preferable to the default bridge?
- How do you connect a container to multiple networks?
- Why would one container be in multiple networks?
- How do you isolate groups of containers from each other using networks?
- What happens when publishing a port with `-p 8080:80`?
- How is `EXPOSE` different from `-p`?
- What is the difference between `127.0.0.1:8080:80` and `8080:80` when publishing a port?
- Why can publishing a port on `0.0.0.0` be unsafe?
- How do you check which ports a container publishes externally?
- How do you debug a situation where a container “works” but the port is not open from outside?
- How do you debug a situation where one container cannot reach another container?
- What DNS problems can occur inside a Docker network?
- How do you check network connectivity between containers?
- Which tools do you use for network diagnostics inside containers?
- What is Docker Compose?
- Why is Docker Compose needed?
- For which scenarios is Compose the best fit?
- For which scenarios is Compose not suitable?
- How is Docker Compose especially convenient in local development?
- How is Docker Compose different from Docker Swarm and Kubernetes?
- What is `docker-compose.yml` / `compose.yaml`?
- Which main sections usually exist in a Compose file?
- What is a service in Docker Compose?
- How do you describe several services in one Compose file?
- How do you connect backend, frontend, database, and Redis using Docker Compose?
- Is it necessary to explicitly specify `depends_on`, and what does it do?
- Does `depends_on` guarantee that a dependent service is fully ready to work?
- How do you correctly organize startup if the database is not yet ready to accept connections?
- What is a healthcheck in Compose and how is it used?
- How do you make one service wait for another to become ready?
- How do you set environment variables in Docker Compose?
- What is `.env` in the context of Docker Compose?
- How is `.env` for Compose substitution different from `env_file` for a container?
- How do you securely pass secrets in Docker Compose?
- Why should you not commit real secrets into a compose file or Git?
- How do you attach volumes in Docker Compose?
- How do you attach bind mounts in Docker Compose?
- How do you declare named volumes in Compose?
- How do you declare networks in Compose?
- When do you need to explicitly declare networks in Compose, and when can you use the default one?
- How do you restrict access between services in Compose using different networks?
- What are profiles in Docker Compose?
- When is it convenient to use profiles?
- How do you start only part of the services in a Compose configuration?
- How does override work in Docker Compose?
- Why is `docker-compose.override.yml` used?
- How do you separate configurations for dev, test, and prod in Docker Compose?
- Why is Docker Compose rarely used as a production orchestrator?
- What anti-patterns occur when using Docker Compose in production?
- How do you organize hot reload for a backend or frontend in Compose?
- What problems can arise when mounting source code into a container with hot reload?
- How do you organize local development of a Node.js/Java/Python application via Compose?
- How do you organize e2e tests through Docker Compose?
- How do you start a test database and dependencies for integration tests via Compose?
- How do you use Compose in CI?
- What limitations does Compose have in CI/CD?
- Can Compose be used for smoke tests after a build?
- How do you collect logs from several services in Compose?
- How do you view logs for a specific service in Compose?
- How do you rebuild and restart only one service in Compose?
- How do you remove containers, networks, and volumes after working with Compose?
- What does `docker compose down -v` do?
- What does `docker compose up --build` do?
- What does `docker compose up -d` do?
- How do you avoid situations where “old” volumes or networks remain in Compose?
- Which best practices do you use for Compose files in a team?
- How do you make a compose configuration readable and maintainable?
- What trade-offs exist between simplicity, convenience, and closeness to production when using Compose?
- How would you design docker-compose for a microservice system of 5–10 services?
- How would you explain to a junior developer the difference between volumes, bind mounts, networks, and Compose using a simple example?
- What do you check during code review for `compose.yaml`?
- When does Docker Compose become insufficient and it is time to move to Kubernetes or another orchestrator?
- How do you organize a local environment so that it is close to production but not too heavy for a developer?
- How do you organize database usage in Compose so that migrations, seeds, and the application start predictably?
- What common problems do developers run into with Docker networking, and how do you usually diagnose them?
- How would you design a Compose environment for a fullstack project with frontend, backend, PostgreSQL, Redis, Nginx, and a worker service?

#### 11.3.3 Container security
- What does container security mean in general?
- Why does containerization by itself not make an application secure?
- What are the main classes of risks in containers?
- How is container security different from virtual machine security?
- How do containers isolate processes from each other?
- Which Linux kernel mechanisms provide container isolation?
- What are namespaces and what role do they play in container security?
- What are cgroups and how are they related to security?
- What are Linux capabilities?
- Why is running a container as root a risk?
- How is root inside a container different from root on the host?
- Why is root inside a container still dangerous even though “it’s not the host”?
- How do you run a container as a non-privileged user?
- How do you check which user a process is running as inside a container?
- What are rootless containers?
- What are the pros and cons of rootless Docker / rootless containers?
- What is the principle of least privilege as applied to containers?
- Which capabilities can usually be dropped from a container?
- What are `--cap-drop` and `--cap-add`?
- Why is it dangerous to give a container extra capabilities?
- What is a privileged container?
- Why is `--privileged` almost always a bad idea?
- In which rare cases might a privileged container be justified?
- What is `no-new-privileges` and why is it needed?
- What is a seccomp profile?
- How does seccomp help protect a container?
- What are AppArmor and SELinux in the context of containers?
- How do MAC mechanisms help restrict a container?
- What is user namespace remapping?
- How do user namespaces reduce the risk of container escape?
- What is container escape?
- What typical causes of container escape do you know?
- How do you reduce the risk of container escape?
- Why is it undesirable to mount the Docker socket inside a container?
- Why is `/var/run/docker.sock` one of the most dangerous mounts?
- Why is it dangerous to mount the host filesystem into a container?
- What risks are associated with bind mounting sensitive host directories?
- How do you protect a container from excessive filesystem access?
- What is a read-only root filesystem?
- When does it make sense to run a container with a read-only filesystem?
- What difficulties arise when running a container with a read-only root filesystem?
- What should an application do if it needs temporary files with a read-only root FS?
- How do you restrict container network access?
- Why should not every container have outbound internet access?
- How do you segment containers by networks?
- Why does publishing unnecessary ports increase risk?
- Which best practices for port publishing do you use?
- How do you minimize a container’s attack surface?
- Why is image size related not only to performance but also to security?
- How does reducing the number of packages in an image improve security?
- What is the attack surface of an image?
- Why can extra shells, package managers, and utilities in a runtime image be a problem?
- What is a distroless image from a security perspective?
- What are the pros and cons of distroless images for security?
- What risks arise from using an outdated base image?
- How often should base images be updated?
- How do you track CVEs in container images?
- Which image scanning tools do you know?
- At which stage of the pipeline should images be scanned for vulnerabilities?
- What should you do if a scanner finds CVEs in a base image?
- How do you distinguish truly dangerous vulnerabilities from noise in scanner reports?
- What is software supply chain security in the context of containers?
- What risks come with using unverified images from public registries?
- Why should you not blindly use random community images from Docker Hub?
- How do you choose a trusted base image?
- What is image provenance?
- What is a container image signature?
- Why sign images?
- What is an SBOM and how does it help security?
- How do you verify that an image has not been tampered with?
- Which admission control and policy enforcement practices do you know for containers?
- What is policy-as-code in container security?
- Which rules would you enforce for containers in a company?
- Can secrets be stored inside an image?
- Why is baking secrets into an image a bad idea?
- How do you securely pass secrets to containers at runtime?
- What risks do environment variables pose as a way of passing secrets?
- Where can secrets leak if they are passed through environment variables?
- When is it better to mount secrets as files rather than as environment variables?
- How do you protect container logs from leaking secrets?
- What measures are needed so that PII or keys do not end up in stdout/stderr?
- How do you secure an image registry?
- Why is a registry a critically important part of the supply chain?
- Which authentication and authorization mechanisms are important for a registry?
- How do you organize access control to images?
- What risks does using `latest` introduce from a security and change-control perspective?
- Why are immutable tags useful for security?
- What is runtime security for containers?
- Which runtime events would you want to monitor in a container environment?
- Which runtime anomalies can indicate container compromise?
- What is Falco and what is it used for?
- How do you monitor suspicious system calls from containers?
- How do you restrict container resources to reduce damage during an attack?
- How are CPU/memory limits related to security and resilience?
- Why are resilience and container security partly related?
- How do you protect against container breakout via kernel vulnerabilities?
- Why is it important to update not only the image, but also the host kernel?
- Who is responsible for container security: the developer, DevOps, security engineer, or platform team?
- How would you integrate container security into the SDLC?
- What would you check in a Dockerfile during code review from a security perspective?
- What would you check in a CI/CD pipeline for containers from a security perspective?
- What minimum requirements for a production-ready container would you define in a company?
- Which security baseline checks should all containers pass?
- How would you introduce container security in a team where developers resist restrictions?
- Which container security anti-patterns have you most often seen in practice?
- What is worse: a large insecure image, a privileged container, or leaked secrets in an image? How would you prioritize them?
- How would you respond if you learned that in production containers are running as root, with the Docker socket mounted, and without image scanning?
- How would you explain to a developer why “it works on my machine” does not mean “it is safe for production”?
- How would you design a container platform with a secure-by-default approach?
- Which trade-offs between security, developer convenience, and delivery speed do you consider acceptable?
- How do you assess the maturity of container security in a company?
- What would you include in a container security checklist for interviewing a senior / lead / platform engineer?

### 11.4 CI / CD

#### 11.4.1 Pipelines: build, test, lint, security
- What is a CI/CD pipeline?
- Why is a pipeline needed in development at all?
- Which stages are usually included in a CI pipeline?
- Which stages are usually included in a CD pipeline?
- What is the difference between continuous delivery and continuous deployment?
- What should run on every commit, and what only before a release?
- Which checks do you consider mandatory in any pipeline?
- Where would you start when building a pipeline for a new project?
- What are the goals of a good CI/CD pipeline?
- What properties should a high-quality pipeline have?
- What is a build stage and what does it usually include?
- What is a test stage and which checks are usually run there?
- What is a lint stage and why is it needed?
- What is a security stage and what is usually included there?
- How is build different from release?
- How is an application pipeline different from an infrastructure pipeline?
- How is a pipeline related to trunk-based development, GitFlow, and feature branches?
- Which artifacts should a pipeline produce?
- What is a reproducible build and why is it important?
- Why is it important for a build to be deterministic?
- What is an idempotent pipeline and why is it useful?
- Why should a pipeline be fast?
- How do you know that a pipeline has become too slow?
- Which pipeline metrics do you track?
- What are lead time, cycle time, deployment frequency, and change failure rate?
- How do you reduce pipeline execution time?
- Which checks would you run in parallel?
- Which checks cannot be safely parallelized?
- How would you organize dependencies between jobs in a pipeline?
- How do you split a pipeline into fast feedback and slow feedback?
- Which checks should run locally before a push?
- Which checks should be mandatory on a pull request / merge request?
- Which checks should be mandatory before deployment to production?
- What do you do if the pipeline often fails for flaky reasons?
- What are flaky tests and how do they harm a pipeline?
- How do you deal with flaky tests in CI?
- How would you isolate flaky tests without breaking the whole pipeline?
- Which tests should be run in CI, and which should not?
- In what order is it better to run unit, integration, contract, e2e, and security tests?
- What is the test pyramid and how does it affect pipeline design?
- What do you do if e2e tests are too slow and unstable?
- How do you choose the balance between pipeline speed and depth of checks?
- Where is the boundary between checks in CI and checks in staging?
- How do you organize a pipeline for a monorepo?
- How do you organize a pipeline for a polyrepo?
- How do you determine which parts of a monorepo need to be rebuilt?
- What is an incremental build and when is it useful?
- What is a cached build and what risks does cache create in CI?
- What can and cannot be cached in a pipeline?
- When does CI cache start doing more harm than good?
- How do you invalidate cache in a CI/CD pipeline?
- What is artifact caching and how is it different from dependency caching?
- How do you speed up dependency installation in CI?
- How do you ensure the same pipeline execution environment for all developers?
- Why is containerization useful for CI?
- When does it make sense to run a pipeline inside Docker containers?
- What risks are there with shared runners?
- What are the pros and cons of self-hosted runners?
- How are ephemeral runners different from persistent runners?
- How do you choose between managed CI and self-hosted CI?
- How do you organize a pipeline for a backend application?
- How do you organize a pipeline for a frontend application?
- How do you organize a pipeline for a mobile application?
- How do you organize a pipeline for a microservice system?
- How do you organize a pipeline for a library or SDK?
- What are the specifics of a pipeline for infrastructure code?
- What are the specifics of a pipeline for data/ML projects?
- What is a quality gate?
- Which quality gates would you set for production deployment?
- What should happen if a quality gate is not passed?
- Can manual override of a quality gate be allowed? When?
- How do you implement branch protection policy through a pipeline?
- How is a pipeline related to code owners and required approvals?
- How do you organize checks for commits, conventional commits, and commit message policy?
- Is it necessary to check code formatting in CI if there are pre-commit hooks?
- Why do you need linting if the code already compiles?
- How is a linter different from a static analyzer?
- What does static analysis usually check in a pipeline?
- Which SAST tools have you used?
- Which types of security checks would you add to a pipeline?
- What is SAST?
- What is DAST?
- What is SCA?
- What is container image scanning?
- What is IaC scanning?
- What is secret scanning?
- What is the difference between dependency vulnerability scanning and container image scanning?
- Where in the pipeline is it best to check dependency vulnerabilities?
- What do you do if a security scanner constantly finds low severity vulnerabilities?
- Which security findings should block a release?
- How do you work with false positives in security scanning?
- What do you do if the business requires shipping a release despite a security issue?
- How do you integrate secret scanning into a pipeline?
- What do you do if a secret has already leaked into Git history?
- How do you verify artifact signatures and build provenance?
- What is an SBOM and why is it needed?
- What is software supply chain security?
- How do you protect a pipeline from dependency substitution?
- How do you protect a pipeline from executing untrusted code from forked repositories?
- What risks exist when running a pipeline on a pull request from a public repository?
- How would you restrict pipeline access to secrets?
- Why should you not blindly pass all secrets into all jobs?
- How do you minimize blast radius if a runner is compromised?
- How do you organize separate secrets for dev, stage, and prod?
- How would you store secrets for a pipeline?
- Why are long-lived credentials in CI/CD bad?
- How do you use short-lived credentials in a pipeline?
- What is OIDC federation for CI/CD and why is it needed?
- How do you securely give a pipeline access to the cloud?
- How do you check who ran a pipeline and when?
- Which audit trail data is important to retain for a pipeline?
- How do you organize manual approval stages?
- When is a manual step needed in a pipeline, and when does it get in the way?
- What do you think about manual approvals before production?
- How do you organize a pipeline so that developers get fast and useful feedback?
- Which pipeline error messages do you consider good?
- What should a developer see when a pipeline fails?
- How do you make a pipeline understandable to a new team?
- How do you document a pipeline?
- How would you debug a failing pipeline?
- What typical causes of pipeline failures have you encountered?
- What do you do if a pipeline passes locally but fails in CI?
- What do you do if a pipeline depends on an unstable external service?
- How do you mock external dependencies in CI?
- When is it worth bringing up an ephemeral test environment in a pipeline?
- What are preview environments and why are they needed?
- When are preview environments justified, and when are they too expensive?
- How do you organize database migrations inside a pipeline?
- Should migrations be executed before or after application deployment?
- How do you avoid breaking a pipeline because of incompatible database migrations?
- How do you test backward compatibility of migrations in CI?
- How do you organize a pipeline for zero-downtime database schema changes?
- What is contract testing and where does it fit in the pipeline?
- How do you organize consumer-driven contract testing in CI?
- Which smoke tests should run immediately after deployment?
- How are smoke tests different from sanity tests?
- Should performance tests be run in every pipeline?
- Where is it better to run load tests?
- How do you integrate performance regression checks into a pipeline?
- What problems arise when running integration tests in parallel?
- How would you organize test databases in CI?
- What are Testcontainers and why are they useful in CI?
- When is it better to use service containers, and when full environment provisioning steps?
- How do you organize a pipeline for multi-stage deployment: dev → stage → prod?
- How do you ensure promotion of the same artifact between environments?
- Why is it important to promote the same artifact rather than rebuild it in each environment?
- How does a pipeline help enforce engineering standards?
- What would you do if the team starts frequently disabling checks for speed?
- How does a pipeline evolve as the team and product grow?
- How would you design a pipeline for a high-compliance environment?
- What additional checks are needed in regulated environments?
- How do you organize a rollback path already at the pipeline design stage?
- How should a pipeline work for hotfix releases?
- How do you organize release branches and their pipelines?
- Is it necessary to have different pipelines for feature branches, main, and release branches?
- Which CI/CD anti-patterns have you encountered?
- Which is worse: a very slow pipeline or a very fast but superficial one?
- What would you automate in the pipeline first?
- What would you avoid automating too early?
- How does a pipeline affect developer experience?
- How do you convince a team or management to invest time in improving CI/CD?

#### 11.4.2 Blue-green, canary, rollbacks
- What is a blue-green deployment strategy?
- What is canary deployment?
- What is the difference between blue-green and canary?
- When is it better to choose blue-green, and when canary?
- What is rolling deployment and how is it different from blue-green and canary?
- What is recreate deployment and when is it acceptable at all?
- What goals do deployment strategies pursue?
- How is a deployment strategy related to resilience and release speed?
- How does a deployment strategy affect incident risk?
- How do you choose a release strategy for a stateless service?
- How do you choose a release strategy for a stateful service?
- What complexities arise with blue-green deployment of an application with a database?
- What complexities arise with canary for a service with user sessions?
- What is traffic shifting?
- How do you technically implement traffic switching in blue-green?
- How do you technically implement a canary rollout?
- At which layer is traffic splitting usually managed: load balancer, ingress, service mesh, or application?
- Which metrics should you watch during a canary rollout?
- Which alerts should automatically stop a rollout?
- What is automated rollback?
- When should rollback be automatic, and when manual?
- Which signals are sufficient grounds for a rollback?
- What are the risks of automatic rollback?
- What are the risks of manual rollback?
- What is a rollback in general?
- How is rollback different from roll forward?
- When is it better to roll back, and when is it better to quickly fix forward?
- What is a roll forward strategy?
- Why is rollback not always possible?
- Which changes are hardest to roll back?
- Why do database schema changes often make rollback difficult?
- How do you design DB migrations so that a release can be safely rolled back?
- What is a backward-compatible migration?
- What is the expand-and-contract pattern?
- How do you implement zero-downtime rollout for incompatible API changes?
- How do you release a change when old and new code need to work simultaneously for a while?
- What do you do if a new version of a service is incompatible with the old one at the message or contract level?
- How do you perform canary for asynchronous consumers?
- Can you do canary for batch jobs and cron jobs? How?
- How do you do canary for a frontend application?
- How is canary for backend different from canary for frontend?
- How are feature flags and canary deployment related?
- When are feature flags more useful than a complex deployment strategy?
- Can you release via feature flags without a new deployment?
- How does blue-green work together with feature flags?
- What is a dark launch?
- How is a dark launch different from a canary?
- What is progressive delivery?
- How is progressive delivery different from just CI/CD?
- Which metrics are important for analyzing rollout success?
- What would you watch in the first minutes after a release?
- Which metrics are important for a backend service?
- Which metrics are important for a frontend during canary?
- Which business metrics may signal a release problem?
- How long should you keep a canary before a full rollout?
- What determines the size of the canary audience?
- How do you choose the percentage of traffic for the first canary wave?
- By what steps would you increase the canary: 1% → 5% → 25% → 50% → 100%, or differently?
- What do you do if everything is fine at 5%, but problems start at 50%?
- Which problems can appear only at a high percentage of traffic?
- How does canary help identify performance regressions?
- Which problems will canary not detect?
- What role do synthetic tests play after deployment?
- What are smoke tests after rollout?
- What should be checked immediately after switching blue-green traffic?
- How do you make sure the green environment is actually ready to accept production traffic?
- Why are readiness checks needed before traffic switching?
- How do you avoid a situation where traffic has already been switched but the application is not yet warmed up?
- Is it necessary to warm up the cache before switching traffic?
- How do you roll out interdependent services?
- Which version should be rolled out first if you have several interdependent services?
- How do you roll out breaking changes in a distributed system?
- How do you coordinate the release of several services?
- Which anti-patterns have you seen in rollout strategies?
- Why is a “big bang deploy” dangerous?
- In which cases is blue-green too expensive?
- In which cases is canary too complex?
- How can blue-green be dangerous when there is strong coupling to state?
- How does blue-green affect infrastructure cost?
- How does canary affect observability complexity?
- What additional observability requirements appear with canary rollout?
- How do you segment users for canary?
- Should users for canary be selected randomly or from certain segments?
- Which users are suitable for early rollout waves?
- Can you first roll out to an internal audience? What are the pros and cons?
- How is rollout related to blast radius?
- How do you reduce the blast radius of a release?
- How do you organize a kill switch?
- What is an emergency rollback plan?
- What should be prepared before a production rollout begins?
- Which runbooks are needed for a safe release?
- Who should have the authority to trigger a rollback?
- Who decides whether to continue a rollout or stop it?
- Which roles participate in a release and rollback?
- How do you document the progress of a rollout?
- How do you analyze a failed rollout after an incident?
- What should go into a postmortem after a failed release?
- How would you automate the rollback decision?
- Can rollout decisions be fully trusted to automation?
- Which error thresholds would you use for rollback?
- How does rollout behave during an active degradation of an external provider?
- How do you distinguish a release problem from an external degradation?
- What do you do if there is already an active incident in the system at rollout time?
- Should you continue rollout during platform instability?
- How would you organize safe deployment windows?
- Is it necessary to prohibit releases during certain hours or days?
- How do you roll out a hotfix if another release is already in canary?
- What do you do if you need to roll back and urgently release a fix at the same time?
- How would you explain the difference between blue-green, canary, and rolling to a non-technical manager?
- Which release strategy would you recommend to a startup?
- Which release strategy would you recommend to a bank or a high-risk product?
- Which release strategy would you recommend for an internal corporate tool?
- How does deployment strategy change as a company and user base grow?

#### 11.4.3 Versioning, releases, artifacts
- What is versioning in the context of CI/CD?
- Why is it necessary to version artifacts and releases at all?
- What is a release?
- How is a release different from a deployment?
- How is a release version different from a build number?
- What is an artifact?
- Which types of artifacts exist in a pipeline?
- Which artifacts are usually preserved after a build?
- Why is it important to store artifacts instead of rebuilding them every time?
- What is an immutable artifact?
- Why should artifacts be immutable?
- What are the requirements for a good artifact repository?
- Which artifact storage systems have you used?
- What is an artifact repository and how is it different from a package registry?
- What is a Docker registry?
- What is a package registry for npm/Maven/PyPI and how does it fit into the release process?
- How is versioning related to reproducibility and traceability?
- How do you understand from which commit a specific artifact was built?
- What metadata would you attach to an artifact?
- Should artifacts be signed? Why?
- How should artifact provenance be stored?
- What is semantic versioning?
- What are the SemVer rules?
- When is a change considered major, minor, or patch?
- Is semantic versioning always applicable?
- When does SemVer start to lie?
- How does versioning work for internal services rather than public libraries?
- Should internal microservices be versioned as strictly as external SDKs?
- How should Docker images be versioned?
- Why is the latest tag considered problematic?
- Which image tags would you use in production?
- Is it acceptable to deploy an image by a mutable tag?
- Why is it better to deploy an image by digest rather than by tag?
- How is versioning related to rollback?
- Why does poor versioning make rollback harder?
- What is a release candidate?
- What are alpha, beta, and RC?
- When does it make sense to release pre-release versions?
- What is a release train?
- What is a release branch?
- Are release branches needed? When yes, and when not?
- How do you organize releases in trunk-based development?
- How do you organize versioning in GitFlow?
- What are annotated tags and lightweight tags in git?
- Should releases be marked with git tags?
- What should be included in release notes?
- Who should prepare release notes?
- How can release notes be generated automatically?
- How do you connect conventional commits and changelog generation?
- What is a changelog and how is it different from release notes?
- How often should releases be made?
- What is better: small frequent releases or rare large ones?
- How do you choose a release cadence?
- What is release orchestration?
- How do you coordinate the release of multiple services?
- How does versioning work in a monorepo?
- How does versioning work in a polyrepo?
- What are independent versioning and lockstep versioning?
- When is a shared version better for a monorepo, and when are independent package versions better?
- How should APIs be versioned?
- Is a versioning strategy needed for REST APIs?
- When should an API be versioned in the URL, and when through headers or content negotiation?
- Should a GraphQL API be versioned?
- How do you determine that an API change is a breaking change?
- How do you maintain backward compatibility between releases?
- How is versioning related to database migrations?
- How do you ensure compatibility between a new application version and an old database schema?
- How is versioning related to message contracts in Kafka/RabbitMQ?
- How should message schemas be versioned?
- What is a schema registry and why is it needed?
- How do you ensure backward/forward compatibility for events and messages?
- How is versioning related to protobuf/avro/json schema?
- How do versioning strategies differ for libraries versus applications?
- Should frontend assets be versioned?
- How is cache busting related to versioning of frontend artifacts?
- How is versioning related to CDN and old clients?
- How do you release mobile applications if client versions live in production for a long time?
- How do you support multiple client versions simultaneously?
- What is artifact promotion?
- How do you promote the same artifact between dev, stage, and prod?
- Why is rebuilding an artifact in every environment considered a bad practice?
- What is a release candidate artifact?
- What is a signed release?
- How do you track which release is currently installed in production?
- How do you quickly understand which version broke the system?
- How would you organize a release dashboard?
- What data should be on a release dashboard?
- What is build metadata?
- How do you use commit SHA, build number, and build date in a version?
- What is CalVer and when is it appropriate?
- How is CalVer different from SemVer?
- When does it make sense to use a date instead of a semantic version?
- What is a snapshot version?
- When is it acceptable to use snapshot versions?
- Why are snapshot versions dangerous for production?
- How do you manage dependencies between versions of services and libraries?
- What is dependency locking?
- How do you avoid “dependency hell” at the release level?
- How do you release a shared library used by dozens of services?
- How is rolling out a shared library different from rolling out a service?
- How does versioning affect API consumers?
- How do you communicate breaking changes to clients or other teams?
- What is a deprecation policy?
- How long should a deprecated API be supported?
- How do you safely remove an old API version?
- How is versioning related to release governance in a large company?
- Who should approve a production release?
- Which checks must be passed before creating a release?
- Should the main branch be frozen before a release?
- What is a code freeze and when is it useful?
- What is a release freeze and when is it justified?
- What are typical mistakes in release management?
- Why is a manual release process bad?
- Which parts of the release process should definitely be automated?
- Which parts should not be automated without preparation?
- How should versioning and the release process be organized in a high-compliance environment?
- Which artifacts and metadata need to be stored for audit?
- How long should old artifacts be retained?
- When and how should artifact repositories be cleaned?
- What are the risks of uncontrolled artifact accumulation?
- What do you do if a critical bug is found in an already released version?
- How would you package a hotfix release?
- How do you organize an emergency release process?
- How do you build a release process so that the team is not afraid of releases?
- How would you explain the difference between versioning, release management, and deployment management to a non-technical person?

### 11.5 GitOps
- What is GitOps?
- What is the main idea behind GitOps?
- Why is GitOps called GitOps?
- How is GitOps different from the traditional deployment approach?
- What role does Git play in the GitOps approach?
- What is considered the source of truth in GitOps?
- What problems does GitOps solve?
- For which types of infrastructure and applications is GitOps best suited?
- When is GitOps not very convenient or overkill?
- What is the difference between GitOps and Infrastructure as Code?
- What is the difference between GitOps and regular CI/CD?
- Where does CI end and GitOps begin?
- How is GitOps different from the imperative approach to infrastructure management?
- Why is the declarative approach better than the imperative one in the context of GitOps?
- What key GitOps principles would you name?
- What does “desired state” mean in GitOps?
- What does “actual state” mean in GitOps?
- How does GitOps determine drift between desired state and actual state?
- What is the reconciliation loop in GitOps?
- What does a GitOps operator do in a cluster?
- How does GitOps ensure environment reproducibility?
- How does GitOps help with change auditing?
- Why does GitOps simplify rollback?
- Why can GitOps improve change security?
- How is GitOps related to idempotency?
- What are the main benefits of GitOps for a development team?
- What are the benefits of GitOps for an SRE / platform team?
- What are the benefits of GitOps for audit and compliance?
- What are the disadvantages and limitations of the GitOps approach?
- What typical challenges arise when adopting GitOps?
- What risks appear if Git becomes the only source of truth?
- What types of changes fit well into GitOps?
- What types of changes fit poorly into GitOps?
- Is GitOps suitable only for Kubernetes?
- Can GitOps be applied outside Kubernetes?
- Which GitOps tools do you know?
- What is the difference between Argo CD and Flux?
- What tasks does Argo CD solve?
- What tasks does Flux solve?
- How would you choose between Argo CD and Flux for a new team?
- What are push-based and pull-based deployment approaches?
- Why is GitOps more often associated with the pull-based model?
- What are the advantages of the pull-based approach compared to the push-based one?
- What risks are associated with push-based deployment directly from CI into the cluster?
- How does a GitOps agent get access to the cluster and the Git repository?
- Why is CI usually not given direct access to the production cluster in GitOps?
- What does a typical GitOps pipeline look like from commit to production rollout?
- What does the lifecycle of a change look like in GitOps?
- Who initiates the application of changes in GitOps?
- What happens after a merge into main/master in the GitOps model?
- What happens if someone manually changes a resource in the cluster?
- How does GitOps react to drift?
- What is drift detection?
- How would you explain drift detection to a non-technical manager?
- What happens if actual state cannot be reconciled to desired state?
- How does GitOps behave when there is an invalid manifest in the repository?
- How does GitOps behave when changes are only partially applied successfully?
- How does GitOps behave with conflicting changes across multiple repositories?
- How do you organize rollback in GitOps?
- How is rollback via revert in Git different from rollback via kubectl rollout undo?
- When is it better to revert a commit, and when to do a hotfix with a new commit?
- How does GitOps affect change management in a company?
- How does GitOps help perform peer review of infrastructure changes?
- Why does GitOps work well with a pull request workflow?
- How does code review affect the quality of the GitOps process?
- Which branches and branching strategy are best suited for GitOps?
- Is GitFlow needed for GitOps, or is a trunk-based approach better?
- How do you organize promotion of changes between dev, stage, and prod in GitOps?
- What is better for promotion: separate branches, separate folders, or separate repositories?
- How would you structure a repository for GitOps?
- When should you use a monorepo for GitOps?
- When should you use a multi-repo approach for GitOps?
- What are the pros and cons of a monorepo in GitOps?
- What are the pros and cons of multi-repo in GitOps?
- How do you separate application manifests from platform/infrastructure manifests?
- Should Helm charts and values be stored in the same repository?
- How do you organize GitOps for multiple environments?
- How do you organize GitOps for multiple clusters?
- How do you organize GitOps for multiple regions or availability zones?
- How do you organize GitOps for a multi-tenant Kubernetes cluster?
- How do you organize GitOps for many microservices?
- How does GitOps scale to hundreds of services?
- Which naming conventions are useful in GitOps repositories?
- What directory structure would you propose for a GitOps repository?
- How do you store Kubernetes manifests in GitOps?
- How does GitOps work with Helm?
- How does GitOps work with Kustomize?
- When is it better to use Helm, and when Kustomize, in GitOps?
- What problems arise when using Helm in GitOps?
- What problems arise when using Kustomize in GitOps?
- What is a Helm release in the context of GitOps?
- Should generated manifests be committed to the repository?
- What is better to store in GitOps: templates or ready-made manifests?
- How do you ensure readability and maintainability of GitOps manifests?
- How do you validate changes in GitOps before applying them?
- Which checks should be run in CI for a GitOps repository?
- Should Kubernetes manifests be linted?
- How do you test GitOps configurations before applying them to a cluster?
- How do you verify that a GitOps change will not break production?
- How would you organize preview/ephemeral environments in GitOps?
- How does GitOps work with policy-as-code?
- Which policy engines do you know for the GitOps process?
- How do you restrict unsafe changes through policy-as-code?
- How does GitOps work with admission controllers?
- How does GitOps help enforce platform standards?
- How does GitOps fit into platform engineering?
- How do you use GitOps in a self-service platform for developers?
- How do you give developers autonomy without losing platform control?
- How do you split responsibility between the platform team and product teams in GitOps?
- How do you use GitOps in an organization with strong compliance requirements?
- Which audit artifacts does GitOps provide?
- How do you track who changed the production configuration and when?
- How do you prove to an auditor that only reviewed changes were rolled out?
- How do you use GitOps together with a change approval process?
- How do you organize approvals for production changes in GitOps?
- Where is it better to implement approvals in GitOps: in the Git platform, in CI, or in the CD tool itself?
- What do you do if a change is urgent, but the formal GitOps process is too slow?
- How do you organize an emergency change in GitOps?
- How do you correctly “bring back” a manual emergency fix into the GitOps process afterward?
- Why are manual production edits dangerous for GitOps?
- What is the “break glass” approach, and how does it work with GitOps?
- How does GitOps work with secrets?
- Why is storing secrets in Git a problem?
- Which approaches to storing secrets in GitOps do you know?
- What are sealed secrets?
- What is SOPS and how is it used in GitOps?
- How do you integrate Vault with GitOps?
- What is better: encrypted secrets in Git or an external secret manager?
- What are the pros and cons of sealed secrets?
- What are the pros and cons of an external secret manager in GitOps?
- How do you rotate secrets in a GitOps process?
- How does GitOps work with certificates and TLS secrets?
- How do you prevent secret leakage through pull requests and CI logs?
- How does GitOps work with container images?
- How do you update the Docker image version in GitOps?
- What is better: update the image tag manually or automate image updates?
- Which tools for automatically updating image tags do you know?
- What risks are associated with automatic image updates in production?
- Should mutable tags like latest be used in GitOps?
- Why are immutable tags or digests preferable in GitOps?
- How does GitOps work with release management?
- How do you organize release versioning in GitOps?
- How do you separate an application release from a config change in GitOps?
- What should you do if application and infrastructure changes need to be rolled out in a coordinated manner?
- How does GitOps help in disaster recovery?
- How do you restore a cluster with GitOps after a complete loss?
- What besides the Git repository is needed for full environment recovery?
- What are the limitations of GitOps in disaster recovery scenarios?
- How do you use GitOps for bootstrapping a new cluster?
- What is cluster bootstrapping in GitOps?
- How do you automate cluster bootstrap through GitOps?
- How do you organize GitOps for managed Kubernetes and on-prem clusters?
- How does GitOps work together with Terraform?
- What is better to manage with Terraform, and what with a GitOps operator inside the cluster?
- Where is the boundary between IaC and GitOps in a real project?
- Can Terraform be used in a GitOps model?
- How do you structure the process if Terraform creates the cluster and GitOps manages the applications?
- How do you avoid conflicts between Terraform and GitOps when managing the same resources?
- Which Kubernetes resources should not be managed simultaneously by both Terraform and a GitOps tool?
- How does GitOps work with Argo Rollouts or progressive delivery?
- How do you implement canary deployment in GitOps?
- How do you implement blue-green deployment in GitOps?
- How do you implement feature flag rollout alongside GitOps?
- How does GitOps work with a service mesh?
- How do you use GitOps to manage Istio/Linkerd configuration?
- What risks are associated with GitOps when managing network policies and ingress rules?
- How do you use GitOps for platform-wide components such as ingress controller, cert-manager, monitoring stack?
- How do you separate app-level and cluster-level changes in GitOps?
- What permissions should a GitOps operator have in the cluster?
- Should a GitOps tool be given cluster-admin?
- How do you minimize the privileges of GitOps components?
- How do you implement RBAC for teams in a GitOps model?
- How do you isolate teams from each other in a shared cluster under GitOps?
- What supply chain risks exist in GitOps?
- What happens if an attacker gains access to the GitOps repository?
- What happens if an attacker gains access to the GitOps operator in the cluster?
- How do you protect the GitOps repository?
- How do you protect the GitOps process from a malicious commit?
- Why should commits or tags be signed in GitOps?
- Should branch protection be used for GitOps repositories?
- Which security measures would you introduce for a production GitOps workflow?
- Which GitOps process metrics would you track?
- How do you know that the GitOps process is working stably?
- Which alerts are useful for a GitOps system?
- What should be monitored in Argo CD / Flux?
- How do you track lag between merge and actual application of changes?
- How do you measure lead time for change in a GitOps model?
- How do you measure deployment frequency and rollback rate in GitOps?
- What typical incidents occur in GitOps systems?
- How do you diagnose why an application is not syncing in GitOps?
- What do you do if GitOps shows OutOfSync, but the application works fine?
- What do you do if GitOps shows Sync, but the application is actually not working?
- How do you debug reconciliation issues?
- How do you debug GitOps operator access problems to the Git repository?
- How do you debug GitOps operator access problems to the cluster API?
- How do you resolve conflicts between manual changes and GitOps reconciliation?
- What anti-patterns in GitOps do you know?
- Why does “kubectl edit in prod” fit poorly with GitOps?
- Why can a giant monorepo with thousands of unrelated changes be a problem?
- Why is storing secrets in plaintext in Git a bad idea?
- Why does the absence of review and protection rules undermine the meaning of GitOps?
- Why does mixing infrastructure, applications, and secrets without structure make GitOps harder?
- What organizational changes does a team need for a successful transition to GitOps?
- How do you train developers in the GitOps approach?
- How do you convince a team to move to GitOps?
- How do you explain the business value of GitOps?
- In which teams does GitOps provide the greatest effect?
- When can GitOps slow down a team?
- How would you introduce GitOps in a company that already has mature CI/CD without GitOps?
- Where would you start adopting GitOps in a new company?
- What minimal GitOps MVP would you launch first?
- What migration steps to GitOps would you propose for an existing Kubernetes cluster?
- How do you migrate to GitOps without stopping current releases?
- How do you migrate to GitOps if there are already many manual changes in the cluster?
- How do you inventory the current state before introducing GitOps?
- What questions would you ask a company before choosing a GitOps tool?
- Which criteria do you consider the main ones when choosing a GitOps tool?
- How would you compare the GitOps approach for a startup and for a large enterprise company?
- How would you design GitOps for a highly regulated industry?
- How would you design GitOps for a platform with hundreds of teams and thousands of services?
- How would you build a multi-cluster GitOps architecture?
- How would you organize the separation of production and non-production GitOps processes?
- Which trade-offs would you accept between speed of change and manageability in GitOps?
- How would you design a governance model for GitOps in a large organization?
- What would you never do in a GitOps process, and why?

### 11.6 Kubernetes

#### 11.6.1 Deployments, Services, Ingress
- What is Kubernetes and what problem does it solve?
- What is a Kubernetes cluster?
- What are the main components of a Kubernetes cluster?
- What are the control plane and worker nodes?
- What is a Pod?
- Why is a Pod, rather than just a container, the basic unit in Kubernetes?
- Can multiple containers be run in one Pod? When does that make sense?
- What happens to a Pod if a container inside it crashes?
- How is a Pod different from a container in Docker?
- What is a ReplicaSet?
- Why is a Deployment needed?
- What is the difference between a Pod, ReplicaSet, and Deployment?
- Why are Pods usually not created directly in production?
- What is desired state in Kubernetes?
- How does a Deployment ensure that the required number of replicas is maintained?
- What is a rolling update?
- How does Kubernetes update a Deployment without full downtime?
- How is a rolling update different from a recreate strategy?
- How do you roll back a Deployment to a previous version?
- What is rollout history?
- What happens if a new version of a Deployment fails to start?
- What is a revision in a Deployment?
- How do you check rollout status?
- What is the difference between a Deployment and a StatefulSet?
- What is the difference between a Deployment and a DaemonSet?
- When is a Deployment not suitable for the task?
- What typical fields are present in a Deployment manifest?
- What is a selector in a Deployment?
- Why is it important that selectors and labels match correctly?
- What happens if you change the selector of an existing Deployment?
- What is a template in a Deployment?
- How are labels and annotations used in a Deployment?
- What is immutable infrastructure in the context of Kubernetes Deployments?
- How do you specify a container image in a Deployment?
- How do you update the image version in a Deployment?
- What does imagePullPolicy do?
- What is the difference between imagePullPolicy: Always and IfNotPresent?
- Why is using the latest tag in Kubernetes considered a bad practice?
- What is the best way to version container images for Deployments?
- What is kubectl apply?
- How is kubectl apply different from kubectl create and kubectl replace?
- What does kubectl rollout restart do?
- When is it appropriate to use kubectl scale?
- What happens during a Deployment scale up?
- What happens during a Deployment scale down?
- How does Kubernetes choose which nodes to place Deployment Pods on?
- What is scheduling in Kubernetes?
- How is a Deployment related to a ReplicaSet in practice?
- What are maxUnavailable and maxSurge?
- How do you configure zero-downtime rollout through Deployment parameters?
- What is strategy type RollingUpdate?
- What problems can arise during rolling updates of stateful or non-backward-compatible applications?
- What needs to be considered when deploying an application with database migrations?
- How would you organize a safe rollout of an application if the new version is incompatible with the old database schema?
- How do you organize blue-green deployment in Kubernetes?
- How do you organize canary deployment in Kubernetes?
- In which cases is a single Deployment insufficient for canary or blue-green strategies?
- How do you verify which image is actually running in the Pods of a Deployment?
- What is kubectl describe deployment and how is it useful?
- What are typical reasons a Deployment gets “stuck” during rollout?
- What is CrashLoopBackOff and how does it affect a Deployment?
- What does the ImagePullBackOff status mean?
- How would you diagnose a situation where a Deployment is created but the Pods are not Ready?
- What should be checked if the Deployment exists but traffic does not reach the application?
- How are Deployment and Service related?
- What is a Service in Kubernetes?
- Why is a Service needed if a Pod already has an IP?
- Why can Pods not be addressed directly in a reliable way?
- What types of Service exist in Kubernetes?
- What is the difference between ClusterIP, NodePort, and LoadBalancer?
- What is a headless service?
- When is a headless service needed?
- What is a selector in a Service?
- How does a Service determine which Pods to route traffic to?
- What happens if Pod labels and the Service selector do not match?
- What are Endpoints and EndpointSlices?
- How do you check which endpoints are attached to a Service?
- What is the difference between port, targetPort, and nodePort?
- Can targetPort be specified by name instead of number?
- What is kube-proxy?
- How does kube-proxy provide traffic routing to Pods?
- What is the difference between iptables and IPVS modes in kube-proxy?
- How does internal service discovery work in Kubernetes?
- What DNS address does a Service get inside the cluster?
- How does Kubernetes DNS resolve service names?
- What is a namespace in the context of Service access?
- How do you access a Service from another namespace?
- Can one Service be used for multiple Deployments?
- How do you organize one microservice’s access to another inside the cluster?
- What are sticky sessions in the context of Kubernetes Service? Are they needed?
- How does Kubernetes balance traffic between Service Pods?
- Does a Service guarantee even traffic distribution?
- What happens if one Pod is unhealthy but the Service still sees its endpoint?
- How are readiness probes related to Service routing?
- Why is readiness more important than liveness for traffic balancing?
- What is externalTrafficPolicy?
- How is externalTrafficPolicy: Local different from Cluster?
- In which cases is NodePort needed?
- Why is NodePort often considered an intermediate or outdated solution?
- When is a LoadBalancer Service used?
- Where does the external IP for a LoadBalancer Service come from?
- What is cloud controller manager in the context of Service LoadBalancer?
- What is Ingress?
- What problem does Ingress solve?
- How is Ingress different from a LoadBalancer Service?
- How are Ingress and Ingress Controller related?
- Why does the Ingress resource itself do nothing without an Ingress Controller?
- Which popular Ingress Controllers do you know?
- How is NGINX Ingress Controller different from Traefik or HAProxy Ingress?
- What is host-based routing in Ingress?
- What is path-based routing in Ingress?
- How do you configure multiple routes to different services through Ingress?
- How do you configure TLS for Ingress?
- Where is the TLS certificate for Ingress usually stored?
- What is the default backend in Ingress?
- What happens if a request does not match any Ingress rule?
- How would you debug a situation where an Ingress is created but the service is not accessible by domain?
- What should be checked if Ingress returns 404?
- What should be checked if Ingress returns 502/503?
- What is the difference between Ingress and an API Gateway?
- When is Ingress no longer enough and a full API Gateway is needed?
- What is IngressClass?
- How can multiple Ingress Controllers coexist in one cluster?
- How do you restrict which Ingress Controller should process a specific Ingress?
- How would you organize routing for frontend, backend API, and internal admin panel in one cluster?
- How do you implement HTTPS termination at the Ingress?
- Where does SSL termination happen: at the Ingress, at the Service, or in the application?
- When does TLS passthrough make sense instead of termination?
- What is rewrite-target and why is it needed in Ingress?
- Which Ingress Controller annotations are often used in real projects?
- How do you configure redirect from HTTP to HTTPS at the Ingress level?
- Can you restrict request body size at the Ingress level?
- How do you configure rate limiting at the Ingress level?
- How do you configure basic auth at the Ingress level?
- What are the risks of putting too much logic into Ingress annotations?
- What is Gateway API and how is it different from Ingress?
- Is Ingress becoming an outdated abstraction compared to Gateway API?
- How would you explain the differences between Pod / Deployment / Service / Ingress to someone without Kubernetes experience?
- How would you design exposing a web application externally: Deployment + Service + Ingress?
- What typical anti-patterns have you seen when using Deployment, Service, and Ingress?
- When is it better to use several small Deployments, and when one large one?
- How does poor service decomposition affect the number of Ingresses, Services, and operational complexity?
- How would you organize a multi-environment setup for dev/stage/prod in Kubernetes in terms of Deployment, Service, and Ingress?
- Which manifest fields do you consider critical for code review in Deployment, Service, and Ingress?
- How do you approach naming conventions for Deployment, Service, and Ingress in large clusters?
- What security risks arise from exposing services incorrectly through Ingress and LoadBalancer?
- When can a service mesh partially replace or complement the capabilities of Service/Ingress?

#### 11.6.2 Probes, resources, HPA
- What are probes in Kubernetes?
- Which types of probes exist in Kubernetes?
- How is readiness probe different from liveness probe?
- What is a startup probe?
- Why is a startup probe needed if liveness and readiness already exist?
- What happens if a liveness probe starts failing?
- What happens if a readiness probe starts failing?
- Does a readiness probe affect container restart?
- Does a liveness probe affect traffic routing through a Service?
- When is a readiness probe sufficient without liveness?
- When is a startup probe needed?
- Which probe types can be used?
- What is the difference between HTTP probe, TCP probe, and exec probe?
- When is it better to use an HTTP probe?
- When is it better to use a TCP probe?
- When is it better to use an exec probe?
- What risks are associated with exec probes?
- Why should a probe not be too heavy?
- What happens if the healthcheck endpoint itself depends on a database or external service?
- Is it a good idea to make the readiness probe check availability of all application dependencies?
- Why is an overly “smart” healthcheck bad?
- What should be considered a minimally sufficient healthcheck?
- How do you configure initialDelaySeconds, periodSeconds, timeoutSeconds, and failureThreshold?
- What happens if initialDelaySeconds is too small?
- What happens if timeoutSeconds is too small?
- What happens if periodSeconds is too frequent?
- What are false positive / false negative results for probes?
- How do probes affect Deployment rollout?
- Why can a misconfigured readiness probe completely break a rollout?
- Why can a misconfigured liveness probe put an application into endless restarts?
- How would you configure probes for a slowly starting Java application?
- How would you configure probes for a Node.js API?
- How would you configure probes for an application that performs heavy initialization at startup?
- What should you do if the application takes 2–3 minutes to start and liveness kills it?
- How do you use startup probe for a long startup?
- Can the same endpoint be used for readiness and liveness?
- When is that acceptable, and when is it a bad practice?
- How would you separate health endpoints: live, ready, startup?
- What does it mean that a container is Running but not Ready?
- How do you diagnose a Pod that is Running but 0/1 Ready?
- Where do you look for the reasons a probe is failing?
- What will kubectl describe pod show when there are probe problems?
- How would you debug intermittent readiness probe failures?
- How are probes related to graceful shutdown of an application?
- What happens to a Pod during deletion if readiness has already failed?
- How does readiness help gracefully drain traffic before a Pod stops?
- Do you need to change readiness on SIGTERM?
- How should an application behave on shutdown so that rollout happens without losing traffic?
- What are resource requests and limits?
- What is the difference between requests and limits?
- What does Kubernetes use requests for?
- What does Kubernetes use limits for?
- How does the scheduler use resource requests?
- What happens if requests are not set?
- What happens if limits are not set?
- What happens if an application uses more CPU than its limit?
- What happens if an application uses more memory than its limit?
- How is CPU throttling different from OOMKilled?
- Why is a memory limit more dangerous than a CPU limit?
- What does the OOMKilled status mean?
- How do you determine that a container was killed because of lack of memory?
- How do you choose memory requests and limits for a service?
- How do you choose CPU requests and limits for a service?
- Why are requests that are too small bad?
- Why are requests that are too large bad?
- How do requests affect cluster utilization?
- How do requests affect autoscaling?
- What is overcommit in Kubernetes?
- Why can a cluster be “half empty” while new Pods still cannot be scheduled?
- What are QoS classes in Kubernetes?
- Which QoS classes exist?
- How is Guaranteed different from Burstable and BestEffort?
- How does QoS affect kubelet behavior under resource pressure?
- Which Pods will be killed first under memory pressure?
- What is eviction?
- How is eviction different from OOMKilled?
- How does node pressure affect Pods?
- What is LimitRange?
- What is ResourceQuota?
- How does LimitRange help standardize requests/limits in a namespace?
- How does ResourceQuota help protect the cluster from one noisy namespace?
- How would you configure requests/limits for a backend API with uneven load?
- How would you configure requests/limits for a batch job?
- How would you configure requests/limits for a JVM application?
- What problems arise for JVMs in containers when limits are configured incorrectly?
- Why is it important to account for heap/non-heap memory in Java inside a container?
- How would you determine requests and limits based on metrics rather than guesses?
- Which metrics would you look at to size resources?
- What is HPA?
- What does Horizontal Pod Autoscaler do?
- How does HPA decide to scale?
- Which metrics can HPA use?
- How does HPA use CPU utilization?
- What is target utilization?
- How does HPA calculate the required number of replicas?
- Can HPA work based on memory?
- What are the limitations of scaling by memory?
- Can you scale by custom metrics?
- How is HPA related to metrics-server?
- What happens if metrics-server is not working?
- Why does HPA not work correctly without proper resource requests?
- How does HPA behave if requests are not set?
- What are scale up stabilization and scale down stabilization?
- Why can autoscaling flap?
- How do you avoid constant scale up/down?
- What is cooldown behavior in HPA?
- Can different scale up and scale down policies be configured?
- How does HPA interact with Deployment?
- Can HPA be used with StatefulSet?
- How is HPA different from VPA?
- What is VPA?
- When is HPA better, and when is VPA better?
- Why can HPA and VPA conflict?
- What is Cluster Autoscaler?
- How is HPA different from Cluster Autoscaler?
- How do HPA and Cluster Autoscaler work together?
- What happens if HPA increases replicas but the cluster does not have enough resources?
- What happens if HPA scales a service but the bottleneck is not CPU?
- Why does autoscaling by CPU not always reflect real load?
- When is it better to scale by RPS, latency, or queue length?
- How do you configure autoscaling for queue consumers?
- How do you scale background workers in Kubernetes?
- How do you scale a service with long-running requests?
- How does HPA affect infrastructure cost?
- What are the risks of overly aggressive autoscaling?
- What are the risks of overly slow autoscaling?
- How would you investigate a situation where HPA does not scale a service under load?
- How would you investigate a situation where HPA scales a service too often?
- How would you investigate a situation where latency does not decrease after scale up?
- When can autoscaling make a system worse rather than better?
- Which dependencies need to be considered before enabling HPA for a service?
- How does HPA interact with readiness probe?
- Can slow startup of new Pods make autoscaling useless?
- How does startup time affect HPA effectiveness?
- For a critical API, would you choose a fixed number of replicas or HPA? Why?
- How would you explain the difference between requests/limits, probes, and HPA to someone without operations experience?
- Which anti-patterns have you seen in configuring probes, resources, and HPA?
- How would you conduct a code review of a manifest with probes, resources, and HPA?
- Which default values do you consider dangerous in production for probes and resources?
- What is more important for production reliability: well-configured probes or properly chosen requests/limits? Why?
- How would you build a strategy for tuning probes, resources, and autoscaling for a new service with an unknown load profile?

#### 11.6.3 ConfigMap, Secret
- What is a ConfigMap in Kubernetes?
- What is a ConfigMap used for?
- What is a Secret in Kubernetes?
- How is a Secret different from a ConfigMap?
- Why should a Secret not be considered a fully secure secret store “out of the box”?
- Which types of Secret exist in Kubernetes?
- What is an Opaque Secret?
- What is a kubernetes.io/tls secret?
- What is a docker-registry secret?
- What kind of data is appropriate to store in a ConfigMap?
- What kind of data is appropriate to store in a Secret?
- What data should not be stored in either ConfigMap or Secret?
- Why should passwords not be stored in a ConfigMap?
- Can non-sensitive config be stored in a Secret?
- When might that be justified, and when not?
- How do you pass a ConfigMap into a container?
- How do you pass a Secret into a container?
- What is the difference between passing ConfigMap/Secret through env vars and through volume mounts?
- When is it better to use env vars?
- When is it better to use mounted files?
- What happens if you change a ConfigMap connected through env vars?
- What happens if you change a ConfigMap mounted as a volume?
- Does the application pick up ConfigMap changes automatically?
- Is a Pod restart required after changing a ConfigMap?
- Does a Secret pick up changes automatically?
- How does the application learn about a Secret or ConfigMap change on the filesystem?
- Which applications can correctly reload config without restart?
- How do you organize hot reload of configuration in Kubernetes?
- What is an immutable ConfigMap/Secret?
- When does it make sense to make a ConfigMap immutable?
- What are the pros and cons of immutable ConfigMaps?
- How do you create a ConfigMap from literals?
- How do you create a ConfigMap from a file?
- How do you create a Secret from a file?
- Why is Secret data stored in base64, and why is that not encryption?
- How is encoding different from encryption in the context of Secret?
- How do you protect Secret data in etcd?
- What is encryption at rest for Kubernetes secrets?
- Why is enabling encryption at rest important?
- Who can read a Secret in the cluster?
- How does RBAC affect access to Secrets?
- How do you restrict Pod and user access to a Secret?
- Why should permissions to read Secrets be granted as narrowly as possible?
- What are the risks of secrets in env vars?
- Why can secrets in env vars leak into logs, dumps, or debug output?
- What are the risks of mounted secrets in files?
- Which is safer: env vars or volume mounts for secrets?
- How do you pass a TLS certificate to an Ingress via Secret?
- How do you pass Docker registry credentials via Secret?
- What is a service account token and why is it also secret material?
- How are ServiceAccount and Secrets related?
- How do you disable automatic mounting of a service account token if it is not needed?
- How would you store database credentials for an application in Kubernetes?
- How would you store API keys for external services?
- How would you store configuration for dev/stage/prod environments?
- How do you separate config and secrets between environments?
- How do you organize naming conventions for ConfigMap and Secret?
- Is it better to create one large ConfigMap for all settings or several small ones?
- Is it better to create one large Secret for all secrets or split them?
- What are the pros and cons of granular ConfigMaps/Secrets?
- How are ConfigMap and Secret used in a Helm chart?
- How do you template ConfigMaps and Secrets in Helm?
- What are the risks of storing secrets directly in values.yaml?
- Why is storing secrets in plaintext in Git a bad practice?
- Which approaches to secure storage of secrets in GitOps do you know?
- What are Sealed Secrets?
- What is External Secrets Operator?
- How can Kubernetes integrate with Vault, AWS Secrets Manager, GCP Secret Manager?
- When are built-in Kubernetes Secrets not enough and an external secret manager is needed?
- What is secret rotation?
- How do you organize secret rotation without long downtime?
- What problems arise during rotation of a database password or API token?
- How should an application be designed to survive secret rotation?
- How do you verify that a Pod actually received the required ConfigMap or Secret?
- What do you do if a Pod does not start because a Secret is missing?
- What will the container see if a key is missing in a Secret but the application expects it?
- How does optional: true work for ConfigMap/Secret volumes?
- What are projected volumes?
- How do you combine several configuration sources into one volume?
- Can ConfigMap be used for feature flags?
- When is ConfigMap suitable for feature flags, and when is a separate feature flag management service needed?
- What anti-patterns exist when working with ConfigMap?
- What anti-patterns exist when working with Secret?
- Why is it bad to use Kubernetes Secret as a universal secret database for everything?
- What problems can arise with a large number of large ConfigMaps/Secrets?
- How would you explain the difference between ConfigMap and Secret to a junior engineer?
- How would you build a strategy for managing configs and secrets in a large company with multiple teams and environments?
- Which checks would you perform in code review for ConfigMap and Secret?
- How would you investigate an incident involving secret leakage in Kubernetes?
- How would you organize an audit of Secret usage in a cluster?
- What questions should be asked to a team before allowing them to store sensitive data in Kubernetes Secret?
- What is more important in Secret operations: protection of storage, protection of access, or rotation? Why?

### 11.7 Infrastructure as Code

#### 11.7.1 Terraform
- What is Infrastructure as Code?
- Why is Infrastructure as Code needed?
- What problems does Infrastructure as Code solve compared to manual infrastructure management?
- What are the benefits of describing infrastructure as code?
- What disadvantages and risks does the Infrastructure as Code approach have?
- How is Infrastructure as Code different from configuration management?
- Which IaC tools do you know?
- Why has Terraform become a popular IaC tool?
- In which cases is Terraform a better fit than manual setup through a cloud console?
- In which cases might Terraform not be the best choice?
- What is Terraform?
- How does Terraform work at a high level?
- What are the main entities of a Terraform configuration?
- What is a provider in Terraform?
- What is a resource in Terraform?
- What is a data source in Terraform?
- What is the difference between a resource and a data source?
- What are input variables in Terraform?
- What are output values?
- What are local values?
- What is a module in Terraform?
- Why are modules needed in Terraform?
- What is the difference between a root module and a child module?
- What does the typical Terraform workflow look like?
- What do the commands `terraform init`, `terraform plan`, `terraform apply`, and `terraform destroy` do?
- What does `terraform validate` do?
- What does `terraform fmt` do?
- What does `terraform plan` show?
- Why is it important to read the plan before applying?
- What is state in Terraform?
- Why does Terraform store state?
- Where is terraform state stored by default?
- Why is storing state locally often inconvenient or dangerous?
- What is remote state?
- Which backends for state do you know?
- Why should state be stored remotely?
- What are the advantages of remote state storage in a team?
- What is state locking and why is it needed?
- What problems arise if two people run apply at the same time?
- Which tools/mechanisms provide state locking?
- Why is state considered a sensitive artifact?
- What secrets can accidentally end up in state?
- How do you protect Terraform state?
- Can terraform.tfstate be committed to Git?
- Why should state not be committed to a repository?
- What is drift in Terraform?
- How does Terraform help detect infrastructure drift?
- What happens if someone manually changes infrastructure in the cloud outside Terraform?
- How do you return infrastructure to the desired state after drift?
- What is idempotency in the context of Terraform?
- Why is the declarative approach convenient for infrastructure management?
- How is Terraform’s declarative approach different from imperative scripts?
- Which variable types does Terraform support?
- How do you assign variable values?
- What is the difference between a `default` and a required variable?
- What is a `.tfvars` file?
- When should you use `terraform.tfvars`, and when environment variables?
- How do you pass sensitive values into Terraform?
- What does the `sensitive = true` flag do?
- Does `sensitive = true` fully protect secrets?
- What is interpolation in Terraform?
- How do expressions work in Terraform?
- Which built-in Terraform functions have you used?
- What is `count`?
- What is `for_each`?
- What is the difference between `count` and `for_each`?
- When is it better to use `count`, and when `for_each`?
- What problems can arise when migrating a resource from `count` to `for_each`?
- What are dynamic blocks?
- When are dynamic blocks useful, and when do they hurt readability?
- What is the dependency graph in Terraform?
- How does Terraform determine the order of resource creation?
- When do you need to explicitly use `depends_on`?
- Why is overusing `depends_on` bad?
- What are lifecycle meta-arguments?
- What is `create_before_destroy` used for?
- What is `prevent_destroy` used for?
- What is `ignore_changes` used for?
- When is `ignore_changes` useful, and when is it dangerous?
- What are provisioners in Terraform?
- Why are provisioners usually considered undesirable?
- In which cases are `local-exec` or `remote-exec` still acceptable?
- How does Terraform manage resources that already exist?
- What does the `terraform import` command do?
- What limitations does `terraform import` have?
- Why does a resource often need to be manually described in code after import?
- What does `terraform state list` do?
- What does `terraform state show` do?
- What is `terraform state mv` used for?
- What is `terraform state rm` used for?
- In which cases do you have to edit state, and why is it dangerous?
- How do you safely refactor Terraform configuration without recreating resources?
- What are moved blocks?
- How do you rename a resource without deleting and recreating it?
- How do provider version constraints work?
- Why is it important to pin provider versions?
- Why is it important to pin the Terraform version?
- What is `.terraform.lock.hcl`?
- Should `.terraform.lock.hcl` be committed to the repository?
- How do you organize the structure of a Terraform repository?
- How do you separate infrastructure by environments: dev, stage, prod?
- What is better: separate folders, workspaces, or separate repositories for environments?
- What are Terraform workspaces?
- When are workspaces useful?
- When do workspaces lead to confusion?
- Which environment approach do you consider the most reliable?
- How do you organize reusable modules?
- What should good Terraform modules be like?
- What should not be hardcoded inside a module?
- Where is the boundary between an “overly generic” and an “overly specialized” module?
- How do you version Terraform modules?
- How do you publish and reuse internal modules?
- Which best practices for naming resources and variables in Terraform do you know?
- How do you test Terraform code?
- What can be checked in CI before apply?
- Which static analysis tools for Terraform do you know?
- What are `tflint`, `tfsec`, `checkov`, and `terraform validate` used for?
- How do you integrate Terraform checks into a CI/CD pipeline?
- How do you organize review of Terraform changes in a team?
- Should `terraform apply` be allowed from a developer’s local machine?
- How do you restrict permissions for applying infrastructure changes?
- What is policy as code in the context of Terraform?
- Which policy as code tools do you know?
- How do you use policies to restrict dangerous changes?
- How do you organize secrets when working with Terraform?
- Why is it a bad idea to store secrets directly in `.tf` or `.tfvars` files?
- How does Terraform integrate with Vault or cloud secret managers?
- How do you work with multiple clouds in Terraform?
- What complexities arise in multi-cloud configurations?
- How realistic is it to write truly cloud-agnostic Terraform?
- How would you describe infrastructure for a simple web application with Terraform?
- How would you organize Terraform for VPC, subnets, security groups, and compute instances?
- How would you provision a managed database through Terraform?
- What would you consciously avoid creating through Terraform?
- Is it worth managing users and temporary manual objects through Terraform?
- Where is the boundary between Terraform and Kubernetes manifests/Helm?
- Should a Kubernetes cluster be created with Terraform?
- Should Kubernetes resources inside the cluster be created with Terraform?
- When is it better to use Terraform, and when Helm/Kustomize?
- How is Terraform used together with GitOps?
- What does the workflow “pull request → plan → review → apply” look like?
- What is a speculative plan?
- How do you organize automatic application of changes only after approval?
- What are the risks of full auto-apply?
- How do you minimize blast radius in Terraform changes?
- How do you split large configurations into independent stacks?
- What is layered infrastructure?
- When should networking, platform, and application infrastructure be separated into different states?
- What are the problems of a monolithic Terraform state?
- What is a remote state data source?
- When should outputs from one stack be used in another?
- What are the risks of tightly coupling stacks through remote state?
- How would you migrate existing infrastructure into Terraform?
- Where would you start Terraform automation in a company where everything is configured manually?
- How would you explain Terraform to a developer who has never dealt with infrastructure?
- What is taint in Terraform and when is it used?
- What does `terraform apply -refresh-only` do?
- How is `plan` different from `apply` from the perspective of process safety?
- How does Terraform behave during a partially successful apply?
- What do you do if apply fails in the middle of changes?
- How do you roll back changes made by Terraform?
- Does Terraform have “rollback” in the classical sense?
- How do you organize safe rollback of infrastructure changes?
- How does Terraform work with eventual consistency in clouds?
- What difficulties arise when creating cloud resources that do not appear instantly?
- Why do Terraform configurations sometimes become too complex, and how can this be avoided?
- Which anti-patterns in Terraform have you seen?
- Why are “universal modules for all cases” bad?
- Why is excessive module nesting bad?
- How do you evaluate the quality of Terraform code?
- How would you conduct code review of Terraform configuration?
- What would you check first in a PR with Terraform changes?
- What questions would you ask a candidate about Terraform if you were hiring them to the team?
- Tell me about the most difficult Terraform incident you have encountered.
- Tell me about a case where a Terraform configuration caused unwanted changes in production.
- How do you prevent accidental deletion of critical infrastructure?
- How would you design a Terraform landscape for a large company with multiple teams and environments?
- How would you ensure separation of access between teams to different parts of the infrastructure?
- How would you organize a migration strategy when changing the structure of modules or state?
- How would you scale Terraform processes in a company with dozens of services and teams?

#### 11.7.2 Ansible
- What is Ansible?
- What is Ansible used for?
- Which tasks are usually solved with Ansible?
- How is Ansible different from Terraform?
- How is Ansible different from shell scripts?
- How is Ansible different from Chef, Puppet, and SaltStack?
- In which scenarios is Ansible a better fit than Terraform?
- In which scenarios is Terraform a better fit than Ansible?
- Can Ansible and Terraform be used together?
- How do you split responsibility between Terraform and Ansible?
- What does it mean that Ansible is an agentless tool?
- What advantages does the agentless approach provide?
- What limitations does the agentless approach have?
- How does Ansible connect to remote machines?
- Which protocols does Ansible use for Linux and Windows?
- What is inventory in Ansible?
- What types of inventory are there?
- How is static inventory different from dynamic inventory?
- What is dynamic inventory and when is it needed?
- How does inventory work with host groups?
- What are host variables and group variables?
- Where can variables be defined in Ansible?
- What is a playbook?
- What is a play?
- What is a task?
- What is a role?
- What is a handler?
- What is a module in Ansible?
- What is a collection in Ansible?
- How is a role different from a playbook?
- Why split a playbook into roles?
- What does a typical Ansible role structure look like?
- What is usually placed in `tasks`, `handlers`, `templates`, `files`, `vars`, `defaults`, and `meta`?
- What is the difference between `vars` and `defaults`?
- Why are variables in `defaults` considered safer to override?
- What is idempotency in Ansible?
- Why is idempotency important for configuration management?
- How do you know that a task in Ansible is idempotent?
- What typical mistakes make tasks non-idempotent?
- What does the changed status mean in Ansible?
- How do handlers help avoid unnecessary actions?
- When does a handler run?
- How do you force a handler to run?
- What is `notify`?
- How does Ansible determine whether a task changed something or not?
- How are the `command`, `shell`, and `raw` modules different?
- Why is overusing `shell` and `command` considered bad practice?
- When is `shell` still justified?
- Why is a module better than an arbitrary shell command?
- What is `template` and how is it different from `copy`?
- What is Jinja2 used for in Ansible?
- What risks are associated with generating configs through templates?
- How do you work with conditional logic in playbooks?
- What is `when`?
- How do loops work in Ansible?
- What is `loop` and how is it better than the old `with_items`?
- What is `register`?
- How do you use the result of one task in another?
- What are facts in Ansible?
- How are facts gathered?
- What is `gather_facts` used for?
- When does it make sense to disable `gather_facts`?
- What are `ansible_facts`?
- How do you work with facts for different OSes and distributions?
- How do you write playbooks that support both Ubuntu and CentOS?
- How do you work with package managers of different systems in Ansible?
- Which module should be used to install packages?
- How do you manage services through Ansible?
- How do you manage users and groups through Ansible?
- How do you manage files and directories through Ansible?
- How do you set file permissions through Ansible?
- How do you update configs safely so as not to break services?
- How do you validate configuration before restarting a service?
- How do you implement the pattern “first validate config, then reload/restart”?
- How do you work with secrets in Ansible?
- What is Ansible Vault?
- What are the pros and cons of Ansible Vault?
- When is Vault convenient, and when is an external secret manager better?
- How do you encrypt individual variables and entire files?
- How do you organize secrets for different environments?
- How do you store and pass the vault password?
- What is privilege escalation in Ansible?
- How does `become` work?
- What risks are associated with global `become: true`?
- How do you minimize privileges in playbooks?
- What are tags in Ansible?
- Why use tags?
- How does selective execution help with large playbooks?
- What is `--check` mode?
- How much can check mode be trusted?
- What is diff mode?
- How do you verify a playbook before running it in production?
- What is `ansible-lint`?
- Which playbook quality rules do you consider important?
- How do you test Ansible roles?
- What is Molecule used for?
- How would you organize CI for Ansible?
- What should be checked in an Ansible pipeline?
- How do you version roles and collections?
- How do you reuse roles across projects?
- How do you organize an internal library of roles in a company?
- How do you split Ansible code by environments?
- Which is better: one role with lots of conditions or several narrower roles?
- What anti-patterns in Ansible have you encountered?
- Why is a “god playbook” that does everything a bad idea?
- Why are overly clever Jinja2 templates bad?
- How do you avoid turning a playbook into a hard-to-read program?
- What is serial execution in Ansible?
- How do you roll out changes gradually across hosts?
- What is `serial` and when is it needed?
- How do you do a rolling update with Ansible?
- How would you roll out a service update without downtime?
- How do you implement a canary or phased rollout in Ansible?
- How do you limit blast radius when changing configuration across many servers?
- What is `delegate_to`?
- When is it useful to run a task not on the target host but on another host?
- What is `run_once`?
- When is `run_once` useful?
- How does Ansible work in pull and push models?
- What is ansible-pull and when can it be useful?
- Which approach to Ansible do you prefer: push or pull?
- How does Ansible integrate with cloud providers?
- Can Ansible be used to create infrastructure, not just configure it?
- Should Ansible be used for infrastructure provisioning in the cloud?
- How does Ansible integrate with Docker and Kubernetes?
- In which cases is Ansible appropriate for Kubernetes, and when are Helm/Kustomize better?
- How do you work with dynamic inventory from AWS/GCP/Azure?
- How do you run Ansible in a container or CI environment?
- How do you organize server access for Ansible in a team?
- How do you ensure auditability of changes made through Ansible?
- How do you track who ran a playbook and when?
- How do you organize review of Ansible changes?
- What do you do if a playbook fails in the middle of execution?
- How does Ansible behave on a partially successful run?
- How do you rerun a playbook after an error?
- Does Ansible have rollback in the classic sense?
- How do you design changes so that rollback is possible?
- What is immutable infrastructure and how does Ansible relate to this approach?
- In which cases does Ansible work well with an immutable approach, and in which does it conflict with it?
- How do you use Ansible to bootstrap new servers?
- How do you use Ansible for Linux host hardening?
- How would you describe configuring a web server with Ansible?
- How would you automate Nginx installation, config deployment, and service restart?
- How would you deploy an application to multiple hosts via Ansible?
- How would you implement installation of a monitoring agent on hundreds of servers?
- How would you safely mass-update configuration on production servers?
- How do you ensure playbooks are repeatable and readable?
- What naming conventions and style rules do you use for Ansible?
- How do you conduct code review for Ansible?
- What do you check first in a PR with Ansible code?
- What questions would you ask a candidate about Ansible in an interview?
- Tell us about the most complex automation you built with Ansible.
- Tell us about a case where an Ansible change caused an incident.
- How would you establish an Ansible approach in a company with dozens of teams and hundreds of servers?
- How do you decide what in a company should be automated with Ansible and what is better done with other tools?

### 11.8 Observability: monitoring, logging, tracing

#### 11.8.1 Prometheus and Grafana
- What is observability and how is it different from monitoring?
- What are the three main pillars of observability?
- How are metrics different from logs and tracing?
- When are metrics enough, and when can’t you do without logs or tracing?
- What is time series data?
- What is Prometheus and what tasks is it used for?
- What is Grafana and what role does it play in an observability system?
- How do Prometheus and Grafana usually work together?
- What is the difference between Prometheus, Grafana, and Alertmanager?
- What main components of Prometheus do you know?
- What is a scrape in Prometheus?
- How does Prometheus obtain metrics from applications?
- What is the pull model in Prometheus?
- How is Prometheus’s pull model different from a push model?
- In which cases can a push model be preferable to a pull model?
- What is an exporter in Prometheus?
- Which popular exporters have you used or know?
- What is node_exporter and what metrics does it collect?
- What are application metrics?
- Which metrics should a backend application expose first?
- Which metrics should be collected from the frontend, backend, database, and infrastructure?
- What are the four “golden signals” you know?
- What is the RED methodology?
- What is the USE methodology?
- In which cases would you use RED, and in which USE?
- What are counter, gauge, histogram, and summary?
- How is a counter different from a gauge?
- When should you use a histogram, and when a summary?
- What is the difference between a histogram and a summary from the perspective of aggregation?
- What is a bucket in a histogram?
- How do you choose bucket boundaries for a histogram?
- What mistakes are often made when designing histogram buckets?
- What is a label in Prometheus?
- What are labels used for?
- How do labels help filter and aggregate metrics?
- What is cardinality and why does it matter?
- What are high-cardinality labels?
- Why are user_id, request_id, or session_id bad ideas in labels?
- What problems does high cardinality create for Prometheus?
- How do you design metrics so you don’t kill Prometheus with a cardinality explosion?
- What naming conventions for metrics do you use?
- Why is it important to follow consistent metric naming?
- How should metrics for time, size, quantity, and errors be named correctly?
- Why are metrics usually stored in base units?
- What is scrape interval?
- How do you choose a scrape interval for different types of services?
- What is evaluation interval?
- How is scrape interval different from evaluation interval?
- What is a target in Prometheus?
- What are job and instance in Prometheus?
- How does Prometheus discover new targets?
- What is service discovery in Prometheus?
- Which service discovery mechanisms have you used?
- How does service discovery work in Kubernetes for Prometheus?
- What is relabeling in Prometheus?
- What is relabel_configs used for?
- What are recording rules?
- What are recording rules used for?
- When should complex PromQL be moved into recording rules?
- What are alerting rules?
- How are alerting rules different from recording rules?
- What is Alertmanager?
- How does Alertmanager group, deduplicate, and route alerts?
- What are inhibit rules in Alertmanager?
- What is a silence in Alertmanager?
- How do you organize alert routing by severity, team, service, and environment?
- How do you avoid alert fatigue?
- How do you know there are too many alerts?
- Which alerts do you consider genuinely useful?
- Which alerts are usually considered noise?
- Why is symptom-based alerting better than threshold-only alerting?
- When is a CPU alert actually useful, and when is it a false signal?
- Why is an alert like “CPU > 80%” often not good enough on its own?
- Which metrics would you alert on for a backend service?
- Which metrics would you alert on for a database?
- Which metrics would you alert on for a Kubernetes cluster?
- Which metrics would you alert on for a message queue?
- What is SLO-based alerting?
- How is Prometheus used in an SLI/SLO approach?
- How do you build an alert for error rate?
- How do you build an alert for latency?
- How do you build an alert for saturation?
- What is burn rate alerting?
- Why are multi-window multi-burn-rate alerts often used for SLOs?
- How would you configure an alert so that it does not flap?
- What is `for` used for in alerts?
- How does `for` affect false positives?
- What are flapping alerts and how do you deal with them?
- What is PromQL?
- What is PromQL used for?
- How do you get the raw value of a metric in PromQL?
- How do you filter metrics by labels in PromQL?
- What do the operators `sum`, `avg`, `max`, `min`, and `count` do in PromQL?
- How is `rate()` different from `irate()`?
- When do you use `increase()` instead of `rate()`?
- How is `sum(rate(...))` different from `rate(sum(...))`, and why does it matter?
- What does `histogram_quantile()` do?
- How do you calculate p95 latency using PromQL?
- How do you calculate a service’s error rate in PromQL?
- How do you calculate service availability in PromQL?
- How do you find the most problematic instance or pod in PromQL?
- How do you aggregate metrics by service, method, route, and status_code?
- What mistakes are most often made in PromQL queries?
- Why is it not always correct to average latency?
- Why can percentiles from a summary not be aggregated correctly across instances?
- What is vector matching in PromQL?
- When do you need `on()`, `ignoring()`, `group_left()`, and `group_right()`?
- How do you compare current values with historical ones in PromQL?
- How do you build an alert on an anomaly rather than on a fixed threshold?
- Can Prometheus be used for long-term analytics?
- What are the limitations of Prometheus as a TSDB?
- What is retention in Prometheus?
- How do you choose a retention period?
- How does the size of retention affect disk usage and performance?
- What happens if Prometheus cannot keep up with scraping targets?
- What are remote write and remote read?
- What is remote write used for?
- Which long-term storage systems for Prometheus do you know?
- Why would you use Thanos, Cortex, Mimir, or VictoriaMetrics?
- How is single-node Prometheus different from an HA setup?
- How is high availability built for Prometheus?
- How do you avoid a single point of failure in the observability stack?
- Can you run two Prometheus instances for the same set of targets?
- How does deduplication work in systems on top of multiple Prometheus instances?
- What is federation in Prometheus?
- When is Prometheus federation useful?
- What are the pros and cons of federation?
- How do you monitor Prometheus itself?
- Which Prometheus self-metrics are important to track?
- How do you know that Prometheus is overloaded?
- How do you know you have cardinality problems?
- How do you know scrape targets are unstable?
- How do you monitor scrape failures?
- How do you debug a situation where a target exists but metrics are not coming in?
- How do you debug a situation where Grafana “shows nothing” even though Prometheus is working?
- What is a Grafana dashboard?
- What good dashboard creation practices do you use?
- How is an operational dashboard different from a business dashboard?
- What should be on a dashboard for a backend service?
- What should be on a dashboard for a database?
- What should be on a dashboard for Kubernetes?
- What anti-patterns exist when building dashboards?
- Why is “a huge dashboard with 200 charts” often useless?
- How do you group panels on a dashboard?
- What are variables in Grafana?
- How does templating in Grafana help reuse dashboards?
- How do you organize dashboards for multiple environments?
- How do you organize dashboards for multiple teams and services?
- What are annotations in Grafana?
- Why are annotations for deploys, incidents, and scaling events useful?
- How do you connect releases and graphs on a dashboard?
- How do you use Grafana Explore?
- When is it better to look at a dashboard, and when to go to Explore?
- How do you make a dashboard useful for an on-call engineer?
- How would you design a dashboard so that a new engineer understands it in 2 minutes?
- How do you choose the right visualizations for different types of metrics?
- When is it better to use a stat panel, and when a time series panel?
- What mistakes are common when visualizing percentile latency?
- How do you build a dashboard for SLA/SLO?
- How do you display error budget consumption on a dashboard?
- What is synthetic monitoring and how is it different from infrastructure monitoring?
- How do you connect application metrics with business metrics?
- Which business metrics are useful to display next to technical ones?
- How do you use monitoring in capacity planning?
- Which metrics help understand that it’s time to scale a service?
- Which metrics help understand that the application is degrading even though users are not complaining yet?
- Which metrics would you collect for a task queue?
- Which metrics would you collect for Kafka?
- Which metrics would you collect for RabbitMQ?
- Which metrics would you collect for PostgreSQL?
- Which metrics would you collect for Redis?
- Which metrics would you collect for Nginx / reverse proxy?
- Which metrics would you collect for a JVM application?
- Which metrics would you collect for a Node.js application?
- Which metrics would you collect for a Python service?
- How do you instrument your own application for Prometheus?
- Which Prometheus client instrumentation libraries have you used?
- Which custom metrics are actually useful to add to code?
- Which custom metrics often turn out to be useless?
- Where is the line between “we need to add a metric” and “we need to look at logs/traces”?
- How do you connect Prometheus monitoring with the incident response process?
- How would you build observability for a small startup?
- How would you build observability for a large microservices platform?
- How does the monitoring approach change in a multi-tenant system?
- How does the monitoring approach change in a high-load system?
- How would you design an observability stack from scratch for a new company?
- What trade-offs do you see between observability depth, cost, and operational complexity?
- What would you monitor in the first week after launching a new service?
- How would you explain the difference between Prometheus and Grafana to a non-technical manager?
- If you had a limited budget, what would you implement first: metrics, logs, or tracing? Why?
- What typical mistakes have you seen teams make when adopting Prometheus and Grafana?
- How do you assess the observability maturity of a team or platform?
- How do you know that observability in a company is truly helping rather than existing “for show”?

#### 11.8.2 OpenTelemetry
- What is OpenTelemetry?
- What problems does OpenTelemetry solve?
- How is OpenTelemetry different from Prometheus?
- How is OpenTelemetry different from OpenTracing and OpenCensus?
- Why has OpenTelemetry become the de facto standard for observability instrumentation?
- Which signals does OpenTelemetry support?
- What are traces, spans, metrics, and logs in the context of OpenTelemetry?
- What is distributed tracing?
- What problem does distributed tracing solve?
- When are metrics alone not enough and tracing is needed?
- When is tracing useless without logs and metrics?
- What is a trace?
- What is a span?
- What parts does a span usually consist of?
- What are a parent span and a child span?
- What is a root span?
- What are span attributes?
- What are events inside a span?
- What is span status?
- What is baggage?
- How is baggage different from span attributes?
- What is context propagation?
- Why is propagation needed in a distributed system?
- What happens to trace context when moving between services?
- How is trace context propagated over HTTP?
- How is trace context propagated through a message broker?
- Which propagation header formats do you know?
- What is W3C Trace Context?
- What are traceparent and tracestate?
- What is a correlation ID and how is it different from a trace ID?
- Is it necessary to use a correlation ID if distributed tracing already exists?
- How do you connect a trace ID with application logs?
- Why is it important for logs to contain trace_id and span_id?
- What is instrumentation?
- How is auto-instrumentation different from manual instrumentation?
- When is auto-instrumentation sufficient?
- When can’t you do without manual instrumentation?
- What are the pros and cons of auto-instrumentation?
- What are the pros and cons of manual instrumentation?
- What typical mistakes happen in manual instrumentation?
- How do you instrument an incoming HTTP request?
- How do you instrument an outgoing HTTP call?
- How do you instrument database access?
- How do you instrument a message queue?
- How do you instrument a batch job or cron job?
- How do you instrument background tasks?
- How do you instrument a long-running process?
- What are semantic conventions in OpenTelemetry?
- Why are semantic conventions needed?
- Why is it important to use standard attribute names?
- Which semantic conventions do you know for HTTP, DB, and messaging?
- What is the OpenTelemetry SDK?
- What is the OpenTelemetry API?
- What is the difference between the API and the SDK in OpenTelemetry?
- What is the OpenTelemetry Collector?
- Why is the OpenTelemetry Collector needed?
- What roles can the Collector play?
- What is a receiver in the Collector?
- What is a processor in the Collector?
- What is an exporter in the Collector?
- Which receivers do you know?
- Which processors do you know?
- Which exporters do you know?
- What is the batching processor and why is it needed?
- What is the memory limiter processor?
- What is the tail sampling processor used for?
- What is the attributes processor used for?
- What problem does the Collector solve compared with sending data directly from the application?
- When is it better to send telemetry directly, and when through a Collector?
- How does the agent deployment model for the Collector work?
- How does the gateway deployment model for the Collector work?
- When would you choose agent deployment, and when a centralized gateway?
- Can both approaches be used simultaneously?
- How does the OpenTelemetry Collector fit into Kubernetes?
- How do you deploy the Collector in Kubernetes: as a DaemonSet, Deployment, or sidecar?
- In which cases is the sidecar approach appropriate, and in which is it not?
- What is sampling in distributed tracing?
- Why is sampling needed?
- Which types of sampling do you know?
- What is head-based sampling?
- What is tail-based sampling?
- What is the difference between head sampling and tail sampling?
- When is tail sampling better than head sampling?
- What trade-offs does tail sampling have?
- Why can’t you store 100% of traces in a high-load system?
- When does it make sense to temporarily enable 100% sampling?
- How do you choose a sampling rate?
- How does sampling affect troubleshooting?
- How does sampling affect observability cost?
- How does sampling affect the representativeness of the data?
- What are exemplars and how do they connect metrics and traces?
- How do you connect a spike on a latency graph to specific traces?
- How do you organize tracing in a microservices architecture?
- How do you organize tracing in an event-driven architecture?
- How do you trace a request through an API Gateway, backend, database, and message broker?
- How do you trace asynchronous processing when there is no direct request/response?
- What difficulties arise when tracing queues and background jobs?
- How do you trace fan-out/fan-in scenarios?
- What do you do if some services do not support OpenTelemetry?
- How do you gradually introduce OpenTelemetry into a legacy system?
- What minimum value can you get from OpenTelemetry already at the first stage?
- Which languages and frameworks have you instrumented using OpenTelemetry?
- What pitfalls are there when introducing OpenTelemetry in Java?
- What pitfalls are there when introducing OpenTelemetry in Node.js?
- What pitfalls are there when introducing OpenTelemetry in Python?
- How does OpenTelemetry work together with Prometheus?
- Can OpenTelemetry be used for metrics?
- What is the difference between collecting metrics via a Prometheus client and via OpenTelemetry metrics?
- Is it worth migrating metrics to OpenTelemetry if Prometheus is already in place? Why?
- How does OpenTelemetry integrate with Jaeger?
- How does OpenTelemetry integrate with Tempo?
- How does OpenTelemetry integrate with Zipkin?
- How does OpenTelemetry integrate with the Grafana stack?
- How does OpenTelemetry integrate with Datadog, New Relic, Honeycomb, and other vendors?
- What is vendor neutrality in the context of OpenTelemetry?
- Why does OpenTelemetry help reduce vendor lock-in?
- What limitations does OpenTelemetry have as a standard and ecosystem?
- What are resource attributes?
- How are resource attributes different from span attributes?
- Which resource attributes should definitely be set?
- Why is it important to pass service.name, service.version, and deployment.environment?
- What are service.namespace and service.instance.id?
- How do you use version and environment in observability?
- How do you connect deploys and changes in telemetry?
- How do you debug a situation where traces are not reaching the backend storage?
- How do you debug a broken trace when some spans are missing?
- Why can context propagation break?
- What happens if one service does not propagate trace headers further?
- How do you find the service that “breaks” the trace chain?
- How would you test correctness of instrumentation in tests?
- Are tests for observability instrumentation necessary?
- What data should not be sent thoughtlessly in spans and logs?
- What security and privacy risks exist when collecting telemetry?
- Why should PII and secrets not be placed into span attributes?
- How do you mask or redact sensitive data in the Collector?
- What is redaction in an observability pipeline?
- What performance problems can excessive instrumentation create?
- How does instrumentation affect application latency and resource usage?
- How do you know that the overhead from OpenTelemetry has become too high?
- What data in a span is genuinely useful, and what only creates noise?
- How do you avoid turning distributed tracing into an “expensive but useless” system?
- How do you determine that there are too many observability signals and that they are poorly designed?
- What best practices do you use when designing spans?
- How do you choose span boundaries?
- What should be a separate span, and what should not?
- When is a span too large, and when is it too small?
- What are business spans and technical spans?
- Is it worth tracing business operations separately from technical calls?
- How does OpenTelemetry help in root cause analysis?
- How does OpenTelemetry help in performance optimization?
- How does OpenTelemetry help in incident response?
- Which incident investigation scenarios become easier with distributed tracing?
- In which scenarios are traces especially valuable?
- In which scenarios do traces provide almost no value?
- How would you introduce OpenTelemetry in a company from scratch?
- What would you instrument first in a new product?
- How would you prioritize the introduction of OpenTelemetry in an existing platform?
- How do you explain the value of distributed tracing to a manager or product team?
- How do you show the business the value of observability if it is “not a new feature for users”?
- In a new system, what would you choose first: metrics, logs, or tracing? Why?
- How do you build a mature observability strategy using OpenTelemetry?
- What anti-patterns in OpenTelemetry adoption have you seen?
- What mistakes do teams most often make when introducing distributed tracing?
- How do you know that tracing has been implemented well in a company rather than just “formally”?

### 11.9 Networking for DevOps / SRE
- What is a computer network and why should a DevOps / SRE engineer understand networking?
- How do LAN, WAN, VPN, and VPC differ?
- What is the OSI model and why is it useful in practice?
- Which OSI layers does a DevOps / SRE engineer most often have to deal with?
- How is the TCP/IP stack different from the OSI model?
- What is an IP address?
- What is the difference between IPv4 and IPv6?
- What is a public IP address and a private IP address?
- Which private IPv4 ranges do you know?
- What is a loopback address?
- What is localhost?
- How is 127.0.0.1 different from 0.0.0.0?
- What are a subnet mask and CIDR?
- How do you read a notation like 10.0.0.0/24?
- What is a subnet?
- How do you calculate the number of addresses in a /24, /16, /30, or /32 subnet?
- What is a default gateway?
- What is a MAC address?
- How is a MAC address different from an IP address?
- What is ARP and why is it needed?
- What is NAT?
- Why is NAT needed?
- How is SNAT different from DNAT?
- What is PAT?
- What is routing?
- What is a routing table?
- How do you determine where a packet will be sent?
- What is a default route?
- What is a static route?
- What is dynamic routing?
- Which dynamic routing protocols do you know?
- What is DNS?
- How does DNS work at a high level?
- What is a recursive resolver?
- What is an authoritative DNS server?
- What is the difference between A, AAAA, CNAME, MX, TXT, NS, and PTR records?
- What is TTL in DNS?
- Why are DNS changes sometimes “not visible immediately”?
- What is reverse DNS?
- What is split-horizon DNS?
- What is DHCP?
- How does a device get an IP address via DHCP?
- What is lease time in DHCP?
- How is TCP different from UDP?
- When is TCP used, and when UDP?
- What is a port?
- How are well-known ports different from ephemeral ports?
- What is a socket?
- What is the TCP three-way handshake?
- What happens when a TCP connection is closed?
- What are FIN_WAIT, TIME_WAIT, and CLOSE_WAIT?
- Why can TIME_WAIT become a problem?
- What is retransmission in TCP?
- What is window size?
- What is flow control?
- What is congestion control?
- How is latency different from bandwidth?
- How is throughput different from bandwidth?
- What is packet loss?
- What is jitter?
- What is MTU?
- What is MSS?
- What is fragmentation?
- Why can an incorrect MTU break connections?
- What is ICMP?
- How does ping work?
- Why can ping be blocked even though the service is reachable?
- What is traceroute and how does it work?
- How do traceroute on Linux and tracert on Windows usually differ?
- What is a firewall?
- How is a stateful firewall different from a stateless firewall?
- What is an ACL?
- How is a firewall different from a security group?
- How is a security group different from a network ACL in clouds?
- What is a reverse proxy?
- How is a reverse proxy different from a forward proxy?
- What is a load balancer?
- How is an L4 load balancer different from an L7 load balancer?
- What is an ingress controller?
- What is a service mesh?
- What is TLS termination?
- Where is it better to terminate TLS: at the load balancer, ingress, proxy, or application?
- What is mTLS?
- Why is mTLS needed on an internal network?
- What is a VPN?
- How does a site-to-site VPN work?
- What is a bastion host?
- What is a jump host?
- What is an overlay network?
- What is VXLAN?
- What is VLAN?
- What is a broadcast domain?
- What are unicast, multicast, and broadcast?
- What is anycast and where is it used?
- What is a CDN?
- How does a CDN help availability and performance?
- What is a WAF?
- How is a WAF different from a regular firewall?
- What is rate limiting at the network level?
- What is QoS?
- What is port forwarding?
- What is hairpin NAT?
- What is asymmetric routing?
- Why can asymmetric routing break a stateful firewall?
- What is conntrack?
- What is ephemeral port exhaustion?
- Why can a service stop opening outgoing connections?
- What is a SYN flood?
- What is a DDoS attack at the network level?
- What basic protection methods against network attacks do you know?
- What is BGP at a high level?
- What is an ASN?
- Why is BGP important for large networks and clouds?
- What is peering?
- What is a private link / private endpoint in cloud platforms?
- What is egress traffic?
- Why can egress be expensive and important for design?
- How is north-south traffic different from east-west traffic?
- How would you explain the difference between north-south and east-west traffic using a Kubernetes cluster as an example?
- What is a network namespace in Linux?
- What is a bridge interface?
- What is a veth pair?
- How does docker0 work?
- How does a container get networking?
- What is CNI in Kubernetes?
- How does a Pod get an IP address in Kubernetes?
- What are ClusterIP, NodePort, LoadBalancer, and ExternalName?
- How does a request get from outside into a Pod in Kubernetes?
- How does kube-proxy work?
- How is iptables mode different from IPVS mode?
- What is Ingress?
- How is Ingress different from a Service?
- What are egress policy and ingress policy in Kubernetes NetworkPolicy?
- What is NetworkPolicy?
- What can NetworkPolicy not do without additional components?
- How do you restrict network communication between namespaces in Kubernetes?
- What is service discovery?
- How do services discover each other in Kubernetes?
- How does internal DNS work in Kubernetes?
- What is a headless service?
- How is a headless service different from a regular service?
- What are readiness issues from a networking perspective?
- What happens to network connections during a rolling update?
- How do you organize zero-downtime deployment with long-lived connections in mind?
- What networking considerations are important for WebSocket services?
- Why can an idle timeout on a load balancer break WebSocket connections?
- What networking characteristics of HTTP/2 matter for proxies and load balancers?
- What is HTTP keep-alive?
- How does keep-alive affect performance and resources?
- What is connection pooling on the client side?
- Why is a large number of short-lived connections harmful?
- How do you diagnose the problem “the service is reachable locally but not from outside”?
- How do you diagnose the problem “DNS resolves, but the connection is not established”?
- How do you diagnose the problem “a TCP connection exists, but HTTP requests time out”?
- How do you diagnose an intermittent network issue?
- Which commands do you use for basic network diagnostics on Linux?
- When do you use ping, traceroute, mtr, dig, nslookup, curl, nc, ss, and tcpdump?
- Why is ss better than netstat?
- How do you view open ports on a machine?
- How do you determine which process is listening on a specific port?
- How do you check TCP port reachability from a client?
- How do you verify that DNS is returning exactly the address you expect?
- How do you verify a TLS certificate chain?
- How do you inspect SNI and ALPN for a TLS connection?
- How would you debug a TLS handshake issue?
- How would you debug the problem “curl works, but the browser does not”?
- How would you debug the problem “it works from one data center, but not from another”?
- How would you debug network degradation after a release?
- How would you determine that the problem is in DNS rather than in the application?
- How would you determine that the problem is in the load balancer rather than the backend service?
- How would you determine that the problem is MTU-related?
- How would you investigate packet loss between two hosts?
- What does tcpdump show and in which cases do you use it?
- How do you filter only DNS, only TCP SYN, or only traffic to a specific port in tcpdump?
- What is pcap?
- How would you analyze a network dump?
- What is Wireshark and when is it useful?
- How do you assess whether the network is the bottleneck for an application?
- What matters more for different kinds of systems: latency or throughput?
- How do you design a network topology for a highly available service?
- What needs to be considered in networking for a multi-zone architecture?
- What changes in network design in a multi-region architecture?
- What risks arise from having a single central ingress for all services?
- When should you use an internal load balancer instead of an external one?
- When is a dedicated egress gateway needed?
- What trade-offs do you see between network simplicity and isolation level?
- How would you design network segmentation for production/staging/dev?
- How would you design a network for a zero-trust approach?
- How would you reduce the blast radius of network problems?
- What networking questions would you ask a team when designing a new platform?
- What typical networking anti-patterns have you seen in infrastructure?
- What do developers and DevOps engineers most often break in networks?
- Which network metrics do you consider mandatory for platform observability?
- How are network issues related to a service’s SLO?
- How would you assess networking risk during an infrastructure migration?
- How would you organize the networking part of disaster recovery?
- What networking question would you ask a senior DevOps / SRE candidate to quickly understand their level?
- What networking trade-offs would you expect to hear from a Staff+ / architect candidate?
- How would you explain to the business why network design affects reliability, latency, and cost?

### 11.10 Reliability engineering: SLI, SLO, SLA, error budget
- What is reliability engineering?
- How is SRE different from the classic ops approach?
- What is service reliability?
- How do you define a “reliable service”?
- How is reliability different from availability?
- How is reliability different from durability?
- What is an SLI?
- What is an SLO?
- What is an SLA?
- How is an SLI different from an SLO?
- How is an SLO different from an SLA?
- Why shouldn’t an SLA be used as an internal engineering target?
- Why is an error budget needed?
- How are SLO and error budget related?
- What is burn rate?
- Why are SLI/SLO important if the service already has monitoring?
- What advantages do SLOs give a development team?
- What advantages do SLOs give the business?
- Why is “100% availability” almost always a bad or meaningless goal?
- What do “four nines” and “five nines” of availability mean?
- How much downtime per month corresponds to 99%, 99.9%, and 99.99%?
- How do you choose a reasonable SLO target?
- Why can an overly high SLO hurt the product and development speed?
- Why is an overly low SLO also harmful?
- What is a user journey in the context of SLI?
- Why should good SLIs measure user experience rather than just component health?
- What types of SLI do you know?
- Which SLIs are most commonly used for a web service?
- Which SLIs are suitable for an API?
- Which SLIs are suitable for batch systems?
- Which SLIs are suitable for streaming systems?
- Which SLIs are suitable for queues and message brokers?
- Which SLIs are suitable for databases?
- What matters more for different services: availability, latency, throughput, correctness, or freshness?
- What is a request-based SLI?
- What is a window-based SLI?
- When is it better to use request-based SLI, and when window-based SLI?
- What is an availability SLI?
- What is a latency SLI?
- What is a quality SLI?
- What is a correctness SLI?
- What is a freshness SLI?
- What is a durability SLI?
- How do you measure the availability of an HTTP API?
- Which HTTP status codes should be considered successful for an availability SLI?
- Should 4xx be counted as errors in an SLI?
- Should 429 be counted as an error?
- Should canceled requests be counted as errors?
- How do you account for retries when calculating an SLI?
- How do you account for partial failures?
- How do you account for degraded mode?
- How do you account for maintenance windows?
- How do you calculate an SLI for asynchronous processing, where the request is accepted but the job later fails?
- How do you calculate an SLI for a message queue?
- How do you calculate an SLI for a data pipeline?
- How do you calculate an SLI for cron/job systems?
- How do you calculate an SLI for a mobile backend when some errors are caused by the client or the user’s network?
- How do you choose a good latency threshold for an SLI?
- Should a service have one SLI or several?
- How do you know that a chosen SLI is too technical and does not reflect real UX?
- Why are CPU, RAM, and disk usage alone not good SLIs?
- Why is instance uptime not equal to service availability?
- Why does “all pods are green” not mean the service is reliable?
- How are health checks related to SLI?
- What is the difference between black-box and white-box signals for reliability?
- When are synthetic checks more useful than real traffic?
- When can synthetic checks be misleading?
- How do you choose an SLO for an internal service with no direct external user?
- Should internal platform services have their own SLOs?
- How are SLOs inherited across dependent services?
- Why can’t you keep stacking aggressive SLOs across the whole service chain?
- What is a reliability stack?
- How do dependencies affect the achievability of an SLO?
- How would you distribute the reliability budget across frontend, API, database, and external dependencies?
- What is a composite SLI / composite SLO?
- How do you measure reliability for a multi-region service?
- How do you measure reliability for an active-active architecture?
- How do you measure reliability when graceful degradation exists?
- Can you have different SLOs for different endpoints?
- Can you have different SLOs for different client tiers?
- Is it worth having different SLOs for read and write operations?
- When does it make sense to have separate SLOs for background jobs and the online path?
- What is alerting based on SLO?
- Why are SLO-based alerts better than ordinary threshold-based alerts?
- When are ordinary alerts still needed?
- What are fast burn and slow burn alerts?
- Why are multiple burn-rate alert windows needed?
- What do you do if burn rate suddenly spikes?
- What do you do if the error budget is almost exhausted?
- How do you use error budget in release management?
- Should releases be stopped when the error budget is burned?
- How do you explain to developers why they need an error budget?
- How do you explain to product why error budget limits delivery speed?
- How do SLOs help balance feature velocity and reliability?
- What is toil in the context of SRE?
- How is reliability engineering related to reducing toil?
- How are incident management and SLO related?
- How does postmortem affect SLO?
- How should reliability work be prioritized against new features?
- What are signs that SLOs have been chosen incorrectly?
- What anti-patterns occur when introducing SLOs?
- What happens if you choose too many SLIs?
- What happens if a team has no SLOs at all?
- What happens if SLOs exist but the team makes no decisions based on them?
- What happens if SLOs are tied too rigidly to bonuses?
- What are “vanity metrics” in the context of reliability?
- Why is average response time often a poor metric for SLO?
- Why is percentile latency better than average?
- How is p95 different from p99, and when does each matter?
- How do you choose between p90, p95, and p99?
- Why can p99 be noisy?
- How do you account for seasonality and traffic spikes in SLO reporting?
- What is a rolling window for SLO?
- Why is a rolling window better than a calendar month?
- When is a calendar month more convenient for the business and a rolling window more convenient for the team?
- How do you calculate an SLO for a low-traffic service?
- What do you do if a service has too little data for a statistically meaningful SLI?
- How do you calculate an SLI for rare but critical operations?
- How do you measure reliability for B2B integrations where there are few clients but high criticality?
- How do you measure reliability for admin tools?
- How do you measure reliability for internal platform products?
- Can error budget be used for capacity planning?
- How are SLOs related to capacity planning?
- How are SLOs related to disaster recovery?
- How are SLOs related to chaos engineering?
- How are SLOs related to autoscaling?
- How are SLOs related to canary releases and progressive delivery?
- How does reliability engineering help during migrations?
- How does reliability engineering help in multi-cloud or hybrid cloud?
- How do external dependencies affect your SLO?
- Should a third-party API be included in your availability SLI?
- How do you separate your responsibility from someone else’s in reliability measurements?
- How would you build an SLO for an API gateway?
- How would you build an SLO for a task queue?
- How would you build an SLO for a database?
- How would you build an SLO for search?
- How would you build an SLO for an authentication system?
- How would you build an SLO for a CDN-dependent application?
- How would you build an SLO for a Kubernetes platform team?
- How would you build an SLO for a CI/CD system?
- How would you build an SLO for the observability stack?
- How do you connect a technical service’s SLO with product metrics?
- Can reliability be measured by successful completion of a business scenario rather than by technical requests?
- How is reliability related to retention and conversion?
- How do you choose which user scenarios are most critical for SLO?
- What is a critical user journey and how do you identify it?
- How would you introduce SLOs in a team that has not used them before?
- Where do you start when introducing an SRE approach in a small team?
- What would you do first: configure alerts, SLOs, dashboards, or runbooks? Why?
- What data is needed to propose the first SLI/SLOs for a new service?
- What questions would you ask product owners before choosing SLOs?
- How do engineering and product agree on target SLOs?
- How would you present an SLA to a client?
- What risks come with an overly aggressive SLA?
- What legal and financial consequences are associated with an SLA?
- Why is an SLA often made softer than the internal SLO?
- How would you formulate an SLA for a B2B SaaS product?
- How would you design SLOs for an early-stage startup?
- How would you design SLOs for a mature enterprise platform?
- How does the SLO approach for an internal tool differ from the approach for a customer-facing product?
- How do SLI/SLO evolve as a product matures?
- How do you know it is time to revisit an SLO?
- How do you revise SLOs after architectural changes?
- What reliability questions would you ask in an architecture review?
- What reliability questions would you ask in a senior SRE interview?
- What reliability trade-offs would you expect to hear from an architect?
- What real-world cases would you cite where a well-chosen SLO changed a team’s behavior?
- What cases would you cite where the absence of SLOs led to chaos?
- How would you explain to a non-technical stakeholder the difference between SLI, SLO, SLA, and error budget?
- How would you convince a team that reliability is not only an “ops problem” but part of engineering culture?

### 11.11 Incident management and postmortem
- What is an incident in operating systems and services?
- How is an incident different from a bug, problem, defect, and change?
- What are incident severity and priority? What is the difference between them?
- Which incident severity levels do you know?
- What are P0, P1, P2, and P3 incidents? How do they usually differ?
- What criteria would you use to assign an incident a severity level?
- What is a major incident?
- What are outage, degradation, and partial outage?
- What is incident response?
- What are the main stages of incident management?
- Who usually participates in handling an incident?
- What role does the incident commander play?
- What other roles can exist during an incident besides the incident commander?
- Who should make technical decisions during an incident?
- Who is responsible for communication with stakeholders during an incident?
- What is on-call?
- How is on-call organized in an engineering team?
- What are primary and secondary on-call?
- What are the pros and cons of on-call rotation?
- What skills does an engineer on on-call duty need?
- What are alert fatigue and alert storms? Why are they dangerous?
- What is a false positive alert?
- What is a false negative alert?
- What is a runbook?
- What is a playbook? How is it different from a runbook?
- What should a good runbook contain?
- When does a runbook help, and when does it not?
- What is an escalation policy?
- What is a paging policy?
- Which communication channels are usually used during an incident?
- What should go into the first status update about an incident?
- How often should incident status be updated for the business and for users?
- When should an incident be escalated?
- When should a war room be created?
- How do you know that an incident requires involving additional teams?
- What is mitigation?
- How is mitigation different from a root cause fix?
- How is a rollback different from a workaround?
- When is rollback the best option, and when is it dangerous?
- What is blast radius?
- How do you assess blast radius during an incident?
- What are time to detect, time to acknowledge, time to mitigate, and time to resolve?
- Which incident management metrics do you consider key?
- When can an incident be considered closed?
- How do you determine the moment when the system can already be considered restored?
- What is an incident timeline?
- Why is it important to record the exact sequence of events during an incident?
- What is a postmortem?
- Why is a postmortem needed after an incident?
- When is a postmortem mandatory, and when is it optional?
- What is a blameless postmortem?
- Why is a blameless approach important?
- How is a blameless postmortem different from trying to “find the guilty party”?
- What usually goes into a good postmortem?
- How do you structure a postmortem document?
- What should be in the impact section?
- What should be in the root cause section?
- What should be in the contributing factors section?
- What should be in the action items section?
- How is a root cause different from a contributing factor?
- What are proximate cause and systemic cause?
- How do you distinguish an incident symptom from its cause?
- What are the five whys? When is this method useful?
- What other root cause analysis methods do you know?
- What is a fishbone diagram and is it applicable in a postmortem?
- What mistakes are often made in root cause analysis?
- Can an incident have multiple root causes?
- What do you do if the exact root cause is unknown?
- How do you write action items after a postmortem so that they are useful?
- Which action items are considered poor?
- How are corrective, preventive, and detective action items different?
- How do you prioritize action items after an incident?
- Who should own action items?
- How do you verify that action items are actually completed?
- What do you do if action items keep accumulating but the team does not execute them?
- When should you change the architecture after an incident, and when are operational measures enough?
- When should alerts be changed after an incident?
- When should dashboards be added after an incident?
- When should a runbook be rewritten after an incident?
- When does an incident point to a process problem rather than a code problem?
- How do you determine whether an incident was caused by human error or by a systemic deficiency?
- How do you investigate an incident if logs are insufficient?
- How do you investigate an incident if metrics contradict logs?
- How do you investigate an incident in a distributed system?
- How does a correlation ID help during incident investigation?
- How does distributed tracing help in incident response?
- What should you do if a problem reproduces only in production?
- What should you do if the incident has already ended by the time the investigation begins?
- What do you do if, during an incident, it is unclear whether the cause is in the application, infrastructure, or a third-party service?
- How do you act if multiple alerts from different systems are firing at the same time?
- How would you investigate an incident if the number of 5xx errors suddenly increased after a release?
- How would you investigate an incident if latency increased but CPU and memory look normal?
- How would you investigate an incident if some users report downtime while others do not?
- How would you investigate an incident if everything appears to work externally but business metrics have dropped sharply?
- What do you do if an incident happens at night and the system experts are currently unavailable?
- What do you do if the only person who knows the system is unavailable?
- How do you reduce bus factor in the context of incident response?
- What do you do if the cause of the incident is a dependency on a third-party provider?
- What do you do if a cloud provider degrades in one region?
- What do you do if a cascading failure starts between services?
- What do you do if the message queue overflows and the system starts lagging behind?
- What do you do if the database starts timing out under load?
- What do you do if you detect a memory leak in production?
- What do you do if a new release has only partially rolled out to some instances?
- What do you do if rollback does not help either?
- How do you make decisions under pressure during an incident?
- How do you avoid chaos in communication during an incident?
- How do you separate technical investigation from business communication?
- How do you write status updates for non-technical stakeholders?
- How do you explain to the business that the service is “formally up” but actually degraded?
- How do you explain to users what happened without disclosing unnecessary internal details?
- When should a public incident report be published?
- What should not be written in a public incident review?
- What can safely be published in external incident communication?
- How are incidents related to the error budget?
- When should incidents influence the team’s roadmap?
- When should feature development be frozen after a series of incidents?
- What is operational excellence?
- How would you improve a team’s incident readiness?
- Which practices help reduce MTTR?
- Which practices help reduce MTTD?
- How do you make sure incidents are detected before users notice them?
- How are resilience testing and incident management related?
- What are game days?
- What is chaos engineering and how is it related to incident readiness?
- What anti-patterns in incident management have you encountered?
- What anti-patterns occur in postmortems?
- Why is it dangerous to close an incident without a deep analysis?
- Why is it dangerous to write a postmortem only as a checkbox exercise?
- What questions would you ask the team after a serious incident as an engineer?
- What questions would you ask the team after a serious incident as a team lead?
- What questions would you ask the team after a serious incident as an EM or Head of Engineering?
- How would you build a postmortem process in a fast-growing startup?
- How would you build a major incident management process in a large organization?
- How do you balance restoration speed against the risk of making things worse?
- When should you deliberately degrade part of the functionality for the sake of overall stability?
- How do you decide which functionality can be degraded and which cannot?
- Which architectural decisions most strongly reduce the likelihood of severe incidents?
- How would you introduce a culture of high-quality incident reviews in a team that is used to hiding them?
- How would you respond if the team blames “human factor” after every incident?
- How do you assess whether the postmortem process is actually improving the system rather than existing formally?
- Tell me about the most complex incident you have investigated.
- Tell me about an incident where the initial hypothesis turned out to be wrong.
- Tell me about an incident where mitigation worked but the root problem remained.
- Tell me about an incident that taught you to change processes, not just code.
- Tell me about a case where a postmortem led to a noticeable improvement in system reliability.

### 11.12 Cloud platforms
- What are cloud platforms?
- How is the cloud different from traditional on-premise infrastructure?
- Which cloud service models do you know?
- What is the difference between IaaS, PaaS, and SaaS?
- What is the difference between public cloud, private cloud, and hybrid cloud?
- What is multicloud?
- How is multicloud different from hybrid cloud?
- What are the advantages of the cloud for a product team?
- What disadvantages of the cloud do you see?
- When is the cloud a good choice, and when is it not?
- Which major cloud providers do you know?
- Which cloud platforms have you worked with?
- Which basic services exist in almost any cloud provider?
- What is usually included in compute services in the cloud?
- What is usually included in storage services in the cloud?
- What is usually included in network services in the cloud?
- What is usually included in managed database services in the cloud?
- What are a region and an availability zone?
- How is a region different from an availability zone?
- Why is it important to design a system with multiple availability zones in mind?
- When should multiple regions be used?
- What trade-offs appear in a multi-region architecture?
- What is the shared responsibility model?
- What is the cloud provider usually responsible for, and what is the customer responsible for?
- How does the shared responsibility model change for IaaS, PaaS, and SaaS?
- What is a virtual machine in the cloud?
- How is a VM different from a container?
- What are instance types and why are they needed?
- How do you choose an instance type for a task?
- How is a general purpose instance different from a compute optimized or memory optimized one?
- What is autoscaling?
- Which metrics can be used for autoscaling?
- What are the risks of autoscaling?
- What is serverless?
- How is serverless different from containers and VMs?
- When is serverless a good choice?
- What limitations does the serverless approach have?
- What is a cold start?
- Why is cold start important for serverless functions?
- What is object storage?
- How is object storage different from block storage and file storage?
- For which scenarios is object storage suitable?
- For which scenarios is object storage not suitable?
- What is a CDN and how is it related to the cloud?
- What is a load balancer in the cloud?
- Which types of load balancer do you know?
- How is an L4 load balancer different from an L7 load balancer?
- What is a VPC / virtual network?
- Why are private and public subnets needed in the cloud?
- What is a route table?
- What are security groups and network ACLs?
- What is the difference between stateful and stateless network rules?
- What is a NAT gateway / NAT instance?
- Why is a bastion host needed?
- What is IAM?
- What are a role, policy, user, and group in IAM?
- Why is it undesirable to use long-lived credentials?
- What are temporary credentials?
- What is the best way to grant a service access to cloud resources?
- What is least privilege in the context of the cloud?
- What is a managed service?
- What are the advantages of managed services?
- What are the risks of managed services?
- When is it better to use a managed database, and when is self-hosted better?
- What are the pros and cons of managed Kubernetes in the cloud?
- What are the pros and cons of managed message brokers?
- What is a cloud-native application?
- What are the characteristics of a cloud-native architecture?
- What is immutable infrastructure?
- What is pets vs cattle?
- Why is cloud infrastructure usually described as code?
- Why is it important to use tagging / labels for cloud resources?
- Which tags would you always add to cloud resources?
- Why is cost allocation tagging needed?
- How do you track who created a given resource and why?
- What is drift in cloud infrastructure?
- How does drift arise between declarative configuration and the real state?
- What are quotas and service limits in the cloud?
- How can service limits lead to an incident?
- What typical mistakes do teams make when first moving to the cloud?
- What typical mistakes occur in cloud network configuration?
- What typical mistakes occur in IAM configuration?
- What typical mistakes occur when storing secrets in the cloud?
- What typical mistakes occur when using object storage?
- What are encryption at rest and encryption in transit?
- How is data encryption usually organized in cloud services?
- What is KMS / key management service?
- When should customer-managed keys be used?
- What risks arise from improper key management?
- What is a secret manager?
- Why is storing secrets in environment variables sometimes not enough?
- What is cloud logging?
- What is cloud monitoring?
- What kinds of metrics are usually collected in the cloud?
- What are audit logs / activity logs in the cloud?
- Why are audit logs important for security and investigations?
- How do you organize centralized logging in a cloud environment?
- How do you organize monitoring of health checks and availability in the cloud?
- How do you organize disaster recovery in the cloud?
- What is a backup policy in the cloud?
- How do you verify that a backup can actually be restored?
- What are RPO and RTO in the context of the cloud?
- How would you design a fault-tolerant system in a single region?
- How would you design a fault-tolerant system across multiple regions?
- How would you organize failover between regions?
- What difficulties arise when replicating data across regions?
- What problems arise with a multi-region database?
- What is eventual consistency in distributed cloud systems?
- How do CDN, object storage, and edge locations help performance?
- What is edge computing?
- When is an edge approach truly needed?
- What is cloud migration?
- Which cloud migration strategies do you know?
- What are rehost, replatform, and refactor?
- When is rehost justified?
- When is lift-and-shift a bad idea?
- How would you migrate a monolithic application to the cloud?
- How would you migrate a stateful system to the cloud?
- How would you migrate a database to the cloud with minimal downtime?
- How would you design network isolation between dev, staging, and prod environments?
- How would you organize developer access to production resources?
- How would you restrict cloud permissions for different team roles?
- How would you organize multiple accounts / subscriptions / projects in the cloud?
- What advantages does a multi-account strategy provide?
- How do you organize a shared services account / project?
- What is a landing zone in the cloud?
- What should be included in a good cloud governance model?
- What is policy as code?
- How do you enforce security and configuration standards in the cloud?
- How do you control the creation of “forgotten” and unused resources?
- How do you identify expensive resources that provide no value?
- What is vendor lock-in in the cloud?
- What are the main sources of vendor lock-in?
- How do you reduce vendor lock-in without losing the benefits of managed services?
- When is fighting vendor lock-in excessive?
- How do you decide what is better: speed of delivery or portability across clouds?
- How would you choose a cloud provider for a new project?
- What criteria matter when choosing a cloud platform?
- How do compliance, data residency, and regulatory requirements affect cloud platform choice?
- How do team ecosystem and existing skills affect cloud platform choice?
- How does cost affect cloud platform choice?
- How does the availability of managed services affect cloud platform choice?
- How do you evaluate the TCO of a cloud solution?
- Why can a cloud bill suddenly increase?
- Which cloud services most often become the source of unexpected costs?
- How would you investigate a sharp spike in cloud costs?
- How do you prevent accidental deletion of a critical cloud resource?
- How do you prevent accidental exposure of data from object storage?
- How can cloud configuration lead to a large-scale data leak?
- Which cloud security misconfigurations do you consider the most dangerous?
- What is zero trust in a cloud context?
- How do cloud platforms help build a zero trust model?
- What is the minimum set of cloud skills a backend/fullstack developer should know?
- What should a DevOps engineer know about cloud platforms at the middle level?
- What should an architect know about cloud platforms at the system design level?
- How would you explain to the business why moving to the cloud is worthwhile?
- How would you explain to the business why not everything should be moved to the cloud?
- How would you defend the decision to use managed services to a team that wants to self-host everything?
- How would you defend the decision not to use Kubernetes in the cloud for a small project?
- How would you design a cloud platform for a team of 10 developers?
- How would you design a cloud platform for multiple product teams?
- How would you organize dev/stage/prod environments in the cloud?
- How would you organize ephemeral environments for pull requests in the cloud?
- How would you design secure CI/CD access to the cloud?
- How would you manage secrets for CI/CD in a cloud environment?
- How would you organize auditing of user and service actions in the cloud?
- How would you build a platform layer on top of the cloud for internal teams?
- Tell me about a case where the cloud significantly simplified your architecture.
- Tell me about a case where a managed service created unexpected limitations.
- Tell me about a case where a cloud configuration mistake led to an incident.
- Tell me about a case where you had to optimize the cost of cloud infrastructure.
- Tell me about a case where you chose between several cloud approaches and why you chose one of them.

### 11.13 Infrastructure Cost and Optimization

#### 11.13.1 FinOps Fundamentals
- What is FinOps?
- Why does a company need to manage infrastructure costs at all?
- How is FinOps different from simply “cutting expenses”?
- What are the main goals of the FinOps approach?
- Who is usually responsible for FinOps in a company?
- Why is FinOps not only a finance team responsibility?
- What role do DevOps, SRE, architects, and developers play in FinOps?
- What is cloud spend?
- What is total cost of ownership (TCO) in infrastructure?
- What are unit economics in the context of infrastructure?
- What basic categories of infrastructure costs do you know?
- Which costs are related to compute?
- Which costs are related to storage?
- Which costs are related to network?
- Which costs are related to managed services?
- Which hidden infrastructure expenses are often underestimated?
- Why can data storage costs grow unexpectedly?
- Why does outbound network traffic often become an unpleasant surprise?
- Why are idle resources dangerous from a cost perspective?
- Why is overprovisioning so common in real systems?
- What typical causes of infrastructure budget overspend do you know?
- Which cost metrics should be monitored daily, weekly, and monthly?
- What questions should you ask the team if the cloud bill suddenly doubles?
- How do you find the main cause of infrastructure cost growth?
- How do you determine whether higher costs are justified by business growth rather than inefficiency?
- What principles of chargeback, showback, and no-back do you know?
- What is the difference between chargeback and showback?
- When should a company introduce chargeback between teams?
- What problems can arise with a chargeback model?
- Why are tags and labels needed for cost management?
- Which mandatory tags for infrastructure resources would you introduce?
- What happens if resource tags are poorly organized in the cloud?
- What is the minimum set of tag attributes needed for cost allocation?
- How do you organize resource ownership so that it is clear who is paying for what?
- How would you build an infrastructure budgeting process for multiple teams?
- How do you set a budget for a team if the product is growing quickly and the load is unstable?
- How do you estimate the budget for a new system before launch?
- How would you explain to developers that infrastructure costs are part of their responsibility?
- How do you build cost awareness into engineering culture?
- How do you avoid turning FinOps into a brake on development?
- What trade-offs exist between development speed and cost savings?
- When does saving on infrastructure hurt the business?
- When, on the contrary, is overspending justified?
- What key FinOps KPIs and metrics do you know?
- What is cost per user?
- What is cost per request?
- What is cost per transaction?
- What is cost per environment?
- What is cost per team or cost per product line?
- Which unit metrics are suitable for a B2C product?
- Which unit metrics are suitable for B2B SaaS?
- Which unit metrics are suitable for a high-load API?
- Which unit metrics are suitable for a data platform?
- What is more important: absolute infrastructure cost or cost per business unit? Why?
- How do you connect engineering metrics with financial metrics?
- How do you know that a system became more expensive but also more efficient?
- How would you measure cloud efficiency?
- What is rightsizing?
- What are commitments, reserved capacity, and savings plans, and when should they be used?
- What risks do long-term cloud discount models have?
- How do you know when it is worth buying reserved instances or similar instruments?
- When are spot/preemptible instances appropriate, and when are they not?
- How do you assess the risk of using spot/preemptible instances?
- Which workloads are best suited for spot/preemptible?
- Which types of workloads should not be thoughtlessly moved to cheap interruptible resources?
- Which simple steps produce the fastest effect in optimizing a cloud bill?
- What would you check first if you needed to cut costs by 20% in a month?
- What would you check if you needed to cut costs by 50% without significantly degrading the SLA?
- Which anti-patterns in cloud consumption have you encountered?
- Why can “scale first, think about cost later” be a dangerous strategy?
- How do you organize regular infrastructure cost reviews?
- How often should cost reviews be conducted?
- Who should participate in a cost review?
- Which artifacts and reports should be prepared for a cost review?
- What should happen after an expensive but unused resource is discovered?
- How do you build a process for deleting forgotten resources and environments?
- How do you deal with “zombie resources”?
- How do you control the cost of dev/stage/test environments?
- How do you ensure that temporary environments are automatically deleted?
- How do you estimate the cost of a specific feature before launch?
- How do you integrate cost estimates into the architectural review process?
- Should cost be discussed at the level of ADRs/design documents? Why?
- Which cost-related questions would you definitely add to an architectural review?
- How would you explain the difference between CapEx and OpEx in the context of the cloud?
- How is the cloud financially different from on-premise?
- In which cases can on-premise be more profitable than the cloud?
- In which cases is the cloud almost always more profitable than on-premise?
- How do you calculate the ROI of a cloud migration?
- How do you prove to the business that a more expensive architecture is ultimately cheaper?
- How do you prove to the business that a cheap architecture will ultimately cost more?
- Which organizational mistakes most often break FinOps practices?
- Which engineering mistakes most often break FinOps practices?
- What FinOps maturity model do you know, or how would you describe FinOps maturity levels?
- How would you introduce FinOps in a company where engineers do not think about cost at all?
- How would you introduce FinOps in a startup?
- How would you introduce FinOps in a large company with many teams?
- How would you introduce FinOps in a company where there is already overspend and distrust between finance and engineering?
- Which processes and rituals would you create for FinOps?
- Which cost alerts would you configure?
- Which cost anomalies must be detected automatically?
- How do you distinguish a real cost anomaly from an expected load spike?
- How would you investigate a cost anomaly?
- How would you build an infrastructure cost dashboard for a CTO?
- How would you build a cost dashboard for an engineering manager?
- How would you build a cost dashboard for a development team?
- What decisions would you make if the CFO demanded a sharp cost reduction while the business demanded high availability?
- How do you explain the trade-off between reliability, performance, and cost to the team?
- Have you ever actually reduced infrastructure costs? What exactly did you do?
- Which optimizations yield a quick effect but may cause harm later?
- Which optimizations are almost always safe?
- Which optimizations seem safe but are actually dangerous?
- How do you prioritize infrastructure cost-saving initiatives?
- What is more important: optimizing the most expensive service or the most inefficient one? Why?
- What do you do if the biggest costs come from a business-critical service that is scary to touch?
- How do you evaluate the ROI of infrastructure optimization?
- How would you present a cost optimization initiative to management?
- What would you do in the first 30 days in an engineering role responsible for cost optimization?
- Which FinOps questions would you ask a candidate for a senior/staff/architect role?
- Which candidate answers on the topic of FinOps would be red flags for you?

#### 11.13.2 Cost-Aware Architecture
- What is cost-aware architecture?
- How is cost-aware architecture different from ordinary “architecture for load”?
- Why should cost be considered already at the system design stage?
- How early in the product lifecycle should you start thinking about cost?
- Which architectural decisions influence operating cost the most?
- What are the main cost drivers in a distributed system?
- How does architecture affect compute cost?
- How does architecture affect storage cost?
- How does architecture affect network cost?
- How does architecture affect the operational cost of the team?
- How does architecture affect observability cost?
- Why can an overly granular microservices architecture sharply increase costs?
- When can a modular monolith be more advantageous than microservices not only technically but also financially?
- How do you compare the cost of a monolith, a modular monolith, and microservices?
- What hidden cost trade-offs do microservices have?
- How does service-to-service communication affect cost?
- Why does chatty architecture often hurt not only latency but also cost?
- How do frequent inter-service calls increase the network bill?
- When does an API gateway reduce cost, and when does it just add another layer of expense?
- When is a BFF justified, and when does it simply add infrastructure overhead?
- How does event-driven architecture affect cost?
- In which cases does a message broker actually save resources, and in which cases does it only complicate and make the system more expensive?
- How does Kafka affect infrastructure cost?
- What cost trade-offs does RabbitMQ have?
- When is a queue cheaper than synchronous calls, and when is it more expensive?
- How can asynchronous processing reduce system cost?
- When is batch processing more cost-effective than real-time processing?
- When is a real-time architecture not economically justified?
- What cost trade-offs exist between polling, webhooks, SSE, and WebSocket?
- When does a WebSocket architecture become expensive?
- When does long-polling turn out to be more expensive than it seems?
- How does caching affect cost?
- When does caching actually save money?
- When does caching increase costs?
- What are the cost trade-offs of a Redis cache?
- How do you choose what to cache if the goal is to reduce cost?
- How do you determine that the cache already costs too much relative to its benefit?
- How does TTL strategy affect cost?
- How can an overheated cache lead to unnecessary expenses?
- When does a CDN help not only with performance but also with cost?
- When can a CDN unexpectedly increase costs?
- How do you choose between storage classes for object storage?
- When is hot/warm/cold tiering justified?
- What are the cost trade-offs of S3-compatible storage and object storage in general?
- How do lifecycle policies reduce storage cost?
- When are archival storage tiers beneficial, and when do they create operational pain?
- How does retention policy affect cost?
- Why do logging and tracing storage often become a major cost item?
- How do you design observability so that it is useful but not financially ruinous?
- What cost trade-offs exist between full logging, sampling, and aggregation?
- How does sampling in tracing affect cost and diagnostic quality?
- How do you design metrics and logs in a cost-aware way?
- When is it cheaper to store less data than to optimize the storage backend?
- How does database architecture affect cost?
- When is one large database cheaper and better than several separate ones?
- When is splitting databases by service justified despite higher cost?
- How can the choice of PostgreSQL vs MySQL vs MongoDB vs ClickHouse affect cost?
- How does a managed database affect cost compared with self-hosted?
- When is a managed service financially justified even with a higher price?
- When can a self-hosted solution be cheaper but ultimately cost more?
- What cost trade-offs does serverless architecture have?
- When is serverless cheaper than virtual machines and containers?
- When does serverless become too expensive?
- Which workload patterns are especially poorly suited to serverless from a cost perspective?
- How do cold starts affect not only latency but also the economics of a solution?
- When are containers cheaper than serverless, and when is it the other way around?
- How does Kubernetes affect the cost of platform ownership?
- What hidden costs does Kubernetes introduce?
- When is Kubernetes economically justified?
- When is Kubernetes overkill and overspending for a team?
- How do you properly account for the cost of the control plane, ingress, observability, service mesh, and platform team?
- Why can service mesh be an expensive solution?
- When is service mesh justified from a business and cost perspective?
- How does multi-region deployment affect cost?
- When is multi-region mandatory despite the price?
- When is an active-active architecture too expensive for real business requirements?
- How do you choose between active-active and active-passive with cost in mind?
- How does HA affect architecture cost?
- How do you explain the price of high availability to the business?
- How do you explain the price of low RTO/RPO?
- How does system cost change as the SLA becomes stricter?
- How would you calculate the cost of moving from 99.9% to 99.99% availability?
- Why do extra “nines” cost disproportionately more?
- What architectural approach would you choose if the budget is limited and SLA requirements are moderate?
- What approach would you choose if SLA requirements are strict and the budget is limited?
- How do you make architecture sufficiently reliable without overinvesting?
- What is overengineering from a cost perspective?
- What are the signs of an overly expensive architecture?
- How do you recognize an architecture that is technically beautiful but economically unjustified?
- How do you assess the cost impact of a new architectural initiative?
- Which cost-related questions should definitely be asked in a system design interview?
- Which cost trade-offs would you discuss when designing an API-heavy system?
- Which cost trade-offs would you discuss when designing a high-load backend?
- Which cost trade-offs would you discuss when designing a data pipeline?
- Which cost trade-offs would you discuss when designing a realtime chat?
- Which cost trade-offs would you discuss when designing video streaming?
- Which cost trade-offs would you discuss when designing an e-commerce platform?
- Which cost trade-offs would you discuss when designing an ML/analytics platform?
- How can queue and retry architecture increase cost?
- When do aggressive retries start harming both reliability and budget?
- How do you protect against uncontrolled cost growth caused by retry storms?
- How does a circuit breaker help not only reliability but also cost?
- How can rate limiting be used as a cost-control tool?
- How does graceful degradation help keep costs under control during traffic spikes?
- How does backpressure help not only platform stability but also platform economics?
- Which architectural decisions reduce the cost of dev/stage/test environments?
- How do you design ephemeral environments so that they are convenient and inexpensive?
- How does a cost-aware approach affect multi-tenancy?
- When is a multi-tenant architecture more advantageous than single-tenant?
- When is a single-tenant architecture justified despite the higher cost?
- What are the cost trade-offs of tenant isolation?
- How do you design batch jobs so that they are inexpensive?
- How do you design cron jobs so that they do not create unnecessary expense?
- How do you design a storage strategy for large volumes of media files?
- How do you design data retention for audit, analytics, and compliance with cost in mind?
- How does the choice of data format affect storage and transfer cost?
- When are Protobuf or Avro more beneficial than JSON?
- How does compression affect network and storage cost?
- When does compression save money, and when does it just consume CPU?
- How do you choose between CPU-heavy and memory-heavy instances from a cost perspective?
- How does rightsizing affect application architecture?
- Which architectural decisions reduce memory requirements?
- Which decisions reduce CPU requirements?
- Which decisions reduce network usage?
- Which decisions reduce storage cost?
- How do you design a system so that it can scale cheaply?
- What does “scales cheaply” and “scales expensively” mean?
- How do you know that the current architecture does not have good cost scalability?
- What is cost per additional user/request/job, and why is it worth calculating?
- How do you architecturally improve cost per request?
- How do you architecturally improve cost per tenant?
- How do you architecturally improve cost per GB processed?
- How do you use workload profiling for cost-aware architecture?
- How much do debug environments, shadow traffic, replay, and test stands contribute to cost?
- When is it worth reducing part of observability in non-critical environments?
- Which architectural compromises would you choose if you urgently needed to reduce costs by 30%?
- What would you optimize in the architecture first if the cloud bill doubled?
- How do you distinguish a bad architecture problem from a bad operations problem?
- How would you defend the decision to make the system simpler but cheaper if the team wants something “more modern and scalable”?
- How would you defend a more expensive architecture if it provides better reliability or team velocity?
- Have you ever redesigned architecture specifically to reduce costs? What exactly did you change?
- Which architectural decisions most often provide the worst price-to-value ratio in your opinion?
- Which decisions most often provide the best price-to-value ratio?
- What would you check first in a system architecture if the goal is to reduce cost without noticeably harming UX and SLA?
- How would you incorporate cost review into the process of architectural changes?
- Which sections should a cost-aware architectural document contain?
- How would you explain to a junior engineer that good architecture is not only about scalability and cleanliness but also about operating cost?
- Which candidate answers on the topic of cost-aware architecture do you consider strong?
- Which candidate answers on the topic of cost-aware architecture do you consider concerning or naive?
